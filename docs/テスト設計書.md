# TripDiary テスト設計書

**バージョン:** 1.1
**作成日:** 2026-07-05
**更新日:** 2026-07-06（Phase 3: ユーザー機能・検索・地図のテスト追加、E2E安定化の知見を反映）
**作成者:** Nakata Saki（Claude Code支援）

---

## 1. テスト方針・目的

Phase 3以降でAPIエンドポイントが急増する前に、単体・統合テストの基盤を整備する（Phase 2.5-B）。
本番影響を避けつつ、権限チェック・バリデーション境界値・DB連携ロジックといった不具合が起きやすい箇所を優先的にカバーする。

同コース内の別課題 TimeLine（Spring Boot）のテスト実装（Controller/Service/Repositoryの3層戦略、`対象_条件_期待結果` の命名規則）を参考にしつつ、TripDiary（Next.js + Prisma + MySQL）向けに翻訳して適用した。

---

## 2. テストレベルと範囲

| レベル | 対象 | 今回の扱い |
|---|---|---|
| 単体テスト | Service層（ホワイトボックス）、Zodバリデーションスキーマ、フロントUIコンポーネント | ✅ 対象 |
| 結合テスト（薄い） | Route Handler（Controller層、依存モック） | ✅ 対象 |
| 結合テスト（実DB） | Repository層 | ✅ 対象 |
| E2Eテスト | ブラウザ経由の画面横断シナリオ | ✅ 対象（認証フローに加え、投稿の主要フローを追加） |
| 性能テスト | 負荷・応答速度 | ❌ 対象外（Phase 5-A） |

---

## 3. 使用ツール・環境

- **テストランナー**: Vitest 4.1.9（`@vitejs/plugin-react` 6.0.3 + `vite` 8.1.3。導入時はvite 8がpostcss未リリースバージョンを要求し一時的に3.2.6/4.7.0へ固定していたが、postcssリリース後に最新版へ更新済み）
- **フロントエンド**: `@testing-library/react` / `@testing-library/jest-dom` / `@testing-library/user-event`、環境は `jsdom`（ファイル先頭に `// @vitest-environment jsdom` を指定）
- **バックエンド**: 環境は `node`（デフォルト）
- **E2E**: `@playwright/test`（`reporter: [html, json, list]` を設定、`playwright-report/` にレポート出力）
- **ORM/DB**: Prisma 5.22 + MySQL 8.0（テスト用コンテナ）
- **カバレッジ**: `@vitest/coverage-v8`
- **環境変数読み込み**: `dotenv`（`vitest.config.ts` 起動時に `.env.test` を読み込み、`dotenv-cli` で Prisma CLI 実行時にも同様に注入）

---

## 4. テストデータ・DB戦略

- `docker-compose.yml` に `mysql-test` サービスを追加（ポート3307、DB名 `tripdiary_test`、`tmpfs` マウントで永続化しない使い捨てDB）
- `.env.test`（gitignore対象）で `DATABASE_URL` をテスト用DBに向ける。開発用DB（`.env` → ポート3306）とは完全に分離
- スキーマ適用: `pnpm prisma:migrate:test`（`dotenv-cli` で `.env.test` を注入して `prisma migrate deploy`）
- Repository層テストは各 `describe` の `beforeEach` でテーブルをクリーンアップ（`SET FOREIGN_KEY_CHECKS=0` で外部キー制約を一時解除し関連テーブルを `deleteMany`）してからデータを投入する方式を採用。Prisma+MySQLではトランザクション分離のみで安全に隔離するのが難しいため、テストごとの明示的クリーンアップとした
- 本番用DBには一切接続しないため、誤って本番データに影響するリスクを構造的に排除
- **⚠️ 2026-07-06 追記**: Repository層テストファイルが複数存在する場合、Vitestの既定（ファイル間並列実行）のままだと各ファイルの `beforeEach` クリーンアップが共有テーブル（`users`等）に対して競合し、他ファイルが投入中のデータを消してしまい外部キー制約違反で失敗することがある（Phase 3-C で `follow.repository.test.ts` を追加した際に発覚）。`vitest.config.ts` に `fileParallelism: false` を設定し、テストファイルを直列実行することで回避した

---

## 5. テスト観点

- **正常系**: 期待するステータスコード・レスポンス形状
- **異常系**: 401（未認証）・403（他人のリソース操作）・404（存在しないID）・400（バリデーション）・409（メール重複）
- **境界値分析**: 例）`title` 40文字（正常）/ 41文字（異常）、`rating` 1・5（正常）/ 0・6（異常）、`body` 2000文字（正常）/ 2001文字（異常）、`nickname` 20文字（正常）/ 21文字（異常）、`password` 8文字（正常）/ 7文字（異常）
- **同値分割**: カテゴリの妥当な値／不正な値、confirmPasswordの一致／不一致
- **権限・認可**: 本人/他人の分岐を両方検証し、権限NG時にrepositoryの更新系関数が**呼ばれないこと**まで確認する

---

## 6. 層別戦略と命名規則

| 層 | 手法 | モック対象 |
|---|---|---|
| Controller（Route Handler） | ブラックボックス・薄い結合テスト | `@/lib/auth`（`vi.mock`）、依存するservice/repository |
| Service | ホワイトボックス・純粋単体 | repository（`vi.mock`） |
| Repository | 実DB検証 | モックしない（テスト用MySQLに対して実行） |

**命名規則**: `対象_条件_期待結果`（例: `createPost_タイトル40文字_201`、`updatePost_他人の投稿_403`）。境界値は正常・異常のペアで書く。対象メソッドごとに `// ─── 対象名 ───` のコメント区切りでグルーピングする。

---

## 7. 機能別テストケース表（実装済み分）

| ID | 対象 | 前提 | 入力 | 期待結果 | 観点 |
|---|---|---|---|---|---|
| T-01 | `createPostService` | - | title 40文字 | repositoryが呼ばれる | 境界値（正常） |
| T-02 | `updatePostService` | 投稿が存在しない | 任意 | `NotFoundError` | 異常系 |
| T-03 | `updatePostService` | 他人の投稿 | 任意 | `ForbiddenError`、repository更新は呼ばれない | 権限・副作用非発生 |
| T-04 | `updatePostService` | 本人の投稿 | 有効な入力 | repositoryが正しい引数で呼ばれる | 正常系 |
| T-05 | `deletePostService` | 投稿が存在しない | - | `NotFoundError` | 異常系 |
| T-06 | `deletePostService` | 他人の投稿 | - | `ForbiddenError`、repository削除は呼ばれない | 権限・副作用非発生 |
| T-07 | `deletePostService` | 本人の投稿 | - | repositoryの削除が呼ばれる | 正常系 |
| T-08 | `toggleLikeService` | いいねON | - | 通知が作成される | 副作用 |
| T-09 | `toggleLikeService` | いいねOFF | - | 通知は作成されない | 副作用非発生 |
| T-10 | `toggleLikeService` | 投稿の著者が見つからない | - | 通知は作成されない | 異常系 |
| T-11 | `signupService` | メール重複あり | - | `ConflictError`、ユーザー作成は呼ばれない | 異常系・副作用非発生 |
| T-12 | `signupService` | メール重複なし | 有効な入力 | ハッシュ化されたパスワードでユーザー作成 | 正常系 |
| T-13 | `GET /api/posts` | 未認証 | - | 401 | 異常系 |
| T-14 | `GET /api/posts` | 認証済み | - | 200、投稿一覧 | 正常系 |
| T-15 | `POST /api/posts` | 未認証 | - | 401 | 異常系 |
| T-16 | `POST /api/posts` | 認証済み | 必須項目欠落 | 400 | 異常系 |
| T-17 | `POST /api/posts` | 認証済み | title 41文字 | 400 | 境界値（異常） |
| T-18 | `POST /api/posts` | 認証済み | title 40文字 | 201 | 境界値（正常） |
| T-19 | `findFollowingPosts` | フォロー中/自分/他人の投稿が混在 | - | フォロー中と自分の投稿のみ取得 | 正常系（実DB） |
| T-20 | `findFollowingPosts` | 複数投稿 | - | 新着順で並ぶ | 正常系（実DB） |
| T-21 | `findLocationCounts` | エリア別に複数投稿 | - | エリアごとの件数が正しく集計される | 正常系（実DB） |
| T-22〜T-30 | `postSchema` / `signupSchema` / `signupApiSchema` / `loginSchema` | - | 各境界値・同値分割 | 成功/失敗 | 境界値・同値分割 |
| T-31〜T-33 | `ToastContainer` | トースト0件/1件/複数件 | - | 表示内容が正しい | UIコンポーネント |
| T-34 | `NotificationList.handleRead` | 既読API成功 | - | 既読状態に更新される | 楽観的UI |
| T-35 | `NotificationList.handleRead` | 既読API失敗 | - | 楽観的更新がロールバックされる | 楽観的UIロールバック |
| T-36 | E2E: 投稿作成 | ログイン済み | フォーム入力 | ホーム画面にハイライト表示される | E2E・主要フロー |
| T-37 | E2E: 投稿詳細 | 投稿作成済み | - | タイトル・本文が表示される | E2E・主要フロー |
| T-38 | E2E: いいね | 投稿詳細表示中 | いいねボタン押下 | トグルされ件数が1増える（実POST完了を確認） | E2E・主要フロー |
| T-39 | E2E: コメント投稿 | 投稿詳細表示中 | コメント入力・送信 | 一覧に反映される（実POST 201を確認） | E2E・主要フロー |
| T-40 | E2E: 投稿削除 | 投稿詳細表示中 | 削除確認 | ホーム画面へ遷移する | E2E・主要フロー |
| T-41 | `toggleFollowService` | 自分自身をフォロー | - | `ValidationError`、repository呼ばれない | 異常系・副作用非発生 |
| T-42 | `toggleFollowService` | フォローON/OFF | - | ONの時のみ通知作成 | 正常系・副作用 |
| T-43 | `getUserProfileService` | ユーザー不在 | - | `NotFoundError` | 異常系 |
| T-44 | `getUserProfileService` | 任意 | - | レスポンスに`email`を含まない | 情報漏洩防止 |
| T-45 | `updateUserService` | 他人のプロフィール編集 | - | `ForbiddenError`、repository更新は呼ばれない | 権限・副作用非発生 |
| T-46 | `calcTabiScore`/`tabiRank` | スコア29/30、59/60、99/100 | - | ランクが正しく切り替わる | 境界値（正常/異常ペア） |
| T-47 | `saveUploadedFile`（post経路） | ファイルサイズ 10MB/10MB+1byte | - | 10MBは許可、超過は`ValidationError`（境界値） | 境界値 |
| T-48 | `saveUploadedFile`（avatar経路） | ファイルサイズ 5MB/5MB+1byte、gif指定 | - | 5MBは許可・超過拒否、gifは拒否（post経路とは別上限） | 境界値・同値分割 |
| T-49 | `GET/PUT /api/users/[id]` | 未認証／バリデーションNG／他人／本人 | 各種 | 200/400/401/403 | ブラックボックス（Controller） |
| T-50 | `POST /api/users/[id]/follow` | 未認証／自己フォロー／正常 | - | 401/400/200 | ブラックボックス（Controller） |
| T-51 | `GET /api/users/search` | qパラメータ空／正常 | - | 400/200 | ブラックボックス（Controller） |
| T-52 | `searchUsersByNickname` | 部分一致3件・limit=2 | - | 2件取得・`hasMore: true`（境界値） | 正常系（実DB）・境界値 |
| T-53 | `postSchema`（lat/lng） | -90/90、-180/180（境界）、±0.1超過 | - | 境界値は成功、超過は失敗 | 境界値（正常/異常ペア） |
| T-54 | `LocationPicker` | 地図クリック／逆ジオコーディング成功・失敗／リセット | - | 座標反映・住所表示・フォールバック・リセット | UIコンポーネント（react-leaflet/fetchをモック） |
| T-55 | E2E: フォロー→マイページ→アンフォロー→設定編集 | 2ユーザー | UI操作一式 | フォロー中バッジ・フォロー中投稿タブ表示・トグル解除・プロフィール反映 | E2E・主要フロー（`users.spec.ts`） |
| T-56 | `CommentSection`（回帰テスト） | 初回コメント一覧取得(GET)が投稿(POST)より後に解決 | - | 投稿したコメントが古いGETの結果で上書きされない | 回帰テスト（レースコンディション、10節参照） |

※ Controller層（Vitest）は代表として `posts/route.ts`・Phase 3の`users`系ルート・`upload`系ルートを実装。他の実装済みエンドポイント（comments, like, wishlist, visited, notifications, auth/signup）への同一パターンでのテスト追加は今後の継続タスクとする（8節参照）。E2Eは認証フロー（`auth.spec.ts`）・投稿の主要フロー（`posts.spec.ts`）に加え、Phase 3でフォロー・マイページ・設定編集フロー（`users.spec.ts`）を追加した。コメント・削除・フォローのE2Eは `page.waitForResponse()` で実APIレスポンスを待ってから検証する方式とし、楽観的UIの見た目だけで判定する不安定なアサーションを避けている。

---

## 8. 今後の継続方針

- Phase 4で新規API追加と同時にテストを書く運用とする（実装計画書 Phase 2.5-B 参照）
- 未カバーの既存エンドポイント（comments, like, wishlist, visited, notifications）へのController層テスト追加
- Repository層テストの対象を `findExplorePosts` / `findTopRatedByCategory` 等にも拡充
- `CommentSection.tsx` で見つかったレース条件パターン（10節）が他のクライアントコンポーネント（`NotificationList.tsx`等）にも潜んでいないか、Phase 4着手時に横展開で確認する

---

## 9. カバレッジ目標

- 暫定目標: `src/lib/services` 及び `src/lib/repositories` の分岐カバレッジ **70%以上**
- 現時点では数値目標は暫定値。Phase 3/4でテストが積み上がった段階で再設定する
- 計測コマンド: `pnpm test:coverage`（HTMLレポートは `coverage/index.html`）
- 閾値のCI強制は行わない（Phase 5-BのCI導入時に導入を検討）

---

## 10. E2E安定化の知見（2026-07-06、Phase 3で判明）

Phase 3で `users.spec.ts` を追加した際、既存の `posts.spec.ts` を含む全体実行が断続的に失敗する事象を詳細調査した。原因は当初「タイムアウトが短い」ことを疑ったが、実際には以下の**2つの異なる根本原因**であり、タイムアウト延長では解決しないことが判明した。

### 10.1 モジュールレベル変数のワーカー間非共有

`posts.spec.ts`・`users.spec.ts` は同一 `describe` 内のテスト間で `let postId = ""` のようなモジュールレベル変数を共有し、「同じファイルの全テストは同じワーカープロセスで実行される」という前提に依存していた。

通常はこの前提が成り立つが、**あるテストが失敗すると、Playwrightが後続テストを別のワーカープロセスに再割り当てすることがある**。新しいワーカーはモジュールを最初から読み込むため、共有変数は初期値（空文字）に戻る。この状態で `page.goto('/posts/' + postId)` を呼ぶと `/posts/`（実質 `/posts`）にアクセスし、404が発生する。

`playwright-report/results.json` の各テスト結果に含まれる `workerIndex` を比較することで、削除テストだけ別ワーカーで実行されていたことを直接確認できた（調査時の実例: テスト1〜4が`workerIndex: 1`、テスト5（削除）のみ`workerIndex: 3`）。

**対処**: `test.describe(...)` を `test.describe.serial(...)` に変更。これは「同一ワーカーでの順次実行」と「1つ失敗したら後続はスキップする」ことを明示的に保証するPlaywrightの公式パターンで、ワーカー再割り当てによる状態喪失が構造的に発生しなくなる。

### 10.2 `CommentSection.tsx` のレースコンディション（Phase 1由来の既存バグ）

上記の対処後も「コメント投稿→一覧に反映される」が稀に失敗するケースが残っていた。調査の結果、`CommentSection.tsx` の初回コメント一覧取得（`useEffect` 内の `loadComments()`）が、投稿（POST）の完了より**後に解決すると**、無条件の `setComments(data.comments)` が投稿前の空リストで上書きしてしまうレースコンディションと判明した。人間の操作速度では顕在化しないが、Playwrightの高速な自動操作では初回GETの完了を待たずに投稿が完了することがあり、負荷時にはさらに発生しやすくなる。

**対処**: `opSeqRef`（操作の連番）を導入し、`loadComments`・`handleSubmit`・`handleDelete` の開始時にインクリメント。`loadComments`のfetch完了時に連番が変わっていれば（＝自分より新しい操作が開始していれば）、コメント一覧・カーソル等のデータ反映のみを破棄する（ローディング解除は常に行う）。回帰テストを `CommentSection.test.tsx` に追加した（T-56）。

この過程で、投稿・コメント双方に同じ「削除」ボタンが存在する場合にPlaywrightのロール検索が曖昧になる問題も判明したため、`DeleteButton.tsx` に `data-testid="delete-post-button"` を追加してテストを安定させた。

**教訓**: ①タイムアウトを延ばす対処は、真の原因（状態管理・並行処理のバグ）を覆い隠すことがある。②E2Eで再現する不具合は、実装側の実際のバグを暴くことがあるため、テスト側だけでなく実装側も疑って調査する。

---

## 11. CI実行フロー（将来像）

Phase 5-BでCI（GitHub Actions等）を導入する際、以下をPRトリガーで自動実行する想定:

1. `pnpm install`
2. `pnpm lint`
3. `pnpm typecheck`（`tsc --noEmit`）
4. テスト用MySQLコンテナを起動 → `pnpm prisma:migrate:test` → `pnpm test`
5. （任意）`pnpm test:coverage` の結果をアーティファクトとして保存
6. 既存のPlaywright E2E（`auth.spec.ts`・`posts.spec.ts`・`users.spec.ts`）は別ジョブとして分離実行

現時点（Phase 3）ではCI設定ファイルは作成せず、ローカル実行の確認のみとする。
