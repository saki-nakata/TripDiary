# TripDiary テスト設計書

**バージョン:** 3.1
**作成日:** 2026-07-05
**更新日:** 2026-08-04（GATE-40 PR-7全体の棚卸し4点に対応：①`docs/画面設計書.md`へPR-7d/7e/7fの更新履歴を追記、②`実装計画書/reviews/6-b-fix-dr-02-dark-theme.md`にPR-7e/7fの実施記録を追記、③ダークテーマ対応とは別軸の既存不具合として発見したフォーカスリングの非文字コントラスト不足（WCAG 1.4.11）を是正——`focus:ring-green-500`および半透明の`focus:ring-[#16a34a]/20`・`/30`（計11ファイル）を`focus:ring-green-700`へ統一、④`docs/pr-7d-ci-evidence`ブランチとのコミット重複リスクをメモリへ記録。10.3.9節参照）
**旧更新日:** 2026-08-03（GATE-40 PR-7f: PR-7e後の全src静的監査（10.3.6節）で見つかった残件——通知ページ／`NotificationList`、プロフィールページ本体・`CommentLoadMoreList`・`UserLoadMoreList`・`FollowButton`・共通`BackButton`、投稿詳細ページ本体・`LikeButton`・`WishlistButton`・`VisitedButton`・`CommentSection`、`MapView`/`LocationPicker`の地図拡大ボタン、`PlanMapViewWrapper`/`PlanMapView`、`error.tsx`系6画面、`ReportSummary`の画像上バッジ——のダークテーマ対応をコードレベルで実施。実装中に既存の隠れたコントラスト不足3件（`bg-green-600`・`bg-green-500`・`bg-[#3B88C3]`、いずれもライト/ダーク共通の既存不足）を追加で発見・是正し、Saki さんの指示によるsrc全体の再監査で`FollowFeed.tsx`・投稿詳細のエリア表示テキストも追加是正した。本節時点ではE2E・スクリーンショット証跡・Linux/CI証跡が未取得のため、GATE-40（DR-02）全体は引き続き未完了とする。10.3.7節参照）
**旧々更新日:** 2026-08-03（GATE-40 PR-7e: 探索・検索・タグ〔`SearchClient`〕＋その他〔`YearFilterBar`・`MapView`・`LocationPicker`・`tags/[tag]`〕のダークテーマ対応を検証。新規`e2e/dark-theme-explore-search.spec.ts`〔20ケース、5回以上連続実行で成功を確認〕を追加。実装中に既存の隠れたコントラスト不足2件（検索の並び替え選択状態`bg-[#16a34a]`系、カテゴリチップ選択状態`bg-blue-400`）を発見・是正。PR-7e（DR-02グループF・G）は完了したが、Terraの全src静的監査で通知・プロフィール・投稿詳細等にGATE-40当初の棚卸し漏れが見つかったため、GATE-40（DR-02）全体はLinux/CI証跡取得と残件対応の両方が完了するまで未完了とする。10.3.6節参照）
**旧々々更新日:** 2026-08-01（GATE-40 PR-7d: 旅行プラン〔`SpotPicker`・`ReportSummary`・`PlanForm`・`PlanActions`・`PlanLoadMoreList`・`CompletedPlansAccordion`等〕のダークテーマ対応を検証。新規`e2e/dark-theme-plans.spec.ts`〔15ケース、2回連続実行で成功を確認〕を追加。Terraレビューでの指摘に対応し、モバイル・タブレット幅（320/375/768px）の追加検証、例外レスポンス時のエラートースト表示検証（ダークモードでの可読性含む）を追加。この過程で全アプリ共通の`toast.tsx`がダークモード未対応だった欠陥を発見・是正。10.3.5節参照）
**旧更新日:** 2026-08-01（GATE-40 PR-7c: 投稿フォーム・カード〔`PostForm`・`DateField`・削除確認ダイアログ・フォロー中フィード・マイページ一覧・ImageCarousel・EmptyState・SavedMapSection等〕のダークテーマ対応を検証。新規`e2e/dark-theme-post-forms.spec.ts`〔10ケース、2回連続実行で成功を確認〕を追加。初回レビューで判明したコントラスト不足6件・Sidebarのヒントトースト表示条件の実装バグを是正。10.3.4節参照）
**旧々更新日:** 2026-08-01（無限スクロール対応の例外レスポンスを追加：継続取得・初回取得の非2xx/通信失敗時にエラー表示を行い、既存一覧・入力値を保持することをT-56・T-145・T-150・T-158・T-160・T-161へ反映。12.5節の「もっと見る」操作応答時間テストをsentinelのスクロール表示による計測に更新）
**旧々々更新日:** 2026-07-26（Phase 6 6-A2の`bcryptjs`→`@node-rs/bcrypt`置き換えに伴う既存ハッシュ互換性・72バイト超挙動の回帰テストをT-126として追加。CI実行フローに`build`ジョブを反映）
**旧々々々更新日:** 2026-07-25（Phase 5-E後のログ相関是正として、`handleApiError()`のエラーログにも`withRequestLogging()`由来の`requestId`が付与されることをT-125として追加）
**旧々々々々更新日:** 2026-07-23（Phase 5-B `peakWorkloadVUs()`によるMax VUs自動算出・preflightのプロセス絞り込み、Loadのp99ゲート、ポータル集計SQLの現行実装を反映）
**作成者:** Nakata Saki（Claude Code支援）

---

## 1. テスト方針・目的

Phase 3以降でAPIエンドポイントが急増する前に、単体・統合テストの基盤を整備する（Phase 2.5-B）。
本番影響を避けつつ、権限チェック・バリデーション境界値・DB連携ロジックといった不具合が起きやすい箇所を優先的にカバーする。

同コース内の別課題 TimeLine（Spring Boot）のテスト実装（Controller/Service/Repositoryの3層戦略、`対象_条件_期待結果` の命名規則）を参考にしつつ、TripDiary（Next.js + Prisma + MySQL）向けに翻訳して適用した。

---

## 2. テストレベルと範囲

| レベル | 対象 | 今回の扱い |
|---|---|---|
| 単体テスト | Service層（ホワイトボックス）、Zodバリデーションスキーマ、フロントUIコンポーネント | ✅ 対象 |
| 結合テスト（薄い） | Route Handler（Controller層、依存モック） | ✅ 対象 |
| 結合テスト（実DB） | Repository層 | ✅ 対象 |
| E2Eテスト | ブラウザ経由の画面横断シナリオ | ✅ 対象（認証フローに加え、投稿の主要フローを追加） |
| 性能テスト | 負荷・応答速度 | ❌ 対象外（Phase 5-A） |

---

## 3. 使用ツール・環境

- **テストランナー**: Vitest 4.1.10（`@vitejs/plugin-react` 6.0.3 + `vite` 8.1.4。導入時はvite 8がpostcss未リリースバージョンを要求し一時的に3.2.6/4.7.0へ固定していたが、postcssリリース後に最新版へ更新済み）
- **フロントエンド**: `@testing-library/react` / `@testing-library/jest-dom` / `@testing-library/user-event`、環境は `jsdom`（ファイル先頭に `// @vitest-environment jsdom` を指定）
- **バックエンド**: 環境は `node`（デフォルト）
- **E2E**: `@playwright/test`（`reporter: [html, json, list]` を設定、`playwright-report/` にレポート出力。失敗時は trace・スクリーンショット・動画に加え、`e2e/fixtures.ts` がページHTMLとURLをテスト成果物へ添付する）
- **ORM/DB**: Prisma 6.19 + MySQL 8.0（テスト用コンテナ）
- **カバレッジ**: `@vitest/coverage-v8`
- **環境変数読み込み**: `dotenv`（`vitest.config.ts` 起動時に `.env.test` を読み込み、`dotenv-cli` で Prisma CLI 実行時にも同様に注入）

---

## 4. テストデータ・DB戦略

- `docker-compose.yml` に `mysql-test` サービスを追加（ポート3307、DB名 `tripdiary_test`、`tmpfs` マウントで永続化しない使い捨てDB）
- `.env.test`（gitignore対象）で `DATABASE_URL` をテスト用DBに向ける。開発用DB（`.env` → ポート3306）とは完全に分離
- スキーマ適用: `pnpm prisma:migrate:test`（`dotenv-cli` で `.env.test` を注入して `prisma migrate deploy`）
- Repository層テストは各 `describe` の `beforeEach` でテーブルをクリーンアップ（`SET FOREIGN_KEY_CHECKS=0` で外部キー制約を一時解除し関連テーブルを `deleteMany`）してからデータを投入する方式を採用。Prisma+MySQLではトランザクション分離のみで安全に隔離するのが難しいため、テストごとの明示的クリーンアップとした
- 本番用DBには一切接続しないため、誤って本番データに影響するリスクを構造的に排除
- **⚠️ 2026-07-06 追記**: Repository層テストファイルが複数存在する場合、Vitestの既定（ファイル間並列実行）のままだと各ファイルの `beforeEach` クリーンアップが共有テーブル（`users`等）に対して競合し、他ファイルが投入中のデータを消してしまい外部キー制約違反で失敗することがある（Phase 3-C で `follow.repository.test.ts` を追加した際に発覚）。`vitest.config.ts` に `fileParallelism: false` を設定し、テストファイルを直列実行することで回避した

---

## 5. テスト観点

- **正常系**: 期待するステータスコード・レスポンス形状
- **異常系**: 401（未認証）・403（他人のリソース操作）・404（存在しないID）・400（バリデーション）・409（メール重複、または投稿・プロフィール更新の楽観ロック競合。Phase 6-A追加）
- **境界値分析**: 例）`title` 40文字（正常）/ 41文字（異常）、`rating` 1・5（正常）/ 0・6（異常）、`body` 2000文字（正常）/ 2001文字（異常）、`nickname` 20文字（正常）/ 21文字（異常）、`password` 8文字（正常）/ 7文字（異常）
- **同値分割**: カテゴリの妥当な値／不正な値、confirmPasswordの一致／不一致
- **権限・認可**: 本人/他人の分岐を両方検証し、権限NG時にrepositoryの更新系関数が**呼ばれないこと**まで確認する

---

## 6. 層別戦略と命名規則

| 層 | 手法 | モック対象 |
|---|---|---|
| Controller（Route Handler） | ブラックボックス・薄い結合テスト | `@/lib/auth`（`vi.mock`）、依存するservice/repository |
| Service | ホワイトボックス・純粋単体 | repository（`vi.mock`） |
| Repository | 実DB検証 | モックしない（テスト用MySQLに対して実行） |

**命名規則**: `対象_条件_期待結果`（例: `createPost_タイトル40文字_201`、`updatePost_他人の投稿_403`）。境界値は正常・異常のペアで書く。対象メソッドごとに `// ─── 対象名 ───` のコメント区切りでグルーピングする。

---

## 7. 機能別テストケース表（実装済み分）

| ID | 対象 | 前提 | 入力 | 期待結果 | 観点 |
|---|---|---|---|---|---|
| T-01 | `createPostService` | - | title 40文字 | repositoryが呼ばれる | 境界値（正常） |
| T-02 | `updatePostService` | 投稿が存在しない | 任意 | `NotFoundError` | 異常系 |
| T-03 | `updatePostService` | 他人の投稿 | 任意 | `ForbiddenError`、repository更新は呼ばれない | 権限・副作用非発生 |
| T-04 | `updatePostService` | 本人の投稿 | 有効な入力 | repositoryが正しい引数で呼ばれる | 正常系 |
| T-05 | `deletePostService` | 投稿が存在しない | - | `NotFoundError` | 異常系 |
| T-06 | `deletePostService` | 他人の投稿 | - | `ForbiddenError`、repository削除は呼ばれない | 権限・副作用非発生 |
| T-07 | `deletePostService` | 本人の投稿 | - | repositoryの削除が呼ばれる | 正常系 |
| T-08 | `toggleLikeService` | いいねON | - | 通知が作成される | 副作用 |
| T-09 | `toggleLikeService` | いいねOFF | - | 通知は作成されない | 副作用非発生 |
| T-10 | `toggleLikeService` | 投稿の著者が見つからない | - | 通知は作成されない | 異常系 |
| T-11 | `signupService` | メール重複あり | - | `ConflictError`、ユーザー作成は呼ばれない | 異常系・副作用非発生 |
| T-12 | `signupService` | メール重複なし | 有効な入力 | ハッシュ化されたパスワードでユーザー作成 | 正常系 |
| T-13 | `GET /api/posts` | 未認証 | - | 401 | 異常系 |
| T-14 | `GET /api/posts` | 認証済み | - | 200、投稿一覧 | 正常系 |
| T-15 | `POST /api/posts` | 未認証 | - | 401 | 異常系 |
| T-16 | `POST /api/posts` | 認証済み | 必須項目欠落 | 400 | 異常系 |
| T-17 | `POST /api/posts` | 認証済み | title 41文字 | 400 | 境界値（異常） |
| T-18 | `POST /api/posts` | 認証済み | title 40文字 | 201 | 境界値（正常） |
| T-19 | `findFollowingPosts` | フォロー中/自分/他人の投稿が混在 | - | フォロー中と自分の投稿のみ取得 | 正常系（実DB） |
| T-20 | `findFollowingPosts` | 複数投稿 | - | 新着順で並ぶ | 正常系（実DB） |
| T-21 | `findLocationCounts` | エリア別に複数投稿 | - | エリアごとの件数が正しく集計される | 正常系（実DB） |
| T-22〜T-30 | `postSchema` / `signupSchema` / `signupApiSchema` / `loginSchema` | - | 各境界値・同値分割 | 成功/失敗 | 境界値・同値分割 |
| T-31〜T-33 | `ToastContainer` | トースト0件/1件/複数件 | - | 表示内容が正しい | UIコンポーネント |
| T-34 | `NotificationList.handleRead` | 既読API成功 | - | 既読状態に更新される | 楽観的UI |
| T-35 | `NotificationList.handleRead` | 既読API失敗 | - | 楽観的更新がロールバックされる | 楽観的UIロールバック |
| T-36 | E2E: 投稿作成 | ログイン済み | フォーム入力 | ホーム画面にハイライト表示される | E2E・主要フロー |
| T-37 | E2E: 投稿詳細 | 投稿作成済み | - | タイトル・本文が表示される | E2E・主要フロー |
| T-38 | E2E: いいね | 投稿詳細表示中 | いいねボタン押下 | トグルされ件数が1増える（実POST完了を確認） | E2E・主要フロー |
| T-39 | E2E: コメント投稿 | 投稿詳細表示中 | コメント入力・送信 | 一覧に反映される（実POST 201を確認） | E2E・主要フロー |
| T-40 | E2E: 投稿削除 | 投稿詳細表示中 | 削除確認 | ホーム画面へ遷移する | E2E・主要フロー |
| T-41 | `toggleFollowService` | 自分自身をフォロー | - | `ValidationError`、repository呼ばれない | 異常系・副作用非発生 |
| T-42 | `toggleFollowService` | フォローON/OFF | - | ONの時のみ通知作成 | 正常系・副作用 |
| T-43 | `getUserProfileService` | ユーザー不在 | - | `NotFoundError` | 異常系 |
| T-44 | `getUserProfileService` | 任意 | - | レスポンスに`email`を含まない | 情報漏洩防止 |
| T-45 | `updateUserService` | 他人のプロフィール編集 | - | `ForbiddenError`、repository更新は呼ばれない | 権限・副作用非発生 |
| T-46 | `calcTabiScore`/`tabiRank` | スコア29/30、59/60、99/100 | - | ランクが正しく切り替わる | 境界値（正常/異常ペア） |
| T-47 | `saveUploadedFile`（post経路） | ファイルサイズ 10MB/10MB+1byte | - | 10MBは許可、超過は`ValidationError`（境界値） | 境界値 |
| T-48 | `saveUploadedFile`（avatar経路） | ファイルサイズ 5MB/5MB+1byte、gif指定 | - | 5MBは許可・超過拒否、gifは拒否（post経路とは別上限） | 境界値・同値分割 |
| T-49 | `GET/PUT /api/users/[id]` | 未認証／バリデーションNG／他人／本人 | 各種 | 200/400/401/403 | ブラックボックス（Controller） |
| T-50 | `POST /api/users/[id]/follow` | 未認証／自己フォロー／正常 | - | 401/400/200 | ブラックボックス（Controller） |
| T-51 | `GET /api/users/search` | qパラメータ空／正常 | - | 400/200 | ブラックボックス（Controller） |
| T-52 | `searchUsersByNickname` | 部分一致3件・limit=2 | - | 2件取得・`hasMore: true`（境界値） | 正常系（実DB）・境界値 |
| T-53 | `postSchema`（lat/lng） | -90/90、-180/180（境界）、±0.1超過 | - | 境界値は成功、超過は失敗 | 境界値（正常/異常ペア） |
| T-54 | `LocationPicker` | 地図クリック／逆ジオコーディング成功・失敗／リセット | - | 座標反映・住所表示・フォールバック・リセット | UIコンポーネント（react-leaflet/fetchをモック） |
| T-55 | E2E: フォロー→マイページ→アンフォロー→設定編集 | 2ユーザー | UI操作一式 | フォロー中バッジ・フォロー中投稿タブ表示・トグル解除・プロフィール反映 | E2E・主要フロー（`users.spec.ts`） |
| T-56 | `CommentSection`（回帰テスト） | 初回コメント一覧取得(GET)が投稿(POST)より後に解決／投稿APIが非2xx | - | 投稿したコメントが古いGETの結果で上書きされない。投稿失敗時は楽観的に追加したコメントを取り消し、入力内容を復元する | 回帰テスト（レースコンディション・楽観的UIロールバック、10節参照） |
| T-57 | `plan.repository`（`createPlan`/`updatePlan`） | spots指定（post/free混在）・spots差し替え・spots未指定 | - | `planSpots`がdisplayOrder順で作成／差し替え／既存維持 | 正常系（実DB） |
| T-58 | `plan.repository`（`deletePlan`） | プラン削除・リンク済み投稿の存在 | - | プランのみ削除、リンク済み投稿の`planId`はnullになり投稿自体は残る | 正常系（実DB） |
| T-59 | `plan.service`（`createPlanService`） | budgetBreakdown複数項目／空項目（金額0・ラベル空）／未指定 | - | `budget`自動集計、空項目除外、未指定時はnull | 正常系・境界値（repositoryモック） |
| T-60 | `plan.service`（`createPlanService`/`updatePlanService`） | spotsに存在しない投稿IDを含む | - | `ValidationError`（400）、repository作成/更新は呼ばれない | 異常系（repositoryモック） |
| T-61 | `plan.service`（`findPlanByIdService`/`updatePlanService`/`deletePlanService`/`togglePlanCompletedService`） | 存在しないプランID／他人のプラン／本人のプラン | - | 404／403／正常処理 | 正常系・異常系・権限（repositoryモック） |
| T-62 | `POST /api/plans`・`PUT /api/plans/[id]` | 未認証／タイトル未指定・61文字／正常 | - | 401／400／201・200 | ブラックボックス（Controller） |
| T-63 | `GET/PUT/DELETE /api/plans/[id]`・`PATCH /api/plans/[id]/complete` | 未認証／存在しないプラン／他人のプラン／本人のプラン | - | 401／404／403／200 | ブラックボックス（Controller） |
| T-64 | `planSchema` | title境界値（60/61文字）／endDate<startDate／budgetBreakdown境界値（amount0/負数/label51文字） | - | 正常系成功・異常系失敗 | 境界値（正常/異常ペア） |
| T-65 | `stats.repository`（`findYearlyPosts`/`findYearlyCompletedPlans`） | 年境界（12/31と1/1）／startDate未設定のプラン | - | 年ごとに正しく分離される、startDate未設定は集計から除外 | 正常系・境界値（実DB） |
| T-66 | `stats.service`（`getYearlyStatsService`） | cost未設定／location同数タイ／投稿0件／category未設定 | - | costは0扱い、topLocationは最初に出現したもの、0件時はnull/空配列、category未設定は除外 | 正常系・境界値（repositoryモック） |
| T-67 | `GET /api/stats`・`GET /api/stats/years` | 未認証／yearパラメータ欠落・不正値／正常 | - | 401／400／200 | ブラックボックス（Controller） |
| T-68 | E2E: プラン作成→詳細表示→編集→完了切り替え→削除 | 1ユーザー | UI操作一式 | 詳細ページ遷移・タイトル更新反映・完了バッジ表示・マイページへのリダイレクト | E2E・主要フロー（`plans.spec.ts`） |
| T-69 | `changePasswordService` | 他人のID／存在しないユーザー／現在のパスワード誤り／確認用アカウント（`isProtected: true`）／正常 | - | 403／404／`ValidationError`（400）／403「確認用アカウントのため変更できません」（メッセージ完全一致まで検証、`compare`・`updateUserPassword`は呼ばれない）／ハッシュ化して更新 | 正常系・異常系・権限（repositoryモック、@node-rs/bcryptモック） |
| T-70 | `changeEmailService` | 他人のID／存在しないユーザー／現在のパスワード誤り／メール重複／現在と同じメール／確認用アカウント（`isProtected: true`）／正常 | - | 403／404／400／`ConflictError`（409）／更新スキップ／403「確認用アカウントのため変更できません」（メッセージ完全一致まで検証、`compare`・`findUserByEmail`・`updateUserEmail`は呼ばれない）／更新 | 正常系・異常系・権限・境界値（repositoryモック、@node-rs/bcryptモック） |
| T-71 | `PATCH /api/users/[id]/password`・`PATCH /api/users/[id]/email` | 未認証／バリデーションエラー／他人のID／メール重複／正常 | - | 401／400／403／409／200 | ブラックボックス（Controller） |
| T-72 | `passwordChangeSchema`・`emailChangeSchema` | newPassword境界値（7/8文字）／confirmNewPassword不一致／email不正形式／currentPassword空文字 | - | 境界値は成功、異常系は失敗 | 境界値（正常/異常ペア） |
| T-73 | E2E: パスワード変更→新パスワードで再ログイン→メールアドレス変更→ログアウト→新メールアドレスで再ログイン | 1ユーザー | UI操作一式 | 旧パスワードでのログイン失敗・新パスワードでのログイン成功、メール変更後の自動サインアウトと新メールアドレスでの再ログイン成功 | E2E・主要フロー（`account-settings.spec.ts`） |
| T-74 | `stats.repository`/`stats.service`/`GET /api/stats`/`statsYearQuerySchema` | `year=all`（全期間） | - | `findYearlyPosts`/`findYearlyCompletedPlans`に`null`が渡される、`year`は`"all"`になり`yearlyPostCount`が年ごとに集計され`monthlyPostCount`は空、`year=all`クエリで200・サービスに`null`が渡される、バリデーション成功 | 正常系（repositoryモック・実DB・Controller・境界値） |
| T-75 | `generateOpenApiDocument`（`src/lib/openapi/registry.ts`） | 登録済み全スキーマ・パスのドキュメント生成 | - | 実行時エラーなく生成完了、`StatsResponse`に`year`/`monthlyPostCount`/`yearlyPostCount`が含まれ`monthlyCost`は含まれない、`/api/plans`・`/api/stats`・`/api/stats/years`が`doc.paths`に含まれる | 正常系（回帰テスト。Zodスキーマの型不整合でSwagger生成が実行時エラーになる不具合が実際に発生したため追加） |
| T-76 | E2E: 旅行レポートで年を選択→別タブへ遷移→ブラウザバック | 1ユーザー | UI操作一式 | URLに`year`クエリが反映され、ブラウザバック後も選択した年が復元される | E2E・回帰（`report.spec.ts`。`router.replace()`によるURL同期の検証） |
| T-77 | E2E: 他人のプロフィール／自分のプロフィールでの戻るボタン表示切替 | 2ユーザー | UI操作一式 | 他人のプロフィールでは戻るボタンが表示され、自分のプロフィールでは表示されない | E2E・回帰（`users.spec.ts`） |
| T-78 | E2E: 投稿作成画面で複数枚アップロード→ドラッグ&ドロップで並び替え→保存→詳細画面で確認 | 1ユーザー・3枚のテスト画像（`e2e/utils/testImage.ts`で生成） | UI操作一式（マウスドラッグの座標移動をシミュレート） | 並び替え後の順序が投稿詳細ページのサムネイル表示に永続化されている | E2E・主要フロー（`posts.spec.ts`。`dnd-kit`によるD&D並び替え機能）。**既知の制限（2026-07-29）**：Phase 6-Aで画像アップロードがS3必須になったが、CI（`.github/workflows/ci.yml`のe2eジョブ）にAWS認証情報が無いため、CI上では`test.skip`で一時的にスキップ中。実バケット・IAMロールを構築するPhase 6-B着手時にskipを解除する。ローカルで`.env.local`にAWS認証情報を設定していれば手動実行では引き続き検証可能 |
| T-79 | `like.service`（`toggleLikeService`） | 自分の投稿へのいいね | - | `ForbiddenError`を投げ、`toggleLike`（repository）は呼ばれない | 異常系（repositoryモック） |
| T-80 | `notification.service`（`getUserNotifications`/`getUnreadCountService`） | 1年保持ポリシー | - | 一覧・件数取得の**前**に`markStaleNotificationsAsRead`が呼ばれる（呼び出し順を`invocationCallOrder`で検証）、カットオフ日時が現在時刻の1年前と一致（誤差1分許容） | 正常系（repositoryモック。削除ではなく既読化する方式の回帰防止） |
| T-81 | `post.repository`（`createPost`） | 投稿作成時 | - | 投稿者自身の`Visited`行が自動作成される（実DBで`visited.findUnique`により検証） | 正常系（実DB。自分の投稿は自動で訪問済み扱いにする仕様のレビュー指摘を受けて追加） |
| T-82 | `stats.service`（`getYearlyStatsService`） | 訪問日が新しい順のエリアを含む複数投稿 | - | `visitedLocations`が訪問日順ではなく`LOCATIONS`配列の並び順（北海道→沖縄県の地理順）で返る | 正常系（repositoryモック。地図（都道府県塗り分け）とバッジ一覧の並び順を揃えるための仕様変更の回帰防止） |
| T-83 | `follow.repository`（`toggleFollow`） | フォロー→アンフォロー | - | フォロー時は双方の`User.followerCount`/`followingCount`が1増加、アンフォロー時は元に戻る（実DBで`user.findUniqueOrThrow`により検証） | 正常系（実DB。非正規化カウンタ導入時にレビューで発見した実DB検証の抜けを埋めるため追加。あわせて未使用になっていた`countFollowers`/`countFollowing`関数と同テストを削除） |
| T-84 | `notification.service`（`getUnreadCountService`） | 同一ユーザーからの短時間の連続呼び出し／異なるユーザーからの呼び出し | - | 同一ユーザーが12時間以内に連続呼び出しした場合は`markStaleNotificationsAsRead`が初回の1回のみ実行される（件数取得自体は毎回実行）、ユーザーが異なれば間引かれずそれぞれ実行される | 正常系（repositoryモック。通知バッジのポーリング（60〜180秒間隔）のたびに対象0件でもUPDATE文が発行される非効率をレビューで指摘され、ユーザー単位で12時間に1回まで間引くよう対応） |
| T-85 | `stats.service`（`getYearlyStatsService`） | 件数が同数のカテゴリを複数含む投稿（出現順とCATEGORIES定義順が逆） | - | `categoryBreakdown`が件数降順、同数の場合は投稿の出現順ではなく`CATEGORIES`（`src/lib/constants.ts`）の定義順で並ぶ | 正常系（repositoryモック。旅行レポートのカテゴリ別投稿数にCATEGORIES定義順のタイブレークを追加した際、テストが漏れていたため追加） |
| T-86 | E2E: プラン詳細で緯度経度ありのスポットを含むプランを表示 | 1ユーザー・緯度経度を指定して作成した投稿1件 | UI操作一式（座標設定自体はAPI直接呼び出し、地図UIのクリック操作はタイル読み込み依存で不安定なため対象外） | プラン詳細ページにスポット地図（Leafletコンテナ）が表示される | E2E・回帰（`plans.spec.ts`。`PlanMapView`は`旅行レポートの都道府県塗り分け地図`と同様クライアント側の可視化のみで新規APIを伴わないため単体テスト対象外だが、レビューで表示確認の抜けを指摘され追加） |
| T-87 | `stats.service`（`getYearlyStatsService`） | 評価済み投稿（★5・★3）＋評価なし投稿1件 | - | `averageRating`が評価済み投稿のみの加重平均（4）になる。評価済み投稿が0件の場合は`null` | 正常系・境界値（repositoryモック。旅行レポートの平均評価カード・高評価スポットTOP3の並び替え基準となる算出ロジックに対するテストが漏れていたため追加） |
| T-88 | `stats.service`（`getYearlyStatsService`） | ★3・★5・評価なし・★3（訪問日が新しい）・★4の5投稿 | - | `topRatedPosts`が評価降順で上位3件のみ返り、評価なしの投稿は除外される。同点（★3が2件）は訪問日が新しい方が上位に来る | 正常系（repositoryモック。高評価スポットTOP3のタイブレーク仕様＝「評価の分布」バーグラフの置き換え機能に対するテストが漏れていたため追加） |
| T-89 | `stats.service`（`getYearlyStatsService`） | `images`が2件（`[{url:"a.jpg"},{url:"b.jpg"}]`）の投稿 | - | `topRatedPosts[].thumbnail`が`images`配列の先頭要素のURLになる | 正常系（repositoryモック） |
| T-90 | `post.repository`（`findPostsByAuthorId`） | 訪問日が異なる年の投稿2件 | - | `year`指定時はその年の投稿のみ、未指定時は全年の投稿が返る | 正常系・境界値（実DB。「自分の投稿」タブの年度絞り込みロジックに対するテストが漏れていたため追加） |
| T-91 | `user.repository`（`countUserPosts`） | 訪問日が異なる年の投稿3件 | - | `year`指定時はその年の件数のみ、未指定時は全年の件数が返る | 正常系（実DB。「自分の投稿」タブのカウントバッジの年度絞り込みロジックに対するテストが漏れていたため追加） |
| T-92 | `lib/date`（`formatRelativeTime`） | 経過時間0秒〜7日超の境界値 | - | 1分未満「たった今」、60分未満「N分前」、24時間未満「N時間前」、7日未満「N日前」、7日以上は`formatDateSlash`と同じ日付表記に切り替わる | 正常系・境界値（`date.ts`に元々テストが1件も無かったため新規追加。フォロー中の投稿フィードの相対時刻表示に使用） |
| T-93 | `lib/date`（`dateGroupLabel`） | 暦日差0〜1・2〜6・7以上（同月/月またぎ）の境界値 | - | 暦日ベースで「今日」「昨日」「今週」「今月」「それ以前」に正しく振り分けられる（年またぎも「それ以前」） | 正常系・境界値（同上。フォロー中の投稿フィードの日付グループ見出しに使用） |
| T-94 | `SavedMapSection`（コンポーネント） | 同一県に投稿2件、「外す」ボタンで1件を解除 | - | 解除API成功時：カードの消失と同時に県グループの件数見出しも即座に1減る。失敗時：カード・件数見出しの両方が元に戻る。グループ最後の1件を解除すると県見出し自体が消える | 正常系・回帰（jsdom、`@testing-library/react`。`RemovableSavedCard`の楽観的除去が親コンポーネントの集計値（件数見出し・地図濃淡の元データ）には反映されていなかったレビュー指摘を受けて修正し、再発防止のテストを追加） |
| T-95 | E2E: 行きたい登録→地図＋県別グループ一覧に表示、「外す」ボタンで解除 | 1ユーザー・投稿1件（行きたい登録済み） | UI操作一式（`/api/posts`・`/api/posts/[id]/wishlist`はAPI直接呼び出し） | 一覧表示：投稿カードと県グループの件数見出し（「東京都（1件）」）が表示される。解除：確認ダイアログ→「外す」クリックでカード・件数見出しが消えトーストが表示され、リロード後も空リストのEmptyStateになる（サーバー側の解除も確認） | E2E・回帰（`saved-map.spec.ts`新設。4-G「行きたい/訪問済みの管理機能強化」に専用のE2Eが無かったため追加） |
| T-96 | E2E: フォロー中の投稿タブが日付グループ分けのフィード形式で表示される | 2ユーザー（A・B）・Bの投稿1件・AがBをフォロー | `users.spec.ts`の既存テストを拡張 | 「今日」の日付グループ見出し、投稿者（Bのニックネーム）、投稿タイトルが表示される | E2E・回帰（既存テストはテキスト表示のみの確認でレイアウト非依存だったが、`FollowFeed.tsx`への刷新（カードグリッド→日付グループ分けフィード）を実際に検証していなかったため拡張） |
| T-97 | E2E: 自分の投稿タブの年度セレクトでカード・カウントバッジが連動する | 1ユーザー・投稿2件（2025年・2026年） | UI操作一式（投稿作成はAPI直接呼び出し） | 年度セレクトで2025年を選ぶと、2025年の投稿のみ表示されカウントバッジも2→1に変わる（URLにも`year=2025`が反映される） | E2E・回帰（`mypage-year-filter.spec.ts`新設。年度フィルター機能にE2Eカバレッジが無かったため追加） |
| T-98 | E2E: 旅行プランタブの完了済みプラン年度セレクトで絞り込める | 1ユーザー・完了済みプラン2件（2025年・2026年） | UI操作一式（プラン作成・完了トグルはAPI直接呼び出し） | 完了済みプランアコーディオンを開き年度セレクトで2025年を選ぶと、2025年開始のプランのみ表示される | E2E・回帰（同上ファイル。完了済みプランの年度フィルターにもE2Eカバレッジが無かったため追加） |
| T-99 | `createPostService`/`updatePostService`（`planId`所有権チェック） | 他人の`planId`／存在しない`planId`／本人の`planId`／`planId`未指定 | - | 他人のplanId指定は`ForbiddenError`、存在しないplanIdは`NotFoundError`、本人のplanIdは正常に作成・更新、未指定時は所有権チェック自体をスキップ | 正常系・異常系・権限（repositoryモック。4-Bで明記済みだった「他人のplanIdを投稿に指定できる認可の穴」をPhase 5-Dで解消） |
| T-100 | `lib/rate-limit`（`checkRateLimit`） | 上限回数以内／超過／異なるkey／ウィンドウ経過後 | - | 上限内は例外を投げない、超過時は`RateLimitError`、keyが異なれば独立してカウント、ウィンドウ経過後はカウントがリセットされる | 正常系・境界値（固定ウィンドウ方式のインメモリカウンタ本体のロジック検証） |
| T-101 | `lib/rate-limit`（`getClientIp`） | `x-forwarded-for`ヘッダあり／なし | - | ヘッダありは最左のIPを返す、なしは`"unknown"`を返す | 正常系・境界値 |
| T-102 | `POST /api/auth/signup`（レート制限） | 正常リクエスト201・メール重複409・バリデーション400・同一IPから10回超の送信 | Controllerテスト（サービス層モック） | 正常系は201でサービスが呼ばれる、重複は409、バリデーションエラーは400でサービス未呼び出し、10回超で429 | 正常系・異常系・境界値（元々Controllerテストが存在しなかったため、429ケースと合わせて基本ケースも新規追加） |
| T-103 | `POST /api/upload/post`・`POST /api/upload/avatar`（レート制限） | 同一ユーザーIDから上限（`post`30回・`avatar`10回）超の送信 | Controllerテスト（サービス層モック） | 上限超過時に429を返す | 境界値（既存Controllerテストに429ケースを追加） |
| T-104 | ログイン（`authorize()`、レート制限） | 同一メールアドレス（大文字小文字違いを含む）への5回超のログイン試行 | - | 5回まではDB照合まで進む、6回目以降は`RateLimitSignInError`（`code=rate_limit`）。メールアドレスは正規化（小文字化）してカウントするため大文字小文字を変えても制限を回避できない | 異常系・境界値・セキュリティ（`/security-review`で「メールの大文字小文字を変えると制限が回避できる」不具合を発見、`email.toLowerCase()`で正規化して修正） |
| T-105 | `GET /api/health` | DB疎通成功／失敗 | 3層構成でテスト：Controller（`checkHealthService`モック、成功時200/失敗時503）・Service（`pingDatabase`モック、例外がそのまま伝播することを確認）・Repository（実DB、`pingDatabase`が例外を投げずに解決することを確認） | 成功時200 `{status:"ok"}`、失敗時503 `{status:"error"}` | 正常系・異常系（初回実装時はRoute Handlerが`prisma`を直接呼び他のroute.tsと異なりService/Repositoryを経由していなかったため、`health.repository.ts`/`health.service.ts`に分離して3層構成に修正） |
| T-106 | `lib/request-logging`（`withRequestLogging`） | 正常レスポンス／`auth()`が例外を投げる場合／動的ルートの`ctx`パススルー／静的ルート（引数無しハンドラ） | - | 元のレスポンスをそのまま返しつつ`method`/`path`/`status`/`duration_ms`/`userId`/`requestId`を`info`ログ出力する。`auth()`失敗時は`userId`なしでログ出力を継続。`ctx`は元のハンドラにそのまま渡る。引数無しハンドラも`req`未定義でクラッシュしない | 正常系・異常系・境界値（全route.tsに適用する共通ラッパー本体の検証） |
| T-107 | `like.repository`（`toggleLike`） | 未いいね状態／いいね済み状態／2回連続トグル／2ユーザーが同一投稿にいいね | - | トグルで`Post.likeCount`が連動して増減する。2回連続で0に戻る（境界値）。2ユーザーで2になる | 正常系・境界値（実DB。Phase 5-Bのパフォーマンステスト用シードデータ生成が同関数を使うため前倒し追加。P2002競合分岐は実DB単体テストでは検証困難なため対象外とし、Stress/Spikeシナリオでの負荷が事実上のカバーになる） |
| T-108 | `comment.repository`（`createComment`/`deleteComment`/`findCommentsByPostId`/`findCommentById`） | コメント作成・削除・一覧取得・単体取得の正常系／存在しないID | - | `createComment`/`deleteComment`で`Post.commentCount`が連動。一覧は作成日時昇順。存在しないIDは`null`/空配列 | 正常系・境界値（実DB。認可はrepository層に無いため権限系ケースは対象外、Service層の責務） |
| T-109 | `visited.repository`（`toggleVisited`） | 未登録状態／登録済み状態／2回連続トグル | - | トグルで登録・解除される。2回連続で0に戻る（境界値） | 正常系・境界値（実DB。`createPost()`が投稿者自身を自動的に訪問済みにする仕様があるため、テストでは投稿者とトグル対象ユーザーを分離） |
| T-110 | `notification.repository`（`createNotification`/`findNotificationByLike`/`findUserNotifications`/`getUnreadCount`/`markAsRead`/`markStaleNotificationsAsRead`） | 各関数の正常系／該当なし・通知なしの境界値／他人の通知IDでの`markAsRead` | - | 作成・取得・未読数集計・既読化・1年保持ポリシーの自動既読化がそれぞれ正しく動作する。他人の通知IDでは更新されない | 正常系・境界値・権限（実DB） |
| T-111 | `lib/prisma-retry`（`withDeadlockRetry`） | 初回成功／P2034が数回発生後に成功／リトライ上限超過／P2034以外のエラー | - | 成功時は1回のみ実行、P2034は上限内でリトライして成功、上限超過時は最後のエラーを送出、P2034以外は即座に送出（リトライしない） | 正常系・異常系（モック。Phase 5-Bの負荷試験で`toggleLike`/`createComment`がP2034を未捕捉で500になる実バグを検出し、対策として追加） |
| T-112 | `post.repository`（`findTopRatedByCategory`） | カテゴリごとの評価降順（複数カテゴリ・複数投稿） | - | カテゴリごとに最高評価の投稿が1件ずつ返り、カテゴリ代表同士も評価・いいね数・IDの降順/昇順規則で並ぶ（評価なしの投稿は対象外） | 正常系（実DB。Phase 5-BのStress実測で全表スキャンによるp95悪化を検出。現行は`ROW_NUMBER()`でカテゴリ代表IDを一括選出し、必要な投稿だけを取得する） |
| T-113 | `post.repository`（`findTopRatedByCategory`） | `excludeIds`指定 | - | 指定した投稿IDを除外した上でカテゴリごとの最高評価が返る | 正常系・境界値（実DB） |
| T-114 | `visited.service`（`toggleVisitedService`） | 正常系 | - | `repository`の戻り値をそのまま返す | 正常系（repositoryモック。薄い委譲のみのためテストも最小限。Phase 5-C-2で判明済みのテスト不足を解消） |
| T-115 | `wishlist.service`（`toggleWishlistService`/`countWishlistByUserService`） | 正常系 | - | `repository`の戻り値をそのまま返す | 正常系（repositoryモック。同上） |
| T-116 | `GET`/`PUT`/`DELETE /api/posts/[id]` | 未認証／存在しない投稿／他人の投稿／本人の投稿 | Controllerテスト（サービス層モック） | GET: 存在しない投稿404、未認証でも公開閲覧200、認証済みは`findPostByIdService`に`userId`が渡り200。PUT/DELETE: 未認証401、存在しない投稿404、他人の投稿403、本人の投稿200 | 正常系・異常系・権限（Phase 5-C-2で判明済みのテスト不足を解消。GETはCodexレビュー指摘を受けて追加） |
| T-117 | `GET`/`POST /api/posts/[id]/comments` | 一覧取得／未認証／本文未指定／正常投稿 | Controllerテスト（サービス層モック） | GET200、POST未認証401、本文未指定400、正常系201 | 正常系・異常系（同上） |
| T-118 | `POST /api/posts/[id]/like`・`visited`・`wishlist` | 未認証／正常系 | Controllerテスト（サービス層モック） | 未認証401、正常系200でサービスに`userId`/`postId`が渡る | 正常系・異常系（同上。3エンドポイントとも同一パターンのためテストケースも共通、3エンドポイント分を個別ファイルで追加） |
| T-119 | `DELETE /api/comments/[id]` | 未認証／存在しないコメント／権限なし／本人または投稿オーナー | Controllerテスト（サービス層モック） | 未認証401、存在しないコメント404、権限なし403、削除可能なユーザーは200 | 正常系・異常系・権限（同上） |
| T-120 | `GET /api/notifications`・`GET .../unread-count`・`PATCH .../[id]/read` | 未認証／認証済み | Controllerテスト（サービス層モック） | 未認証時は`notifications`系401、`unread-count`のみ未認証でも200・`{count:0}`（既存仕様）。認証済みはそれぞれ200でサービスに`userId`が渡る | 正常系・異常系（同上。3エンドポイントとも実装済みのため個別ファイルで追加） |
| T-121 | `GET /api/posts/portal` | 正常系 | Controllerテスト（サービス層モック） | 200でポータルデータを返す | 正常系（同上） |
| T-122 | `DELETE /api/test/cleanup` | `ENABLE_TEST_ENDPOINTS`未設定／`TEST_CLEANUP_SECRET`未設定／シークレット不一致／email未指定／正常系／DB失敗 | Controllerテスト（サービス層モック） | フラグ未設定403、シークレット未設定403、シークレット不一致403、email未指定400、正常系200、DB失敗500 | 正常系・異常系（Codexレビュー指摘を受け、Route直下のPrisma呼び出しを`deleteTestUserByEmailService`/`deleteUserByEmailForTest`へ3層化した上で追加。2026-07-29 GATE-03対応で`TEST_CLEANUP_SECRET`必須化に伴うケースを追加） |
| T-123 | `global-error.tsx` | 正常系 | コンポーネントテスト（jsdom） | マウント時に`console.error(error)`が呼ばれる、再試行ボタンのクリックで`reset`が呼ばれる | 正常系（Codexレビュー指摘を受けて追加。他の`error.tsx`群への同種テストの量産は対象外） |
| T-124 | `openapi/registry`（`generateOpenApiDocument`） | 意図的除外3件（`api/auth/[...nextauth]`・`api/openapi.json`・`api/test/cleanup`）を除く全`route.ts`×HTTPメソッド | - | 各メソッドが`doc.paths`に登録済みであること | 正常系・回帰（Phase 5-Eで追加。`src/app/api`配下の`route.ts`をテキスト解析して`export const GET/POST/...`を抽出し`doc.paths`と突き合わせる。手書き`docs/API仕様書.md`をSwagger自動生成に一本化して削除する根拠として導入） |
| T-125 | `request-logging` + `handleApiError` | Route Handler内で`handleApiError()`が401を返す | `withRequestLogging()`でラップしたハンドラを実行 | アクセスログとエラーログに同じ`requestId`が出力され、エラーログにも`method`/`path`/`errorType`が含まれる | 正常系・回帰（ログ運用設計書の「同一リクエストのアクセスログとエラーログを`requestId`で突き合わせる」仕様を担保） |
| T-126 | `password-compat.test.ts`（`@node-rs/bcrypt`、非モック） | 現行`bcryptjs`で実際に生成した`$2b$`ハッシュ／同一ソルト・ダイジェストのままバージョンタグのみ`$2a$`に差し替えた補助fixture／72バイトを超える長いASCII・日本語パスワード | 正しいパスワードでの`compare()`／誤ったパスワードでの`compare()`／`hash()`→`compare()`往復 | `$2b$`ハッシュの検証成功（既存データ互換性の本命）、誤りは失敗、補助的な`$2a$`ハッシュも検証成功、72バイト超のパスワードでも例外を投げず`bcryptjs`と同じ「サイレント切り詰め」挙動で一致 | 正常系・異常系・境界値・回帰（6-A2の`bcryptjs`→`@node-rs/bcrypt`置き換えに伴う互換性確認。既存3ファイルは`bcryptjs`を`vi.mock`しているため実物のハッシュ比較を検証する非モックファイルとして新設） |
| T-127 | `s3-url.ts`（`getBucketHostname`） | `AWS_REGION`/`AWS_S3_BUCKET_NAME`両方設定／片方のみ設定／両方未設定 | - | 両方設定時のみバーチャルホスト形式のホスト名を返す。片方でも欠けると`undefined` | 正常系・異常系（依存ゼロモジュールのため`@aws-sdk/client-s3`・`logger`のモック不要。`next.config.ts`と`s3.ts`の両方から共有される） |
| T-128 | `s3.ts`（`extractKeyFromUrl`/`isOwnedS3Url`/`uploadObject`/`deleteOwnedObjectsByUrl`、`@aws-sdk/client-s3`モック） | 正常なバケットURL／ホスト名不一致／`uploads/`以外のパス／解析不能な文字列／不正なpercent-encoding／自分のprefix・他人のprefix／`AWS_REGION`等未設定 | 各関数呼び出し | キー抽出・所有者判定が正しく行われ異常系は例外を投げず`null`/`false`を返す。`uploadObject`は`ContentType`付きで`PutObjectCommand`を呼び正しいURLを返し、設定未了時は明確なメッセージの`Error`を投げる。`S3Client`はモジュールimport時点では生成されず初回呼び出し時に遅延生成される | 正常系・異常系・境界値（6-A新規。他人のオブジェクトを削除できてしまう権限昇格を防ぐ`isOwnedS3Url`の検証が中心） |
| T-129 | `s3.ts`（`deleteOwnedObjectsByUrl`のパーシャル失敗・内部完結） | 所有者prefix外URLを含む配列／全件所有者外／`S3Client.send`がreject／`DeleteObjectsCommand`レスポンスに`Errors`が含まれる | `deleteOwnedObjectsByUrl`呼び出し | 所有者外URLは削除対象から除外されログに残る。全件除外時は`send`を呼ばない。`send`自体がrejectしても関数はresolveし`logger.error`を出力（呼び出し側に例外を伝播させない設計）。パーシャル失敗も`Errors`各要素につき`logger.error`を出力 | 異常系・回帰（6-A新規。ベストエフォート削除が「DB操作成功後の未処理rejectionで500」に化けることを構造的に防ぐ設計の検証） |
| T-130 | `upload.service.ts`（`saveUploadedFile`、`@/lib/s3`モック） | `ownerUserId`付き呼び出し／各種MIME・サイズ境界値／細工されたファイル名 | `saveUploadedFile(file, ownerUserId, opts?)` | `uploads/{ownerUserId}/{uuid}.{拡張子}`形式のキーで`uploadObject`が呼ばれ、フルURLが返る。拡張子は`file.name`ではなく検証済み`file.type`の固定マップから決定される（ファイル名に`/`等を含めてもキーの形が歪まない） | 正常系・境界値・回帰（6-Aで`fs.writeFile`からS3化。所有者スコープ付きキーとMIMEベース拡張子決定の検証） |
| T-131 | `post.service.ts`（`updatePostService`、楽観ロック） | `updatePost`がPrisma `P2025`をreject／`data.version`と実際のDB値が一致 | `updatePostService`呼び出し | `P2025`は`ConflictError`（409相当）に変換される。競合時はS3差分削除が実行されない（DB操作優先の順序） | 異常系・回帰（6-A新規、2026-07-30 GATE-04対応で`updatedAt`から`version`列ベースへ変更。複数タブでの同時編集がS3差分削除で他リクエストの更新後の画像を誤って消す競合を、`version`条件付き`where`とP2025→ConflictError変換で防止する設計の検証。実DBでの`Promise.allSettled`による並行更新テストも追加） |
| T-132 | `post.repository.ts`（`updatePost`、実DB） | 画像付き投稿に対し実際とは異なる`version`を指定 | `updatePost(id, data, staleVersion)` | 呼び出しが失敗し、かつ`postImage.deleteMany`で削除されたはずの`PostImage`行がロールバックされそのまま残る | 異常系・回帰（実DB。`prisma.$transaction`のインタラクティブトランザクションが例外時に全体をロールバックする保証を裏付ける。2026-07-30 `version`列ベースへ変更） |
| T-133 | `post.service.ts`/`user.service.ts`（書き込み時URL所有権検証、`isOwnedS3Url`モック） | own-prefix外かつ既存参照にもないURL／own-prefixのURL／更新前から存在するURL（投稿）／`before.image`と一致（アバター） | `createPostService`/`updatePostService`/`updateUserService`呼び出し | 所有者prefix外かつ既存参照でもないURLは`ValidationError`。own-prefixまたは既存参照に一致するURLは許可される | 異常系・正常系（6-A新規。`imageUrls`/`image`が任意文字列を許容する既存バリデーションの穴を塞ぐ。生の`<img>`タグを使う`ImageLightbox.tsx`がCSP制限を回避する実害を踏まえた対応） |
| T-134 | `post.service.ts`/`user.service.ts`（`imageUrls`/`image`が`undefined`の場合のスキップ） | `data.imageUrls`/`data.image`が`undefined`／`[]`または`null`（明示的な全削除） | 各サービス呼び出し | `undefined`の場合は書き込み検証・S3削除処理のいずれも実行されない。`[]`/`null`の場合は通常の差分計算に乗り既存画像・アバターが削除対象になる | 正常系・回帰（6-A新規。`undefined`と「値はあるが空」の取り違えは初版設計で防ごうとした全画像削除バグの再発条件になるため、明確に区別して検証） |
| T-135 | `postSchema`/`postUpdateSchema`（zod v4） | `imageUrls`20件/21件・500文字/501文字境界／`version`が0以上の整数・負の整数・未指定 | `safeParse` | 20件・500文字までは成功、超過は失敗。`postUpdateSchema`は`version`必須で負の整数は失敗、`postSchema`（作成用）には`version`フィールド自体が存在しない | 境界値・異常系（6-A新規、2026-07-30 GATE-04対応で`updatedAt`から`version`へ変更。作成用と更新用でスキーマを分離し、作成APIのOpenAPIドキュメントに無視されるフィールドが載ることや悪意ある`version`混入を防ぐ設計の検証） |
| T-136 | `post.repository.ts`（`findStillReferencedUrls`、実DB） | 対象URLが他の投稿の`PostImage`に一致／`User.image`に一致／どちらにも一致しない／空配列 | `findStillReferencedUrls(urls)` | `PostImage`・`User.image`いずれかに一致するURLだけを含む集合を返す。空配列はDBに問い合わせず空集合を返す | 正常系・境界値（実DB。IN句での一括照会でN+1にしない） |
| T-137 | `post.service.ts`（共有URLの削除保護） | 削除候補URLが他の投稿からまだ参照されている（`findStillReferencedUrls`が非空集合を返す） | `updatePostService`（差分削除）／`deletePostService` | 参照が残っているURLは`deleteOwnedObjectsByUrl`の呼び出し対象から除外され、S3から削除されない | 異常系・回帰（実装後レビュー（Terra・Opus）で指摘。旅行プランから投稿作成への画像引き継ぎ`presetImageUrl`等で同一S3オブジェクトが複数の投稿から参照される状態が通常UIで作られ得るため、DB確定後に他参照の有無を再確認してから削除する） |
| T-138 | `user.service.ts`（共有URLの削除保護） | 削除候補の旧アバターURLが投稿の`PostImage`または他ユーザーの`User.image`からまだ参照されている | `updateUserService` | 参照が残っている場合`deleteOwnedObjectsByUrl`は呼ばれない | 異常系・回帰（同上。投稿とアバターに同じ自分のURLをAPI経由で設定するケースも同種の破損経路になるため） |
| T-139 | `user.service.ts`（アバター更新の楽観ロック） | `updateUser`がPrisma `P2025`をreject／`version`不一致 | `updateUserService` | `P2025`は`ConflictError`（409相当）に変換される。競合時はS3削除が実行されない（DB操作優先の順序） | 異常系・回帰（実装後レビュー（Terra）で指摘。投稿と同じく2タブでのアバター同時更新がS3削除を伴う画像破損を引き起こし得るため、`User.updatedAt`条件付き`where`とP2025→ConflictError変換を追加。2026-07-30 GATE-04対応で`User.version`列ベースへ変更、パスワード・メール変更では進まないため誤検知409が解消） |
| T-140 | `post.service.ts` 統合テスト（実DB + 実service/repository層、`@aws-sdk/client-s3`のみモック） | 投稿Aの画像URLを`presetImageUrl`として投稿Bが引き継ぎ同一URLを共有する／共有されていない新規アップロード画像 | `createPostService`→`deletePostService` | 投稿Bが投稿Aと同じURLを共有している場合、投稿Aを削除してもまだ投稿Bが参照しているためS3からは削除されない（`DeleteObjectsCommand`が呼ばれない）。共有されていない画像は削除時にそのままS3から消える | 正常系・回帰（モジュール境界でのモックでは見えないservice/repository層間の結合を実DB込みで検証する統合テスト。新規ファイル`post.service.integration.test.ts`として追加。コピーオンリファレンス方式を検討・不採用とした経緯で、削除時確認単独の安全性を実DBで裏付けるために追加） |
 | T-141 | `GET /api/users/[id]/posts` | 未認証／認証済み／`cursor`・`year`・`limit`指定／`limit`999指定 | `GET`呼び出し | 未認証でも200（公開一覧）。認証済み時は`viewerId`にセッションの`userId`が渡る。`cursor`/`year`/`limit`はそのまま`findPostsByAuthorIdService`へ渡る。`limit`は50に丸められる | 正常系・境界値（GATE-22種類A新規。投稿一覧の継続取得API） |
 | T-142 | `GET /api/mypage/wishlist`／`GET /api/mypage/visited` | 未認証／認証済み（クライアント指定`userId`クエリあり）／`limit`999指定 | `GET`呼び出し | 未認証は401でrepositoryは呼ばれない。`userId`クエリがあってもセッションの`userId`のみで呼ばれる（他人のリストを覗けない）。`limit`は50に丸められる | 異常系・権限境界（GATE-22種類A新規。本人限定のcursor継続取得API） |
| T-143 | `findPostsByAuthorId`/`findExplorePosts`/`findFollowingPosts`（実DB） | 複数投稿の`createdAt`が同一 | `limit`を件数未満に指定してページング | `id`をタイブレーカーとした`orderBy`により、ページを跨いでも重複・欠落なく全件が取得できる | 境界値・回帰（実DB。GATE-22種類A対応で発覚した、`createdAt`のみの`orderBy`では同時刻投稿が重複・欠落し得た既存バグの再発防止） |
| T-144 | `findWishlistedPosts`/`findVisitedPosts`（実DB） | 複数登録の`createdAt`（`Wishlist`/`Visited`）が同一 | `limit`を件数未満に指定してページング | `postId`をタイブレーカーとした`orderBy`により、ページを跨いでも重複・欠落なく全件が取得できる | 境界値・回帰（同上。行きたい/訪問済みの中間テーブル側cursorにも同種のタイブレーカーを適用） |
 | T-145 | `LoadMoreList`（マイページ/プロフィール共通の無限スクロールコンポーネント） | `hasMore=false`／sentinelが表示範囲に入り追加取得成功／`baseUrl`に既存クエリあり／API失敗 | IntersectionObserverモック（observe直後に`isIntersecting: true`を発火）・レンダリング | `hasMore=false`時はfetchが発生しない。成功時は取得結果を末尾に追記しcursor/hasMoreを更新。既存クエリがある`baseUrl`には`&cursor=`を付与する。失敗時はエラートーストを表示し既存の一覧・状態は変化しない | 正常系・異常系・UIコンポーネント（GATE-22種類A新規、2026-08-01に無限スクロール化に伴い検証方法をクリックからIntersectionObserverモックへ変更） |
 | T-146 | `findUserNotifications`（実DB） | 通知51件、`limit=50`でページング | `limit`のみ／`cursor`指定 | 1ページ目50件・`hasMore=true`。2ページ目でcursor経由の残り1件が重複なく取得できる（合計51件） | 境界値（実DB。GATE-22種類A新規。通知一覧の51件目以降アクセス確認） |
 | T-147 | `findUserNotifications`（実DB） | 複数通知の`createdAt`が同一 | `limit`を件数未満に指定してページング | `id`をタイブレーカーとした`orderBy`により、ページを跨いでも重複・欠落なく全件が取得できる | 境界値・回帰（実DB。GATE-22種類A新規。既存の投稿系cursor実装と同種のタイブレーカー欠如を通知にも先回りして適用） |
 | T-148 | `getUserNotifications`（service、repositoryモック） | `cursor`/`limit`指定 | `getUserNotifications(userId, { cursor, limit })` | `findUserNotifications`へ`{ userId, cursor, limit }`がそのまま渡る | 正常系（GATE-22種類A新規。既読化の順序保証は既存T-番号のテストを踏襲） |
 | T-149 | `GET /api/notifications` | 未認証／認証済み（cursor未指定・指定）／`limit`999指定 | `GET`呼び出し | 未認証は401。認証済みは既定`limit=20`で呼ばれる。`cursor`指定時はそのまま渡る。`limit`は50に丸められる | 正常系・境界値（GATE-22種類A新規） |
| T-150 | `NotificationList`（コンポーネント） | `hasMore=false`／初回取得・sentinel交差による追加取得成功／初回・追加取得の非2xxまたは通信失敗／既読PATCH失敗 | IntersectionObserverモック・レンダリング | `hasMore=false`時はfetchが発生しない。成功時は取得結果を末尾に追記し、2回目以降のfetchには`cursor`クエリが付与される。初回失敗時は空一覧と区別したエラー表示、追加取得失敗時はトーストを表示して既存一覧・cursorを保持する。既読化失敗時は楽観的に消えた未読ドットがDOM上で復元される | 正常系・異常系・楽観的UIロールバック・UIコンポーネント（GATE-22種類A新規、2026-08-01に無限スクロール化に伴い検証方法をクリックからIntersectionObserverモックへ変更） |
 | T-151 | `findCommentsByAuthor`（実DB） | 51件のコメント、`limit=50`でページング／`createdAt`が同一のコメント群 | `limit`のみ／`cursor`指定 | 1ページ目50件・`hasMore=true`、2ページ目で残り1件が重複なく取得できる。`id`タイブレーカーにより同時刻でも重複・欠落なく全件取得できる | 境界値（GATE-22種類B） |
 | T-152 | `findFollowers`（実DB） | 51件のフォロー関係、`limit=50`でページング／`createdAt`が同一 | `limit`のみ／`cursor`指定 | `followerId`タイブレーカーにより、2ページで重複・欠落なく全件取得できる | 境界値（GATE-22種類B） |
 | T-153 | `findFollowersService`（service、repositoryモック） | `viewerId`未指定／指定 | `findFollowersService({userId, viewerId})` | 追加取得ページでもフォロー中状態を正しく付与する | 正常系（GATE-22種類B） |
 | T-154 | `findActivePlansByUserId`/`findCompletedPlansByUserId`（実DB） | 51件・同一`startDate`のプラン群 | `limit`のみ／`cursor`指定 | `id`タイブレーカーにより継続取得で重複・欠落しない | 境界値（GATE-22種類B） |
 | T-155 | `findCompletedPlansByUserId`（実DB） | `year`指定／未指定（`startDate`未設定を含む） | `findCompletedPlansByUserId({userId, year})` | 年指定時は対象年のみ、未指定時は日付未設定分も含む | 正常系・境界値（GATE-22種類B） |
 | T-156 | `findCompletedPlanYears`（実DB） | 日付未設定を含む複数年の完了済みプラン／プランなし | `findCompletedPlanYears(userId)` | 日付未設定分を除外し年降順、プランなしは空配列 | 正常系・境界値（GATE-22種類B） |
 | T-157 | コメント・フォロー・プランのcursor API | 未認証／認証済み／`cursor`・`limit`・`year`指定 | 各`GET`呼び出し | 本人限定は401/403、公開一覧は未認証でも200、`limit`は50に丸める | 正常系・権限境界（GATE-22種類B） |
| T-158 | `PlanLoadMoreList`/`CommentLoadMoreList`/`UserLoadMoreList` | `hasMore=false`／sentinelが表示範囲に入り追加取得成功／コメント・ユーザー一覧APIの非2xxまたは通信失敗 | IntersectionObserverモック・レンダリング | `hasMore=false`時はfetchが発生しないこと、成功時は末尾に追記されることを検証する。失敗時はトーストを表示し、取得済み一覧・cursor・`hasMore`を保持する | 正常系・異常系・UIコンポーネント（GATE-22種類B、2026-08-01に無限スクロール化に伴い検証方法をクリックからIntersectionObserverモックへ変更） |
 | T-159 | `useInfiniteScroll`（共通フック、`src/hooks/useInfiniteScroll.ts`） | `hasMore=false`／sentinel交差／`loading=true`中の交差／アンマウント／`hasMore`がtrue→falseに変化／`rootMargin`省略時・指定時 | IntersectionObserverモック・`renderHook`相当のハーネスコンポーネント | `hasMore=false`時はobserveされずコールバックも呼ばれない。交差時に`loading=false`なら`onLoadMore`を呼ぶ、`loading=true`中は呼ばない。アンマウント時に`disconnect`される。`hasMore`がfalseになると新規observeを行わず既存observerをdisconnectする。`rootMargin`省略時は既定の`200px`が使われる | 正常系・UIコンポーネント（2026-08-01無限スクロール対応で新設した共通フックの単体テスト） |
| T-160 | `SpotPicker`（プラン編集画面のスポットピッカー、`src/components/plans/SpotPicker.tsx`） | 行きたいリスト候補が3件超／3件以下／その他のスポットを開いた際の初回・継続取得／非2xxまたは通信失敗 | クリック（アコーディオン開閉）・IntersectionObserverモック・レンダリング | 行きたいリスト候補は初期3件表示からsentinel交差で段階的に（10件ずつ）追加表示され、全件表示後はsentinelが消える。3件以下の場合はsentinelを表示しない。「その他のスポット」はアコーディオンを開くとcursorページングで初回取得し、sentinel交差で自動的に次ページを継続取得する。失敗時は対象別のトーストを表示し、表示済み候補を保持する | 正常系・異常系・UIコンポーネント（2026-08-01無限スクロール対応で新規追加。従来ユニットテストが存在しなかったコンポーネント） |
| T-161 | `SearchClient`（検索画面の旅スポット・エリア・ユーザー各タブ、`src/components/search/SearchClient.tsx`） | 新着順でsentinel交差による継続取得／次ページが無い場合／各検索APIの非2xxまたは通信失敗 | IntersectionObserverモック・レンダリング | 1ページ目取得後、`hasNextPage=true`であればsentinel交差で自動的に`cursor`付きの次ページを取得し末尾に追記する。次ページが無い場合は追加のfetchが発生しない。失敗時は空結果と区別した対象別エラー表示を行う | 正常系・異常系・UIコンポーネント（2026-08-01無限スクロール対応で新規追加。従来ユニットテストが存在しなかったコンポーネント。人気順は上位20件表示のみでページング対象外のため本テストの対象外） |

※ いいね数・コメント数・フォロワー数/フォロー中数の非正規化カウンタ（`likeCount`/`commentCount`/`followerCount`/`followingCount`、2026-07-13追加）は既存の`user.repository.test.ts`（実DB）・`comment.service.test.ts`（repositoryモック）をカウンタ参照仕様に合わせて更新する形で反映済み（新規テストケース番号は付与せず、既存T-番号のテストを踏襲）。旅行レポートの都道府県塗り分け地図（`PrefectureMapView.tsx`）は既存の`visitedLocations`データをクライアント側で可視化するのみで新規APIを伴わないため、専用のテストケースは追加していない（ブラウザでの目視確認で担保）。

※ 2026-07-20の旅行レポート大幅リニューアルに伴い、`e2e/report.spec.ts`の既存3テストがCIで実際に落ちていたのを発見し修正した（新規テストケース番号は付与せず、既存テストを踏襲）：①「訪問スポット数カード→年別旅行記録へスクロール」は両要素とも削除済みだったため「訪問エリア数カード→エリア別投稿数へスクロール」に差し替え、②「訪問エリアバッジ→検索ページへ遷移」はバッジ一覧→バブルチャートへの変更でリンクの実際のアクセシブルネームが変わった（`exact`一致→正規表現一致に変更）、③「年選択」はセレクターがページヘッダーへ移動した際にスマホ用/PC用の`<select>`がCSS表示切替のみで同時にDOM上に存在するようになり`page.locator("select")`がstrict mode違反になり得たため`select:visible`に変更（Phase 5-Aで踏んだ既知のパターンの再発）。ローカルでの実行確認は`.env.local`のAUTH_URL設定（LAN IP向け）によりブロックされたため、コードレビューでの確認とCI実行結果での最終確認に委ねた。

※ `createPost`が投稿者自身の`Visited`行を自動作成するようになったことに伴い、既存の`post.repository.test.ts`の`findVisitedPosts`テスト（他人の投稿への明示的な訪問済み登録のみを検証していた）と、`user.repository.test.ts`の2件（`countVisitedByUser`・`computeTabiScoreInputsForUsers`）を、新しい自動作成の仕様に合わせて更新した（自分の投稿は自動でカウントに含まれるため期待値を修正、重複作成になる明示的な`visited.create`呼び出しは削除）。

※ 2026-07-20の旅行レポート大幅リニューアルに伴い、`stats.service.test.ts`の既存テストも新仕様に合わせて更新した（新規テストケース番号は付与せず、既存T-番号のテストを踏襲）。`topLocation`のタイブレーク基準を「訪問日順で最初に出現したもの」から「`locationBreakdown`（エリア別バブルチャートの並び）先頭と一致するもの」に変更したのに伴いテストケースを差し替え（同数タイのカードと最大バブルが別の県を指す不整合を防ぐための変更）。「投稿数の推移」の折れ線が単年表示では出なくなった代わりに新設した月別ヒートマップ用に、`seasonalPostCount`（全期間・年をまたいだ月別通算）のテストを追加し、単年/全期間で`monthlyPostCount`/`seasonalPostCount`が排他的に埋まることを検証している。

※ Controller層（Vitest）は代表として `posts/route.ts`・Phase 3の`users`系ルート・`upload`系ルート・Phase 4の`plans`/`stats`系ルート・アカウント設定の`password`/`email`系ルート・Phase 5-Dで追加した`auth/signup`・`health`ルート・Phase 5-C-2で追加した`posts/[id]`・`comments`・`like`・`visited`・`wishlist`・`comments/[id]`・`notifications`系・`posts/portal`（Controller層T-116〜T-121。あわせて`visited.service`/`wishlist.service`のService層テストT-114〜T-115も追加）を実装。E2Eは認証フロー（`auth.spec.ts`）・投稿の主要フロー（`posts.spec.ts`）・フォロー等（`users.spec.ts`）・旅行プランの主要フロー（`plans.spec.ts`）に加え、アカウント設定の主要フロー（`account-settings.spec.ts`）を追加した。コメント・削除・フォロー・プラン・アカウント設定系のE2Eは `page.waitForResponse()` で実APIレスポンスを待ってから検証する方式とし、楽観的UIの見た目だけで判定する不安定なアサーションを避けている。`account-settings.spec.ts` は `test.describe.serial` 内でも各 `test()` が新しいブラウザコンテキストを持つ点に注意し、パスワード変更後の再ログイン確認には `page.context().clearCookies()` でセッションを明示的に破棄している（`/login` は認証済みセッションがあると `proxy.ts` のミドルウェアによりホームへリダイレクトされるため）。

---

## 8. 今後の継続方針

- ✅ Phase 4（旅行プラン・旅行レポート）は新規API追加と同時にテストを実装済み（T-57〜T-68）。この運用を今後のフェーズでも継続する
- ✅ 未カバーだった既存エンドポイント（comments, like, wishlist, visited, notifications）へのController層テスト、および`visited.service`/`wishlist.service`のテストはPhase 5-C-2で追加済み（T-114〜T-121）
- Repository層テストの対象を `findExplorePosts` / `findTopRatedByCategory` 等にも拡充
- `CommentSection.tsx` で見つかったレース条件パターン（10節）が他のクライアントコンポーネント（`NotificationList.tsx`等）にも潜んでいないか、Phase 4着手時に横展開で確認する

---

## 9. カバレッジ目標（Phase 5-C-2で確定）

- 対象範囲: `src/lib/services`・`src/lib/repositories`・`src/app/api`（バックエンドのみ）。`src/components`・`src/lib/validations`（フロントエンド）は対象外とする方針を維持（UIロジックはコンポーネントテスト・E2Eでカバーする方針のため）
- 2026-07-25時点の最終再測定値: Statements 89.1% / Branches 77.88% / Functions 80.55% / Lines 89.79%（DB起動後、全459件のテストがパスした状態での計測）
- 上記実測値から数ポイントの余裕を持たせ、`vitest.config.ts`の`coverage.thresholds`に **Statements 85% / Branches 75% / Functions 78% / Lines 86%** を設定。CIの`test`ジョブはこの閾値を下回ると失敗する（既存の穴を今すぐ塞ぐための閾値ではなく、今後の劣化を検知するための下限値）
- 計測コマンド: `pnpm test:coverage`（HTMLレポートは `coverage/index.html`）

---

## 10. E2E安定化の知見（2026-07-06、Phase 3で判明）

Phase 3で `users.spec.ts` を追加した際、既存の `posts.spec.ts` を含む全体実行が断続的に失敗する事象を詳細調査した。原因は当初「タイムアウトが短い」ことを疑ったが、実際には以下の**2つの異なる根本原因**であり、タイムアウト延長では解決しないことが判明した。

### 10.1 モジュールレベル変数のワーカー間非共有

`posts.spec.ts`・`users.spec.ts` は同一 `describe` 内のテスト間で `let postId = ""` のようなモジュールレベル変数を共有し、「同じファイルの全テストは同じワーカープロセスで実行される」という前提に依存していた。

通常はこの前提が成り立つが、**あるテストが失敗すると、Playwrightが後続テストを別のワーカープロセスに再割り当てすることがある**。新しいワーカーはモジュールを最初から読み込むため、共有変数は初期値（空文字）に戻る。この状態で `page.goto('/posts/' + postId)` を呼ぶと `/posts/`（実質 `/posts`）にアクセスし、404が発生する。

`playwright-report/results.json` の各テスト結果に含まれる `workerIndex` を比較することで、削除テストだけ別ワーカーで実行されていたことを直接確認できた（調査時の実例: テスト1〜4が`workerIndex: 1`、テスト5（削除）のみ`workerIndex: 3`）。

**対処**: `test.describe(...)` を `test.describe.serial(...)` に変更。これは「同一ワーカーでの順次実行」と「1つ失敗したら後続はスキップする」ことを明示的に保証するPlaywrightの公式パターンで、ワーカー再割り当てによる状態喪失が構造的に発生しなくなる。

### 10.2 `CommentSection.tsx` のレースコンディション（Phase 1由来の既存バグ）

上記の対処後も「コメント投稿→一覧に反映される」が稀に失敗するケースが残っていた。調査の結果、`CommentSection.tsx` の初回コメント一覧取得（`useEffect` 内の `loadComments()`）が、投稿（POST）の完了より**後に解決すると**、無条件の `setComments(data.comments)` が投稿前の空リストで上書きしてしまうレースコンディションと判明した。人間の操作速度では顕在化しないが、Playwrightの高速な自動操作では初回GETの完了を待たずに投稿が完了することがあり、負荷時にはさらに発生しやすくなる。

**対処**: `opSeqRef`（操作の連番）を導入し、`loadComments`・`handleSubmit`・`handleDelete` の開始時にインクリメント。`loadComments`のfetch完了時に連番が変わっていれば（＝自分より新しい操作が開始していれば）、コメント一覧・カーソル等のデータ反映のみを破棄する（ローディング解除は常に行う）。回帰テストを `CommentSection.test.tsx` に追加した（T-56）。

この過程で、投稿・コメント双方に同じ「削除」ボタンが存在する場合にPlaywrightのロール検索が曖昧になる問題も判明したため、`DeleteButton.tsx` に `data-testid="delete-post-button"` を追加してテストを安定させた。

**教訓**: ①タイムアウトを延ばす対処は、真の原因（状態管理・並行処理のバグ）を覆い隠すことがある。②E2Eで再現する不具合は、実装側の実際のバグを暴くことがあるため、テスト側だけでなく実装側も疑って調査する。

### 10.3 PR-7a ダークテーマ共有レイアウトの検証（2026-07-31、2026-08-01追記）

`e2e/dark-theme-shared-layout.spec.ts`で、未ログイン時のデスクトップ／モバイル、ログイン後のデスクトップ・タブレット・モバイルのユーザーメニュー、表示テーマ切替後のリロード永続化、ログイン直後ヒントトーストの767px表示／768px非表示境界を検証する（計12ケース）。主要なリンク・ナビゲーション・表示テーマトグル・アバターの文字色について、Canvasベースの`getContrastRatio()`で通常テキストのWCAG基準4.5:1以上を確認し、各表示をPlaywright screenshot baselineと比較する。投稿一覧など他E2Eが並列に作成するデータに依存する`main`領域はマスクし、共有レイアウト（サイドバー・モバイルナビ・テーマトグル・ユーザーメニュー・ロゴ）だけを視覚回帰の対象にする。

実装・検証中に、ログインボタン、表示テーマトグル、ナビゲーションのアクティブ状態、アバター頭文字のコントラスト不足を検出して修正した。また、Tailwind v4が返す`lab(...)`形式を正規表現で読めず誤検知していた初期ヘルパーを、Canvasの色解決を使う実装へ変更した。baselineはPlaywrightのOS別命名に従いWindows用・Linux CI用を`e2e/dark-theme-shared-layout.spec.ts-snapshots/`へ保存する。

**2026-08-01追記（カバレッジの抜けを検出・修正）**: 当初はログイン後のデスクトップ用ユーザーメニュー（`showLabels`表示）のみを検証しており、モバイル用ユーザーメニュー（`showLabels compact`、ラベル文字を縮小する別スタイル）は視覚回帰・コントラスト検証のいずれも行っていなかった。デスクトップ用のトリガーボタンが`hidden md:flex`でDOM上に残るため、モバイル幅のテストでは`aria-label`の部分一致で誤って両方を拾わないよう`.filter({ visible: true })`で絞り込む必要があった（`検索`/`ログイン`テキストの選定で先に判明していた問題と同種）。この抜けを埋める形でモバイル用ユーザーメニューのテストを追加し、7ケース→9ケースに拡張した。

**2026-08-01追記（タブレット幅のラベル折り返し回帰）**: 768〜899pxでは縮小サイドバー内のユーザーメニューに使える横幅が狭く、表示テーマのラベルが1文字ずつ折り返される問題があった。幅768pxのテストを1ケース追加し、各ラベルの`white-space`が`nowrap`であることに加え、ラベルの左右端が対応するradioボタンの境界内に収まることを`boundingBox()`で検証する。これにより計9ケース→10ケースとした。認証後のホスト名は環境設定によって`localhost`以外になる場合があるため、ログイン成功はホスト名ではなく遷移先のpathnameが`/`であることを検証する。

ロゴの3色グラデーション統一（`Logo.tsx`、ログイン状態で緑系統／エメラルド系統の2バリアント）は専用のコントラストアサーションこそ持たないが、各画面のスクリーンショット比較に含まれるため視覚回帰の対象にはなっている。

PR-7b〜7eの対象画面は未実施であり、GATE-40全体の完了とはしない。

### 10.3.1 PR-7b テーマ設定のCookie/DB化・認証/設定画面のダークテーマ対応で発見した重大な不具合（2026-08-01）

**発見の経緯**: Cookie/DB同期の手動検証中、`document.documentElement`の`data-theme="dark"`が正しく設定されているにもかかわらず、`dark:bg-zinc-700`等のTailwindユーティリティクラスが適用されない現象を発見した。Chrome DevTools Protocolでブラウザの`colorScheme`エミュレーションだけを変更すると（`data-theme`属性はそのまま）、`window.matchMedia("(prefers-color-scheme: dark)")`の真偽に応じて反映有無が切り替わることを確認し、**Tailwind v4の`dark:`バリアントが標準で`prefers-color-scheme`メディアクエリのみに従い、`data-theme`属性（PR-7aで導入した手動選択の仕組み）を一切見ていない**ことが根本原因と判明した。

**影響範囲**: `--surface`/`--surface-foreground`/`--surface-border`等のCSS変数経由の配色（`bg-surface`等）は`:root[data-theme="dark"]`という属性セレクタで別途定義されているため無関係だが、それ以外の場所で直接`dark:`接頭辞のTailwindクラスを使っている箇所（PR-7aで既にマージ済みのSidebar等のアクティブ状態色・ホバー色・ロゴのダーク配色や、本PRで新規に追加したAccountForms/SettingsForm等の配色）は、**OS側の設定と手動選択が食い違う場合（例: OSはライトのまま「ダーク」を手動選択）に、CSS変数系の配色だけダークになり残りは元のまま、という中途半端な表示になる**欠陥があった。

**対処**: `src/app/globals.css`に`@custom-variant dark`を追加し、`&:where([data-theme="dark"], [data-theme="dark"] *)`（属性が明示されている場合は最優先）と`@media (prefers-color-scheme: dark) { &:not(:where([data-theme="light"], [data-theme="light"] *)) { ... } }`（属性未指定時のみOS設定にフォールバック）の2条件で`dark:`バリアント自体を再定義した。これにより既存の`--surface`系トークンと同じ優先順位（手動選択 > OS設定）に統一され、PR-7aで既に本番相当の色として使われていた`dark:`クラスも遡って正しく動作するようになった。

**検証時の落とし穴**: `pnpm dev`（Turbopack）の開発サーバーは、この`globals.css`変更を反映せず、プロセスを完全に再起動しても古い挙動（`prefers-color-scheme`のみ）のままだった。標準の`@tailwindcss/cli`で同じファイルを直接コンパイルすると正しい出力になることを確認し、Turbopackの開発サーバー固有のキャッシュ・反映の問題と切り分けた。Playwrightのe2eが実際に使う起動経路（`playwright.config.ts`の`webServer`は`pnpm build && pnpm start`）である本番ビルド・`next start`では正しく反映されることを確認済みであり、**この開発サーバー固有の現象はCI・本番の動作には影響しない**。

**教訓**: ①手動テーマ切替のような「OS設定を上書きする」機能を実装する際、CSS変数だけでなくフレームワークのバリアント機構自体（Tailwindの`dark:`）がどの条件で発火するかを個別に確認する必要がある。静的なコードレビューやOSに合わせたブラウザエミュレーションだけの確認では、「OS設定と手動選択が一致するケース」しか検証できず、本不具合は発見できなかった。②`colorScheme`エミュレーションと実際のDOM属性（`data-theme`）を意図的に食い違わせて確認する手順を、今後のダークテーマ関連PR（7c〜7e）のテスト観点に加える。③開発サーバー（Turbopack）と本番ビルドで挙動が異なる場合があるため、CSSの根本的な設定変更（バリアント定義等）を検証する際は本番ビルド相当（`pnpm build && pnpm start`）でも必ず確認する。

### 10.3.2 PR-7b テスト実行証跡（2026-08-01）

**バックエンド**: `pnpm test:coverage` — 97ファイル・704件成功。カバレッジ Statements 92.06%／Branches 85.09%／Functions 85.17%／Lines 92.64%（閾値85/75/78/86をすべて上回る）。新規に`src/lib/__tests__/theme-cookie.test.ts`（Cookie解決・検証の純粋関数）、`src/app/api/me/theme/__tests__/route.test.ts`（POST/PATCHの認証・検証・Cookie設定）、`user.service.test.ts`・`user.repository.test.ts`への追記（DB優先・null昇格・version不変の確認）を追加。

**E2E**: 新規`e2e/dark-theme-auth-settings.spec.ts`（ログイン・新規登録画面のライト/ダーク、未ログイン時のCookie即時保存、Cookie→DB昇格、ログアウトでのDB→Cookieミラー、`/settings`「テーマ」カードの表示・保存・リロード後維持、設定/アカウント設定画面のライト/ダーク）9ケースと、`e2e/dark-theme-home-feed.spec.ts`（ホームフィードの見出し・投稿カード・エリア/カテゴリ見出しのコントラスト）1ケースを追加。既存`e2e/dark-theme-shared-layout.spec.ts`のテーマ永続化テストは、Cookie/DB化に伴いPATCHのデバウンス完了を待ってからリロードする形に修正した。単独実行で全19ケース成功を2回確認。

**既知の不安定要因（新規）**: ログアウト直後に同一テスト内で即座に再ログインする手順は、`signOut()`のセッションCookieクリアが`/api/auth/signout`のレスポンス受信より遅れて完了することがあり不安定だった。同一テスト内での「ログアウト→即再ログイン」検証は避け、DB値が次回ログインに反映されることは別テスト（新しいブラウザコンテキストでのフレッシュなログイン）で確認する設計にした。

### 10.3.3 PR-7bのPR作成後、CI（Linux・並列実行）で新たに顕在化した3つの不具合と対処（2026-08-01）

ローカル（Windows・単一ワーカー中心）では安定していたが、CIの並列ワーカー・共有DBという実行環境の違いにより以下が発覚した。

1. **`main`要素のマスク範囲が投稿件数に連動して変動する**: `dark-theme-shared-layout.spec.ts`の未ログイン画面テストは、投稿一覧を含む`main`をスクリーンショットマスクで隠すが、マスクの矩形サイズは`main`の実際の高さ（＝投稿件数に依存）で決まる。CIの共有DBは実行順序次第で投稿件数が変動するため、ベースライン撮影時と比較時でマスク範囲がずれ、無関係な背景領域が誤って差分として検出された。`(public)/layout.tsx`の`<main>`に`min-h-screen`を追加し、投稿件数によらずマスク対象領域が最低でもビューポート高になるよう固定した。
2. **新規テストが他specの投稿データに依存していた**: `dark-theme-home-feed.spec.ts`は「投稿が1件は表示される」前提だったが、CIでは他specとの実行順序次第で投稿0件の状態でテストが走ることがあった。自スペック内で専用アカウント作成・投稿作成まで行い、他specのデータに一切依存しない形に修正した。
3. **設定画面系テストが共有アカウントの作成成功に依存していた**: `dark-theme-auth-settings.spec.ts`の「ログイン後」describeブロックは、最初のテスト（新規登録UIの検証）が作成したアカウントを後続3テストが使い回す設計だったが、CIでは実行順序が保証されずアカウント未作成のまま後続テストがログインを試みて失敗した。signup UI自体を検証するテスト（アカウント事前不在が前提）と、フレッシュログインのみを必要とするテスト（`/settings`・設定/アカウント設定画面）を別のdescribeブロック・別アカウントに分離し、後者は`beforeEach`で自分自身のアカウント存在を保証する（409は無視）設計にした。

**教訓**: ローカルでの単独・少数ファイル実行で安定していても、CIの並列ワーカー・共有DBという条件でしか顕在化しない実行順序依存のバグがある。特に「前のテストの成功に暗黙に依存する」設計と「動的なDOM高さに依存するスクリーンショット比較」は、ローカルの単一ワーカー実行では気づきにくい。

### 10.3.4 PR-7c 投稿フォーム・カードのダークテーマ対応の検証（2026-08-01）

新規`e2e/dark-theme-post-forms.spec.ts`（10ケース）で、グループD（投稿フォーム・カード）の残りを検証する。投稿作成フォーム（`/posts/new`）はライト/ダーク比較スクリーンショットとラベル・訪問日欄・費用内訳見出し・送信ボタンのコントラスト検証、訪問日カレンダーを開いた状態のナビゲーション・選択日セルのコントラスト検証とスクリーンショット、投稿作成→マイページ自分の投稿タブ一覧→編集画面→削除確認ダイアログの一連の遷移でのコントラスト検証、投稿詳細ページでの画像付き`ImageCarousel`と`DeleteButton`本体（`MyPostActions`のConfirmDialogとは別コンポーネント）のコントラスト検証、行きたいへ追加した投稿カード・`SavedMapSection`（凡例・地図クリックによるハイライト・地図に戻るボタン）・外す確認ダイアログのコントラスト検証、行きたいリストが空の場合の`EmptyState`のコントラスト検証、フォロー中フィード（`FollowFeed`、`describe.serial`で専用のフォロー対象アカウント・投稿を用意）のカードのコントラスト検証を行う。

**環境チェック**: 本PRの着手前に5節のDoDに沿ってDocker（`mysql-test`）起動・`127.0.0.1:3307`疎通・`pnpm prisma:migrate:test`（適用済み・追加マイグレーションなし）を確認した。ローカルには常駐の開発用サーバー（ポート3000、`.env.local`の`AUTH_URL`がLAN IPのため`next start`本番ビルドで実行するとPlaywrightの`baseURL`〔`localhost`〕とホスト名が食い違いログイン後の遷移検証が失敗する）が存在したため、Saki さんの許可を得てから一時停止し、`DATABASE_URL`（テストDB）・`AUTH_URL`（`http://localhost:3000`）・`TEST_CLEANUP_SECRET`・`DISABLE_RATE_LIMIT_FOR_TESTS`・`AWS_REGION`/`AWS_S3_BUCKET_NAME`（ダミー値。画像アップロードAPIをモックしつつ投稿作成APIの所有権検証・Next Imageの許可ホストを満たすために必要）をこの検証用シェルにだけ上書きしてPlaywrightのwebServer（`next build && next start`）に本番ビルドを起動させた。開発用サーバーの`.env.local`自体は変更していない。

**初回レビュー（Terra）で判明した不具合と対応**（詳細は`実装計画書/reviews/6-b-fix-dr-02-dark-theme.md`のPR-7c実施記録参照）:
- スクリーンショット比較対象の投稿タイトルに`Date.now()`を含めていたため通常実行のたびに差分になっていた（固定文字列へ変更）
- `Sidebar.tsx`のログイン直後アバター長押しヒントトースト（本来ボトムナビ専用）が、ビューポート判定を持たずデスクトップ幅でも表示される実装バグにより、スクリーンショットへ非決定的に写り込んでいた。境界確認用に`e2e/dark-theme-shared-layout.spec.ts`へ767px表示・768px非表示の2ケースを追加する過程で、`matchMedia("(max-width: 767px)")`ではブラウザの端数丸めにより`innerWidth === 767`でも`matches`が`false`になることが判明した。Tailwindの`md:hidden`と同じ条件を確実に使うため、`matchMedia("(min-width: 768px)")`の反転へ修正した。E2E側も、トースト自動消滅（2.5秒）より前にアサーションが始まる保証がなかったため、`sessionStorage`の`justLoggedIn`フラグ消費（＝Sidebarのeffect実行済み）を`page.waitForFunction`で待ってから表示・自動消滅を確認する同期点ベースの検証に修正した
- 画像アップロードAPIが実際にはS3未設定で500エラーとなっていた（アップロードAPIをモックし、投稿作成APIの所有権検証に合う形式のURLを使う方式へ変更。CIの`e2e`ジョブにも`AWS_REGION`/`AWS_S3_BUCKET_NAME`のダミー値を追加した）。ダミーS3 URLをDBへ保存した後に`next/image`が実S3へ404取得しないよう、E2E共通fixtureで該当する`/_next/image`リクエストだけを1px PNGへ応答する。実S3アップロードの検証は認証済み環境で別途行う
- `SavedMapSection`の「地図に戻る」ボタン検証で、`scrollIntoViewIfNeeded()`が地図を画面外に出せていなかった、SVGの都道府県形状への座標クリックがパスを外していた、IntersectionObserverの`rootMargin`を考慮しない閾値だった、の3点を是正
- **再レビューでの追加指摘**: `mypage/page.tsx`の「＋ 新しいプラン」ボタン（`bg-green-600`＋白文字、約3.30:1）も同種のコントラスト不足だったため`bg-green-700`へ変更（コントラスト不足の合計は6件）

**実行証跡**: 通常実行（`--update-snapshots`なし）を2回連続実行し、いずれも10/10ケース成功を確認。Windows用baselineを`e2e/dark-theme-post-forms.spec.ts-snapshots/`へ追加した。Ubuntu CI run `30698925646` のmissing baseline失敗で生成された実画像を確認し、同ディレクトリへLinux用baselineを4枚追加した。fixture変更とLinux baseline追加後のCI成功証跡は、次回のPR更新で取得する。

### 10.3.5 PR-7d 旅行プランのダークテーマ対応の検証（2026-08-01、Terraレビューを反映し更新）

新規`e2e/dark-theme-plans.spec.ts`（初版6ケース→Terraレビュー対応で15ケースに拡充）で、グループE（旅行プラン）を検証する。プラン作成フォーム（`/plans/new`）はライト/ダーク比較スクリーンショットとタイトルラベル・予算「＋ 項目を追加」ボタン・メモラベル・作成/キャンセルボタンのコントラスト検証、`SpotPicker`はキーワード検索欄・行きたいリスト候補（`SpotListItem`）・選択済みスポット（`SortableSelectedItem`）・新規スポット追加欄（自由入力）のコントラスト検証、プラン詳細ページはメモ・予算内訳（合計金額・内訳チップ）・スポット一覧・「旅を記録」リンクのコントラスト検証、プラン編集ページは見出し・完了ステータス切り替えボタンのコントラスト検証、削除確認ダイアログはタイトル・キャンセルボタンのコントラスト検証、マイページ旅行プランタブは`PlanLoadMoreList`の一覧カード・`CompletedPlansAccordion`（開閉両方）のコントラスト検証、マイページ旅行レポートタブは`ReportSummary`のまとめ見出し・カテゴリ別バーグラフ・月別ヒートマップ・高評価スポットTOP3のコントラスト検証、およびエリア別バブルチャート（`LocationBubbleChart`、常に色付きグラデーション背景の上に白文字が乗るため他のカードと異なりページ背景とのコントラスト計算の対象にならない）の表示確認を行う。

**環境チェック**: 10.3.4節（PR-7c）と同じ理由で、`DATABASE_URL`（テストDB`127.0.0.1:3307`）・`AUTH_URL`（`http://localhost:3000`、`.env.local`のLAN IPとPlaywrightの`baseURL`不一致を避けるため）・`TEST_CLEANUP_SECRET`・`ENABLE_TEST_ENDPOINTS`・`DISABLE_RATE_LIMIT_FOR_TESTS`を検証用シェルにのみ上書きした。今回はポート3000で常駐する開発用サーバーが存在しなかったため、サーバー停止の許可確認は不要だった。`pnpm prisma:migrate:test`は適用済み・追加マイグレーションなしを確認済み。

**スコープ拡張**: 実装着手前の棚卸しでDR-02当初のグループE（5ファイル）に加えて`PlanForm.tsx`・`PlanActions.tsx`・`PlanLoadMoreList.tsx`・`CompletedPlansAccordion.tsx`にも未対応箇所が見つかったため、AskUserQuestionでSaki さんに含めるかどうかを確認し、旅行プラン関連一式として本PRに含めた（詳細は`実装計画書/reviews/6-b-fix-dr-02-dark-theme.md`のPR-7d実施記録参照）。

**Terraレビュー（2026-08-01、コード変更なしのE2E・文書レビュー）で「条件付きApprove」となり、以下3点を追加対応した**:

1. **モバイル・タブレット幅の検証が欠落**: 初版は全ケースが1280×900のみで、`SpotPicker`・`PlanForm`・`PlanActions`・`CompletedPlansAccordion`が持つ`sm`/`md`/`xl`のレスポンシブ分岐を検証していなかった。新規`describe`「モバイル・タブレット幅でのダークテーマ対応」を追加し、375px・768pxでプラン作成フォーム・`SpotPicker`（新規スポット追加・選択済みスポット表示、スクリーンショット付き）とマイページ旅行プランタブ（`PlanActions`のiconsバリアント・`CompletedPlansAccordion`、スクリーンショット付き）を検証、320pxは主要要素の表示・コントラストのみの簡易確認（できればの位置づけ、スクリーンショットなし）とした。768pxは`md:dark:border-zinc-700`のような複合バリアントが実際に生成・適用されることを確認する目的も兼ねる。
2. **例外レスポンス時のUI表示が未検証**: API層・Service層・Repository層の例外パスは既存のControllerテストでカバー済みだが、UI側のエラートースト表示（ダークモードでの可読性含む）が未検証だった。新規`describe`「例外レスポンスのUI表示（ダークモード）」を追加し、`PlanForm`（作成APIの400・通信失敗）、`PlanActions`（完了切り替えの409・削除失敗）、`SpotPicker`（スポット検索・その他のスポット取得の失敗）、`PlanLoadMoreList`（継続取得失敗時に既存一覧を保持したままエラー表示すること）を、いずれもトーストのコントラスト検証込みで確認した。
   - **この過程で、`src/components/ui/toast.tsx`（全アプリ共通のトースト表示、`useToast`経由で全フォームが使用）に`dark:`バリアントが一切なく、ダークモードでは常に薄いパステル背景＋中間色文字のライト配色のまま表示される欠陥を発見した**。GATE-40の当初のbg-white件数調査（グループA〜Gの棚卸し）にtoast.tsxが含まれていなかったため見落とされていた（`ui/empty-state.tsx`が同種の理由でPR-7cまで見落とされていたのと同じパターン）。旅行プランに限らず全アプリのトースト表示に影響するため、本PRで`bg-red-50 dark:bg-red-950`・`text-red-700 dark:text-red-400`等のペアリングを追加して是正した。あわせて`data-testid="toast"`をトースト要素に追加し、以後のE2Eから安定して参照できるようにした。
   - 上記に伴い`src/components/ui/__tests__/toast.test.tsx`の既存テストへの影響を確認済み（クラス文字列を検証していないため無変更で成功）。
3. **記述と実テストの不一致**: 上記1節の対応として、旅行レポートタブのエリア別バブルチャートを「コントラスト検証」と記載していたが、実際のテストは`toBeVisible()`による表示確認のみだった（バブル自体が色付きグラデーション背景の上に白文字を乗せる方式で、ページ背景とのコントラスト計算に意味がないため意図的に対象外としていた）。本節の記載を実装に合わせて修正した。

**再現性の問題2件（Terraレビュー後の実装時に判明、コード品質とは別のテスト実装上の不具合）**:
- スクリーンショット比較対象のプランタイトルに`Date.now()`を含めていたため、PR-7cで一度是正したのと同じ「動的テキストが再実行のたびに差分になる」問題を再度作り込んでいた（マイページ旅行プランタブのモバイル/タブレット幅スクリーンショット）。幅ごとの固定文字列へ変更した。
- 無限スクロールの継続取得が失敗しても sentinel が画面内に留まったままだと`IntersectionObserver`が再度発火し、同じ内容のエラートーストが複数表示され得る（アプリ側の意図した自動リトライ挙動であり、バグではない）。トーストの`Locator`に`.first()`を付与し、複数一致時のstrict modeエラーを避けるようにした。

**実行証跡**: 通常実行（`--update-snapshots`なし）を2回連続実行し、いずれも15/15ケース成功を確認。実行コマンド（Windowsローカル、テストDB使用）:
```
pnpm prisma:migrate:test
DATABASE_URL="mysql://tripdiary:tripdiary@127.0.0.1:3307/tripdiary_test" \
AUTH_URL="http://localhost:3000" TEST_CLEANUP_SECRET="<任意の値>" \
ENABLE_TEST_ENDPOINTS=true DISABLE_RATE_LIMIT_FOR_TESTS=true \
pnpm exec playwright test e2e/dark-theme-plans.spec.ts --project=e2e
```
Windows用baselineを`e2e/dark-theme-plans.spec.ts-snapshots/`へ追加した。あわせて既存の`e2e/plans.spec.ts`・`e2e/dark-theme-post-forms.spec.ts`・`e2e/dark-theme-auth-settings.spec.ts`（計24ケース）を実行しデグレがないことを確認した（`dark-theme-post-forms.spec.ts`の画像アップロードケースは上記コマンドに`AWS_REGION=ap-northeast-1 AWS_S3_BUCKET_NAME=tripdiary-e2e-dummy-bucket`を追加すれば成功することを確認済み。CIでは`.github/workflows/ci.yml`にダミー値が設定済みのため影響しない）。

**共有レイアウトvisual回帰8件の是正（2026-08-01）**: 当初はmain相当でも再現したためローカル環境差分として切り分けたが、差分画像を調査した結果、原因はフォントではなかった。投稿量でページ全体の高さが変わると、`main`をマスクしていても右端スクロールバーの長さ・位置はマスク外で変動する。このため、共有レイアウトだけを比較する`e2e/dark-theme-shared-layout.spec.ts`では、スクリーンショット撮影時に限り`html, body`のスクロールバーを隠すヘルパーを導入し、投稿一覧に依存する差分を比較対象から除外した。テストDB（`127.0.0.1:3307`）でWindows baselineを更新し、`--update-snapshots`なしの通常実行でも12/12成功を確認した。Linux baselineとCI実行証跡は別途取得する。

**参考: 実装計画書配下（`実装計画書/reviews/6-b-fix-dr-02-dark-theme.md`のPR-7d実施記録）は`.gitignore`対象のため、PRの再現可能な証跡としては使えない**。上記の実行コマンド・baseline追加・回帰確認結果を本節（テスト設計書、追跡対象）に記録することで代替する（PR-7a〜7cと同じ運用）。

**CI実行証跡（2026-08-02）**: PR #43（https://github.com/saki-nakata/TripDiary/pull/43 、コミット`87ee0d1`）のCIで`lint`・`typecheck`・`build`・`test`・`e2e`の5ジョブ全て成功（実行 https://github.com/saki-nakata/TripDiary/actions/runs/30763048188 ）。Linux用baseline（`e2e/dark-theme-plans.spec.ts-snapshots/*-e2e-linux.png`、6枚）はコミット`cc4c11c`「test: add Linux baselines for PR-7d plans」で追加済み。あわせてスクリーンショット比較の安定化（`caret: "hide"`によるカーソル点滅の除外、コミット`8aed562`）も追加反映されている。

### 10.3.6 PR-7e 探索・検索・タグ＋その他のダークテーマ対応の検証（2026-08-03）

新規`e2e/dark-theme-explore-search.spec.ts`（20ケース）で、DR-02グループF（探索・検索・タグ）＋グループG（その他）の対象5ファイル（`SearchClient.tsx`・`YearFilterBar.tsx`・`MapView.tsx`・`LocationPicker.tsx`・`tags/[tag]/page.tsx`）を検証する。グループFの残り（`explore/TopRatedSection.tsx`・`CategorySection.tsx`・`AreaSection.tsx`）はPR-7bのホームフィード前倒し統合時点で既に`bg-surface`/`dark:`対応済みで本PRでの変更はなく、`dark-theme-home-feed.spec.ts`で「エリアから探す」「カテゴリから探す」見出しを検証済みのため対象外とした（実装計画書のグループF対応表参照）。

**Terraレビューでのスコープ確認**: 当初はTerraが確認した`bg-white`6件（`SearchClient.tsx`のカテゴリチップ2件・`YearFilterBar.tsx`の`select`1件・`MapView.tsx`/`LocationPicker.tsx`の閉じるボタン各1件・`tags/[tag]/page.tsx`の未選択チップ1件）＋その直接の連動クラス（hover/focus/選択状態）のみを対象にする案もあったが、Saki さんとTerraの確認により「対象5ファイル内の、通常・hover・focus・disabled・選択状態でダーク時に不整合となる配色」全てを本PRで対応する方針に確定した（対象外: 5ファイル外への横展開・レイアウト変更・機能/API変更・テーマトークン体系そのものの再設計）。この結果、`SearchClient.tsx`は見出し・検索欄・タブ下線・並び替えボタン・人気順ランキング行・エリアチップ・区切り線・ユーザーカード/バッジまで、`tags/[tag]/page.tsx`は見出し・戻るリンクまで含めて全面的に`dark:`対応した。

**実装中に発見したコントラスト不足2件（本PR対象ファイル内、DR-02の棚卸し対象「選択状態」に該当）**:
- `SearchClient.tsx`の並び替えボタン（新着順/人気順）・タグページの選択中チップの選択状態が`bg-[#16a34a] text-white`（Tailwind green-600相当のhex直書き）で、白文字とのコントラスト比は約3.30:1（WCAG通常テキスト基準4.5:1未満）。これはPR-7cで発見・是正済みの`mypage`の「＋新しいプラン」ボタン（`bg-green-600`→`bg-green-700`）と同種の欠陥だが、hex直書き（`#16a34a`）だったため当時の`bg-green-600`探索では見つからなかったもの。`bg-[#15803d]`（green-700相当）へ変更し、約5.0:1に是正した。
- `SearchClient.tsx`のカテゴリチップ選択状態が`bg-blue-400 text-white`で、コントラスト比は約2.6:1。`bg-blue-600`へ変更し、約5.2:1に是正した。

**検証内容**: 旅スポットタブ（見出し・検索欄・カテゴリチップの通常/hover/選択状態・並び替えボタン・人気順ランキング行の通常/hover）、エリアタブ（未選択/選択チップの通常/hover、絞り込み結果表示）、ユーザータブ（カード・称号バッジの通常/hover）、タグページ（見出し・選択/未選択チップの通常/hover）、マイページの年セレクター（`YearFilterBar`、`sm:hidden`側と`hidden sm:flex`側の2箇所レンダリングのうち表示中の方を`select:visible`で判定）、投稿詳細の`MapView`（通常表示・拡大モーダル・閉じるボタン）、投稿作成フォームの`LocationPicker`（通常表示・拡大モーダル・閉じるボタン）を、コントラスト自動検証（hover/選択状態を含む）とライト/ダーク比較スクリーンショットの両方で確認した。

**地図クリックによるピン設置は対象外**: `LocationPicker`の逆ジオコーディング（`nominatim.openstreetmap.org`への実通信）は、このE2Eスイートで前例のない外部ネットワーク依存であり、CI環境の制限下での安定性が未検証のため踏み込まなかった。対象はTerraが指摘した「地図モーダルの閉じるボタン」を含む通常表示・拡大表示の配色に限定した。

**スクリーンショットの非決定要因とTerraレビューでの是正方針（2026-08-03）**: 初版では以下3画面のスクリーンショットが複数回実行で非決定的になり、いったんスクリーンショット自体を取りやめる案を出したが、Terraから「非決定要因は対象UIではなく撮影範囲にある。ページ全体でなく安定するlocator／領域に絞って残すべき」と指摘を受け、以下の通り撮影範囲を絞ることで解決した（対象UIの視覚証跡は維持）。

| 画面 | 非決定要因 | 対処 |
|---|---|---|
| タグページ | 投稿グリッド（同一エリアの全ユーザー投稿、共有テストDBの蓄積で件数変動）／チップの並び順（投稿数依存で入れ替わる） | 見出し（`h1`単体、件数スパンを含まない）・選択中チップ・未選択チップをそれぞれ個別のlocator単位で撮影し、グリッドとチップの並び順依存を排除 |
| マイページ年セレクター | 投稿一覧側の「新規投稿ハイライト演出」（`highlighted-new-post`、本PR対象外の既存機能）がタイミング依存でフェードアウトする | `YearFilterBar`コンポーネント自身の根要素（ラベル+セレクト）だけをlocator単位で撮影 |
| `MapView`拡大モーダル | ①タイル取得先glob誤り（`**/tile.openstreetmap.org/**`は実際のリクエスト`a.tile.openstreetmap.org`のように`tile`の直前が`.`であり`/`を要求する当該globにマッチしない不具合。`**.tile.openstreetmap.org/**`へ修正）②モーダル背景（`bg-black/60`）越しに透ける背後の投稿カード（タイトルにDate.now()を含む） | モーダル本体はそのまま撮影しつつ、`.leaflet-container`と`[data-testid="post-card"]`を`mask`オプションで視覚回帰の対象から除外 |

`ユーザータブ`のスクリーンショットも同様に、既定では共有テストDB上の全ユーザーが対象になり非決定的だったため、検索欄に検索対象ユーザーのニックネームを入力して結果を1件に絞り込んでから撮影する方式に変更した（`main`配下のリンク数を`toHaveCount(1)`で確認してから撮影。サイドバーの「プロフィール」導線も`/users/{id}`形式のリンクのため、`main`配下に絞らないと2件と誤検出する）。

**実行証跡**: 通常実行（`--update-snapshots`なし）を5回以上連続実行し、いずれも20/20ケース成功を確認。実行コマンド（Windowsローカル、テストDB使用、本番ビルド）:
```
docker compose up -d mysql-test
pnpm prisma:migrate:test
DATABASE_URL="mysql://tripdiary:tripdiary@127.0.0.1:3307/tripdiary_test" \
AUTH_URL="http://localhost:3000" AUTH_SECRET="<任意の値>" \
ENABLE_TEST_ENDPOINTS=true TEST_CLEANUP_SECRET="<任意の値>" DISABLE_RATE_LIMIT_FOR_TESTS=true \
AWS_REGION=ap-northeast-1 AWS_S3_BUCKET_NAME=tripdiary-e2e-dummy-bucket \
pnpm build && pnpm start &
pnpm exec playwright test e2e/dark-theme-explore-search.spec.ts --project=e2e
```
Windows用baselineを`e2e/dark-theme-explore-search.spec.ts-snapshots/`へ追加した。あわせて`e2e/dark-theme-shared-layout.spec.ts`・`e2e/mypage-year-filter.spec.ts`・`e2e/dark-theme-home-feed.spec.ts`（計15ケース）とプロジェクト全体のvitest（97ファイル704件）を実行しデグレがないことを確認した。Linux baselineとCI実行証跡は別途取得する。

**Terraによる追加是正（2026-08-03）**: `SearchClient.tsx`のユーザータブで、アバター未設定時の頭文字フォールバックとメタ情報（投稿数・フォロワー数）のコントラストを直接検証するE2Eケースを追加したところ、以下2件のコントラスト不足が判明し是正した。
- 頭文字フォールバック: `bg-zinc-200 dark:bg-zinc-700`の上の文字色が`text-zinc-500 dark:text-zinc-400`では不足していたため、`text-zinc-700 dark:text-zinc-300`へ変更
- メタ情報（投稿数・フォロワー数）: `text-zinc-400`（dark:未対応）ではライトモードでも不足していたため、`text-zinc-600 dark:text-zinc-400`へ変更

あわせてE2E側の前提も2点修正した: 人気順ランキング行のセレクタ（`a[href^="/posts/"]`）がサイドバー等の他リンクと衝突しないよう`main`配下に限定、ユーザータブのhover状態検証でも検索欄に検索対象ユーザーのニックネームを入力してから対象を特定するよう統一（ライト/ダーク比較テストで先に導入した方式と揃える）。

**再検証**: 上記是正を反映した状態で、ベースライン12枚を再取得したうえで、更新なしの通常実行（`--update-snapshots`なし）で改めて20/20ケース成功を確認した。

**PR-7e（3節の対応表のグループF・G）は完了**: 対象5ファイルについて`grep -r "bg-white" src`は0件（既存の意図的なオーバーレイ`bg-white/NN`と、`ImageCarousel.tsx`のカルーセルドット・`ReportSummary.tsx`の画像上バッジ等、写真・グラデーション上で意図的に固定色を使う箇所を除く）。5節の完了条件（固定色・border・hover/focus・エラー状態・無効化状態がトークンベースまたは`dark:`対応済み、各対象ページの自動コントラスト検証がグリーン、ライト/ダーク比較スクリーンショットの証跡）を対象5ファイルについて満たした。

**GATE-40（DR-02）全体は未完了**: 上記のとおりPR-7eのLinux baseline・CI実行証跡が未取得であることに加え、Terraによる全src静的監査（2026-08-03）で、当初のグループA〜Gの棚卸しに含まれていなかった以下の画面・共有部品にダーク未対応が見つかった。これらはPR-7f（下記10.3.7節）でコード対応済みだが、E2E・スクリーンショット証跡は本節時点でまだない。

| 優先度 | 画面・部品 | 静的監査時点（2026-08-03）の状態 | PR-7f時点の状態 |
|---|---|---|---|
| 高 | 通知ページ / `NotificationList` | 見出し・補足・通知行の border/hover/未読背景・本文・日時・コメント抜粋・空/読込状態が固定色 | コード対応済み。E2E証跡あり（空状態のみ。未読背景・hover・読込状態は未検証） |
| 高 | プロフィールページ（`(public)/users/[id]/page.tsx`） | 名前、自己紹介、件数、モバイル集計カード、編集導線、タブ、見出し、ランクバッジが固定色 | コード対応済み。本人基本表示と、自分宛コメント／フォロワー一覧のE2E証跡あり（フォロー中一覧は未検証） |
| 高 | プロフィールの一覧部品 | `CommentLoadMoreList`・`UserLoadMoreList`・`FollowButton`・共通`BackButton`に背景・文字・border・hoverの未対応あり | コード対応済み。FollowButton操作、自分宛コメント、フォロワー一覧のE2E証跡あり（追加読み込み・フォロー中一覧は未検証） |
| 高 | 投稿詳細ページ（`(public)/posts/[id]/page.tsx`） | タイトル、投稿者、本文、編集導線、費用内訳、関連スポット見出し、カテゴリfallback、地図枠が固定色 | コード対応済み。E2E証跡あり（見出しと地図枠のみ。本文・費用内訳・関連スポットは未検証） |
| 高 | 投稿詳細の操作・コメント | `LikeButton`・`WishlistButton`・`VisitedButton`・`CommentSection`・共通`BackButton`に通常/hover/disabledを含む未対応あり | コード対応済み。Like/Wishlist/Visited操作、コメント投稿・表示のE2E証跡あり（削除・追加読み込みは未検証） |
| 中 | 地図 | `MapView`/`LocationPicker`の「地図拡大表示」ボタンと地図borderは未対応（PR-7eで閉じるボタンのみ是正済み） | コード対応済み。「地図拡大表示」ボタン自体のE2EはPR-7e側（`dark-theme-explore-search.spec.ts`）で対応済み |
| 中 | プラン地図 | `PlanMapViewWrapper`のloading skeletonと`PlanMapView`の地図borderが未対応 | コード対応済み。E2E証跡あり |
| 中 | エラー・404 | `error.tsx`・`global-error.tsx`・`not-found.tsx`・各route groupの`error.tsx`計6画面で、アイコン背景・見出し・本文が固定ライト配色 | コード対応済み。404のみE2E証跡あり。`error.tsx`系5画面（エラー境界を明示的に発生させる必要がある）は未検証 |
| 要確認 | `ReportSummary` | 画像上の`bg-white/90`バッジ2種（未対応）と、グラデーション上の`text-zinc-800`（背景ごとの実コントラスト確認が必要） | バッジ2種と旅行レポートの集計カードのE2E証跡あり。グラデーション文字はPR-7dのエリア別バブルチャートと同じ理由で対象外と判断（下記10.3.7節） |

GATE-40（DR-02）全体は未完了である。PR-7e/7fのLinux baselineとCI実行証跡は取得したが、PR #45 のCI run 30906976214（2026-08-04）はE2E 95件成功・20件失敗・1件flakyとなった。PR-7fの16件はLinux baseline未登録、PR-7e由来の4件は既存baselineとの差分であり、CI実画像を確認してbaselineを追加・更新するまで完了としない。`error.tsx`系5画面は10.3.8節のコンポーネントテストで実証済みである。

### 10.3.7 PR-7f 残件（通知・プロフィール・投稿詳細・地図・エラー画面）の実装（2026-08-03〜04）

10.3.6節の静的監査で判明した残件について、`fix/gate-40f-dark-theme-remaining-screens`ブランチ（`fix/gate-40e-dark-theme-explore-search`から分岐）で対応した。対象は10.3.6節の表のとおり：通知ページ・`NotificationList`、プロフィールページ本体・`CommentLoadMoreList`・`UserLoadMoreList`・`FollowButton`・共通`BackButton`、投稿詳細ページ本体・`LikeButton`・`WishlistButton`・`VisitedButton`・`CommentSection`、`MapView`/`LocationPicker`の「地図拡大表示」ボタンと地図border、`PlanMapViewWrapper`/`PlanMapView`、`error.tsx`系6画面、`ReportSummary`の画像上バッジ2種。

**実装中に発見した既存の隠れたコントラスト不足3件**（ダークモード固有ではなく、ライト/ダーク共通で以前から存在した不足。10.3.6節のPR-7eで発見した`bg-[#16a34a]`系・`bg-blue-400`と同種のパターン）:
- `CommentSection.tsx`の「コメントを投稿」ボタン: `bg-green-600`（白文字で約3.3:1）→`bg-green-700`（約5.0:1）
- `not-found.tsx`の「トップページへ戻る」ボタン: `bg-green-500`（白文字で約2.3:1）→`bg-green-700`
- `error.tsx`系4画面の「再試行する」ボタン: `bg-[#3B88C3]`（白文字で約3.8:1）→`bg-[#2f6ea3]`（約5.4:1）

**Saki さんの指示によるsrc全体の追加監査**（2026-08-03）で、上記残件表に含まれていなかった以下2件も発見・是正した:
- `FollowFeed.tsx`のカテゴリ色フォールバック（`bg-slate-100 text-slate-600`、他ファイルと同型の見落とし）
- `posts/[id]/page.tsx`のエリア表示テキスト（`text-[#16a34a]`が`dark:text-[#4ade80]`と未ペアのまま残っていた）

**`ReportSummary`のグラデーション文字は対象外と判断**: 月別ヒートマップのセル文字色（`t <= 0.75`のとき`text-zinc-800`）は、背景がページのライト/ダーク設定ではなく投稿数に応じた独自のピンク系グラデーション（`HEAT_LIGHT`〜`HEAT_DARK`）であり、ページのテーマ設定と無関係に自己完結した配色である。実際にコントラスト比を計算したところ、`text-zinc-800`が使われる範囲（`t<=0.75`）の最も濃い境界色でも約5.8:1を確保できることを確認した。これはPR-7dで「エリア別バブルチャート（常に色付きグラデーション背景の上に白文字を乗せる方式で、ページ背景とのコントラスト計算に意味がないため意図的に対象外）」とした判断と同じ理由により、本PRでも対象外とする。

**E2E・スクリーンショット証跡（2026-08-04）**: `e2e/dark-theme-remaining-screens.spec.ts`で、通知の空状態、本人プロフィール、投稿詳細と地図枠、プラン地図、404画面に加え、投稿詳細のLike/Wishlist/Visited操作・コメント投稿、プロフィールの自分宛コメント／フォロワー一覧、旅行レポートの集計カードとバッジをライト／ダークで検証する。操作系は所有者・閲覧者を一度だけ登録して保存したログイン済みstorage stateを使い回すため、登録・UIログイン・レート制限の揺れを各テストから除外した。地図タイルとマーカー画像は外部ネットワーク依存を排除するため固定PNGでモックし、投稿詳細スクリーンショットでは地図タイル本体をmaskする。Windowsローカルの`--update-snapshots`実行で16枚のbaselineを生成後、更新なしの通常実行で16/16成功を確認した。PR #45 のCI run 30906976214では、Linux baseline未登録のためこの16ケースが失敗した。CI artifactの実画像から対応する`*-e2e-linux.png`を追加した。あわせてPR-7eのユーザー検索light/darkと地図通常dark・地図モーダルdarkの4ケースも既存baselineとの差分で失敗したため、CI実画像を確認してbaselineを更新した。修正後のCI再実行は未確認であり、GATE-40完了の根拠にはしない。

**教訓（ログイン遷移の不安定化と原因）**: 初版の`signInAndCreatePost`ヘルパーは`page.goto("/login", { waitUntil: "domcontentloaded" })`を使っており、最初の2ケースで断続的にログイン後の`/`遷移待ちがタイムアウトした。当初はマシンリソース・テスト用DBの競合（他specとの同時実行）を主因と疑ったが、実際の原因はNext.jsのハイドレーション完了前に`domcontentloaded`が制御を返し、直後の`click()`がハイドレーション未完了でno-opになる既知のパターンだった。既存spec（`dark-theme-explore-search.spec.ts`等）と同じ`page.goto("/login")`（既定の`"load"`待機）へ戻したところ再現しなくなった。以後、新規specのログイン処理は既存specの実装をそのまま踏襲し、`waitUntil`を独自に指定しないこと。

**回帰確認**: `pnpm tsc --noEmit`・対象ファイルの`eslint`・プロジェクト全体のvitest（97ファイル704件）はすべて成功。vitestは実行時に一部の repository/integration テストがテスト用DBのスキーマ不整合で失敗する事象が発生したが、`pnpm prisma:migrate:test`の再実行で解消しており、本PRのコード変更（Tailwindクラス文字列の変更のみ、ロジック変更なし）とは無関係の環境要因だったことを確認済み。

### 10.3.8 PR-7f追加是正・フロントエンド例外UXの統一（2026-08-03）

Terraによるレビューで、PR-7fに関連して以下3点の懸念（ダークテーマの既知の穴2件）と、ダークテーマとは別の課題（フロントエンドの例外UX不統一）が指摘された。いずれも対応した。

**Error Boundaryのコンポーネントテスト（2026-08-04）**: `src/app/__tests__/error-boundaries.test.tsx`でroot・`(app)`・`(auth)`・`(public)`の4つの`error.tsx`を直接描画し、ダーク背景・本文のダーク文字色・再試行ボタンと`reset()`呼出しを検証する。既存の`global-error.test.tsx`も拡張し、`theme` Cookieの`dark`／`light`／`system`／不正値ごとの`data-theme`反映、ダーク背景と再試行操作を検証する。合計10件が成功した。これは見た目・テーマ反映の契約を対象とするものであり、例外時にどのBoundaryが選ばれるかはNext.jsの`app`ディレクトリ配置規約に依存するため、テスト専用の例外経路は追加しない。

**ダークテーマの既知の穴の是正**:
- `global-error.tsx`はルートレイアウトを経由しない独自の`<html>`のため、RootLayoutがサーバー側で設定する`data-theme`属性が付かず、アプリ内で手動選択したダークテーマ（Cookie保存）がこの画面だけ反映されない欠陥があった。`applyTheme`・`parseThemeCookie`（既存のテーマ機構のユーティリティ）を使い、クライアント側の`useEffect`でCookieを読み直して`data-theme`を設定するよう修正した。
- `ReportSummary`の月別ヒートマップ（グラデーション背景上の`text-zinc-800`）と`PlanMapView`（プラン詳細の地図）を、実際にブラウザ（`pnpm dev`、既存の開発用DB）でダークモード表示を目視確認した。いずれも問題なく、ヒートマップは淡い背景で濃い文字・濃い背景で白文字が正しく出し分けられており、地図はborder・ローディングスケルトンが正しくダーク対応していることを確認した（タイル画像自体は他の地図アプリ同様、テーマに関わらず実際の地図の見た目のままで問題ない）。

**フロントエンド例外UXの統一**: `ExploreFeed`の再フェッチ失敗・通知バッジ件数取得失敗・`SearchClient`の検索失敗等で、エラー表示・再試行導線・JSON形式不正時の扱いがコンポーネントごとにばらついていた（一部は`showToast`で表示、一部は`!res.ok`時に既定値へ静かにフォールバックし利用者に何も伝えない）。以下の共通基盤を追加し、`useQuery`/`useInfiniteQuery`のqueryFnをこの基盤に統一した。

- `src/lib/fetchJson.ts`: fetch失敗・非2xx・JSON形式不正のいずれでも同じ利用者向けメッセージでthrowする共通ヘルパーを新設。
- `QueryProvider.tsx`: `QueryCache({ onError })`でqueryFnが投げたエラーを一箇所に集約し、`error.message`をトーストとして表示するグローバルハンドラーを追加。`meta.silentError: true`を指定したクエリではトーストを抑制できるようにした（TanStack Query v5の`Register`型で`meta`を型安全にした）。
- `toast-context.tsx`: `QueryClient`はReactツリー外（`QueryProvider`のuseState初期化子）で生成されるため`useToast()`を呼べない。マウント中の`ToastProvider`が自身の`showToast`をモジュール変数へ登録し、`showGlobalToast`経由でトースト表示できるようにした。
- 個別修正: `ExploreFeed.tsx`（既存の日本語メッセージのままfetchJson化、トースト抑制なし＝背景更新失敗時に初めて利用者へ通知される）、`SearchClient.tsx`（3箇所の英語エラーメッセージ`"failed to search posts"`等を日本語化）、`Sidebar.tsx`の`fetchUnreadCount`（`!res.ok`時に0へ静かにフォールバックしていたのを、throw+`meta.silentError`へ変更。3分おきのポーリングでトーストが繰り返し出ないようにしつつ、失敗時に直前の正しい件数を0へ誤って戻さないようにした）、`LocationPicker.tsx`の`reverseGeocode`（既存の座標フォールバック表示という正常な設計のため`meta.silentError`でトースト対象外にした）。

**検証**: 新規`src/components/providers/__tests__/QueryProvider.test.tsx`（3件）で、①エラーメッセージがトーストとして表示されること、②`meta.silentError`指定時にトーストが出ないこと、③`ToastProvider`未マウント時でも例外にならないことを確認（単体実行で3/3成功）。プロジェクト全体のvitest（98ファイル707件）は、他specとの兼ね合いなく直後に単独実行した回では全件成功したが、Terraの並行E2E作業と同じテスト用DB（`mysql-test`、ポート3307）へ同時アクセスしていた別の実行では通知関連のrepositoryテスト4件が失敗した（`createTestUser`を使うDB書き込み系テストで、本PRのコード変更〔Tailwindクラス・fetch呼び出し方法の変更のみ〕とは無関係と考えられる。テストDBの共有起因の可能性が高く、Terraの作業完了後に単独実行で再確認する）。あわせて`pnpm dev`実行中の実ブラウザでホーム・通知・プロフィール各画面のコンソールエラーが0件であることを確認した。

**未実施**: 実際の通信断・非2xxレスポンスをブラウザで強制発生させてトースト表示を目視確認する手動テスト（`refetchOnWindowFocus`の内部フォーカス判定がPlaywright操作の同期イベントでは発火せず、この場では再現できなかった。単体テストでの検証で代替）。

### 10.3.9 フォーカスリングの非文字コントラスト不足（WCAG 1.4.11）の是正（2026-08-04）

GATE-40（PR-7a〜7f）全体の棚卸しの中で、ダークテーマ対応とは別軸の既存不具合として、フォーカスリング色がWCAG 1.4.11（非テキストのUIコンポーネントは背景に対し3:1以上）を満たしていないことが手計算で判明した。ダークテーマ対応のスコープ外だが、ユーザーの判断で本セッション内で是正した。

**発見の経緯**: `focus:ring-green-500`（`bg-white`相当の背景に対して）を`getContrastRatio()`（既存のE2Eコントラスト計測ヘルパー）で計測しようとしたところ、このヘルパーはテキスト色と実効背景色の輝度比のみを計測する設計であり、フォーカスリングのような非テキストUIのコントラストは対象外という盲点に気づいた。そのため手計算（sRGB→線形変換→相対輝度→比率、既存の計算式を流用）で検証した。

- `focus:ring-green-500`（`#22c55e`）と白背景：約2.28:1（3:1未達）。ダーク背景相手では約7.79:1で問題なし。ライトモード限定の既存不具合。
- `focus:ring-[#16a34a]/20`・`/30`（半透明の緑リング、ログイン・会員登録・検索・設定系フォームで使用）は、30%不透明度で白へ合成した場合約1.41:1しかなく、`focus:ring-green-500`よりも深刻。不透明度を85%まで上げても約2.76:1にしかならず、半透明のままでは3:1に到達できないことを確認した。

**対応**: 上記2系統・計11ファイルの`focus:ring-*`を、いずれも`focus:ring-green-700`（`#15803d`、本シリーズで既に採用している是正色）へ統一した。半透明系は不透明度サフィックスを外し完全な単色リングにした（不透明度を上げるだけでは3:1に届かないため）。`focus:ring-green-700`は白背景で約5.01:1、ダーク背景（`zinc-900`相当）で約3.54:1と、両テーマで3:1を上回る。

対象ファイル（`focus:ring-green-500`→`focus:ring-green-700`）:
`src/components/mypage/YearFilterBar.tsx`、`src/components/plans/PlanForm.tsx`、`src/components/plans/SpotPicker.tsx`、`src/components/posts/CommentSection.tsx`、`src/components/posts/DateField.tsx`、`src/components/posts/PostForm.tsx`

対象ファイル（`focus:ring-[#16a34a]/20`または`/30`→`focus:ring-green-700`）:
`src/app/(auth)/login/page.tsx`、`src/app/(auth)/signup/page.tsx`、`src/components/search/SearchClient.tsx`、`src/components/settings/AccountForms.tsx`、`src/components/settings/SettingsForm.tsx`

**検証**: `pnpm tsc --noEmit`・変更11ファイルへの`eslint`はいずれも警告・エラーなし。`pnpm vitest run`（全715件）は704件成功・11件失敗だったが、失敗は`post.repository.test.ts`（外部キー制約違反・件数不一致）のみで、`git diff`上で本修正がTailwindクラス文字列以外（`src/lib/repositories`配下等）に一切触れていないことを確認済みであり、10.3.8で既に記録した「テスト用DB共有起因の可能性が高い」既知の環境要因と同種の事象と判断した（本修正によるリグレッションではない）。ブラウザでの目視確認・E2Eの新規追加は行っていない（クラス名変更のみで、既存のダークテーマE2Eスイートのスクリーンショットへの影響もない想定だが、次回Terraのローカル実行時に既存スイートが崩れていないか確認を依頼する）。

### 10.3.6.1 PR-7e、PR作成後のCI（GitHub Actions）実行で判明した3件の修正（2026-08-04）

PR #44作成後の初回CI実行で`e2e`ジョブが失敗した。Dockerで実CIに近いLinux環境（`mcr.microsoft.com/playwright:v1.61.1-jammy`、テスト用DBはホストの`tripdiary-db-test`コンテナへ`host.docker.internal`経由で接続）を再現し、原因を特定・修正した。

- **`search-user-tab-{light,dark}`のLinux baseline未生成**: CIログが`A snapshot doesn't exist at ...-e2e-linux.png, writing actual.`と報告（比較失敗ではなく欠落）。10.3.6節の他のケースは同時にLinux baselineが追加されていたが、この1ケースだけ生成漏れがあった。Docker上で該当テストのみ`--update-snapshots`を実行し追加した。
- **ロケータの実バグ（`旅スポットタブ: カテゴリチップの...`ケース）**: `element(s) not found`の直接原因は`rankingRow.locator("p.hidden.sm\\:flex")`だが、根本原因は`const rankingRow = page.locator('a[href^="/posts/"]').first()`がサイドバー・モバイルナビの「新規投稿」リンク（`href="/posts/new"`も`^/posts/`に一致）を誤って拾っていたこと（診断用の一時ログでリンク一覧を出力し確認）。同ファイル内の`main a[href^="/users/"]`（ユーザータブ検証、148行目）と同種の既知パターンのため、同じ手法で`main a[href^="/posts/"]`へスコープして修正した。
- **`dark-theme-post-forms.spec.ts`の2件のLinux baseline陳腐化**: `post-form-new-dark`（2712px差）・`post-form-datefield-calendar-dark`（4px差）が、リトライ3回とも同一のピクセル差で失敗していた（タイミング起因のフレークなら差分量が毎回変動するはずのため、決定的なレンダリング差と判断）。このPRはPostForm.tsx・DateField.tsxを一切変更していないため、CIランナー側（`ubuntu-latest`のフォント等）の経時的な変化によるbaseline陳腐化と判断し、Docker上で再生成した。

**対象外とした判断（Opus（上位モデル）による裏付けレビュー、2026-08-04）**: ログイン・新規登録・設定画面（`dark-theme-auth-settings.spec.ts`）もDocker検証環境ではわずかな差分が見られたが、実際のGitHub Actions CIの初回失敗報告にはこれらが含まれておらず（`settings-account-dark`のみ「flaky」＝1回目失敗・リトライで成功、他は初回から成功）、ローカルのDocker近似環境（公式Playwrightイメージ）と実CIの`ubuntu-latest`+`playwright install-deps`は完全には同一でないため、実CIが問題視していないbaselineをローカルの見え方だけで書き換えるのは新たな不一致を生むリスクがあると判断し、対象外とした。`git diff`上もこれらの`*-e2e-linux.png`は無変更であることを確認済み。

**残課題（未対応、別途追跡）**: `settings-account-dark`の「flaky」は、実CI環境でも1回目に差分が出ているという事実であり、無視してよい信号ではない。原因はレンダリングの非決定性（アニメーション未完了・フォント読み込み・非同期描画のタイミング）である可能性が高い。対処の本筋は`maxDiffPixels`等の閾値緩和ではなく、スクリーンショット直前の待機条件（`waitForLoadState`・アニメーション無効化・フォント読み込み待ちの明示化）の見直しである。PR-7eのスコープ外のため今回は対応せず、別issue/TODOとして切り出す。

### 10.3.6.2 baseline再生成後もCIで再発した微小差分と、許容誤差設定による解決（2026-08-04）

10.3.6.1節の3件の修正をpushした後の再実行でも、regenerateした3件（`search-user-tab-{light,dark}`・`post-form-new-dark`・`post-form-datefield-calendar-dark`）が依然として失敗した。差分量は大幅に縮小していた（`post-form-new-dark`は2712px→122px、`post-form-datefield-calendar-dark`は4px→8px）ものの、ゼロにはならなかった。

**原因**: `playwright.config.ts`の`expect`にはこれまで`toHaveScreenshot`用の許容誤差が一切設定されておらず、既定では差分1ピクセルでも失敗する。ローカルのDocker検証環境（公式Playwrightイメージ`mcr.microsoft.com/playwright`）と実際のCIランナー（`ubuntu-latest`＋`playwright install-deps chromium`）はChromium本体のバージョンは同一でも、OS側のフォントパッケージ構成までは完全に一致しないため、baselineをどちらで生成してもアンチエイリアシングレベルの数十〜百数十px程度の差は原理的に残り得る（Opus（上位モデルによる裏付けレビュー）でも同じ結論）。

**対応**: `playwright.config.ts`の`expect.toHaveScreenshot`に`maxDiffPixelRatio: 0.001`（0.1%）を設定した。実際に検出された差分はいずれも全画面比較で最大0.01%程度（例: `post-form-new-dark`は1280×900中122px≒0.0106%）のため、10倍の余裕を確保しつつ、実際の配色バグ（本節の従来の発見事例はいずれも数百〜数千px規模）を隠さない範囲に留めた。

**settings-account-darkのflakyとの違い**: 上記「残課題」で述べた`settings-account-dark`の1回目失敗はタイミング起因（アニメーション未完了等）の非決定性であり、今回の許容誤差設定はこれを覆い隠す対応ではない。両者は原因の異なる別問題であり、タイミング起因のフレークは引き続き別issue/TODOとして追跡する。

### 10.3.7.1 PR-7f、mainへのrebase後のマージコンフリクト解消とCI（PR #45）実行で判明した問題の修正（2026-08-04）

`fix/gate-40f-dark-theme-remaining-screens`ブランチ（PR #45）を作成した時点で、`fix/gate-40e-dark-theme-explore-search`（PR-7e）が既にPR #44としてmainへマージ済みだったため、PR #45にマージコンフリクトが生じた。`git rebase origin/main`でmain（PR-7eマージ済み）の上に本ブランチの1コミットを載せ替え、コンフリクトは`e2e/dark-theme-explore-search.spec.ts`の1箇所（10.3.6.1節の修正に付随するコメントの重複）のみで、main側のより詳細な説明コメントを採用して解消した。

**CI（PR #45初回実行）の結果**: `95 passed / 20 failed / 1 flaky / 1 skipped`。失敗は次の2種類に分解できた。

1. **PR-7fで追加した16ケースのLinux baseline未生成**（`e2e/dark-theme-remaining-screens.spec.ts-snapshots/*-e2e-linux.png`が1枚も存在しなかった。10.3.7節のWindowsローカル実行のみで、Linux baseline取得は当時「未取得」と明記していた既知の残課題）。
2. **main側（PR-7e、既にマージ済み）由来の4件が同じPRの実行で再度失敗**: `search-user-tab-{light,dark}`（約3,852px／6,743px、各約1%）、`map-view-normal-dark`（4px）、`map-view-modal-dark`（約1,538px、約1%）。`settings-account-dark`は初回失敗・リトライ成功のflaky扱いで、今回のジョブ失敗の直接原因ではなかった（10.3.6.1節の「残課題」で追跡中の既知のflakyと同一）。

**Docker環境での再現**: 10.3.6.1節と同様にCI相当のLinux環境（`mcr.microsoft.com/playwright:v1.61.1-noble`、`package.json`の`@playwright/test`固定バージョンと一致）で再現した。ただしWindows上のDocker Desktopでは`--network=host`がホストのLAN IPへコンテナのブラウザナビゲーションを迷い込ませる既知の不安定挙動を示したため、既定のbridgeネットワーク＋`--add-host=host.docker.internal:host-gateway`でホストのテスト用DBコンテナ（`tripdiary-db-test`、ポート3307）へ接続する構成に変更した。

**ローカル環境固有の問題（CIには存在しない）**: 上記変更後もログイン後のリダイレクトが`chrome-error://chromewebdata/`で失敗する事象が続いた。トレースを解析した結果、`.env.local`（作業者のスマホ実機LAN確認用に`AUTH_URL="http://192.168.1.6:3000"`を設定したファイル）がコンテナへbind mountされ、`trustHost: true`のNextAuthがこの`AUTH_URL`を使ってログイン後リダイレクト先を構築していたことが原因と判明した。`.env.local`はgit管理外でCIには存在しないため、実CIの失敗原因ではなく本Docker検証環境固有の問題。`-e AUTH_URL="http://localhost:3000"`で明示的に上書きして解消した（Next.jsは`.env.local`より明示的なprocess.env値を優先するため、他の`-e`指定〔`DATABASE_URL`等〕と同じ仕組みで上書きできた）。

**16件のLinux baseline生成**: 上記解消後、`pnpm playwright test e2e/dark-theme-remaining-screens.spec.ts --update-snapshots`で16/16成功・baseline生成を確認した。

**4件の個別調査**:
- `search-user-tab-{light,dark}`・`map-view-normal-dark`: 実画像とbaselineをそれぞれ目視比較した結果、視覚的な差異は認められず、フォントレンダリングのアンチエイリアシングレベルの差と判断した（10.3.6.2節と同種の環境ドリフト）。差分比率（約1%）が現行の`maxDiffPixelRatio: 0.001`を上回っていたため、baselineを現在の実描画に合わせて再生成した。
- `map-view-modal-dark`: 目視すると、投稿詳細ページの見出し（`Date.now()`を含み実行のたびに変わるタイトル文字列）が半透明オーバーレイ越しに透けて写り込んでいた。これはフォントレンダリングの差ではなく**テストのバグ**で、`mask: [..., page.locator('[data-testid="post-card"]')]`の`[data-testid="post-card"]`が`src/components/posts/PostCard.tsx`（一覧・フィード用カード）にしか存在せず、投稿詳細ページ（`src/app/(public)/posts/[id]/page.tsx`）自体の`<h1>`見出しにはそもそも一致していなかったため、と最初は判断した。マスク対象を`page.locator("h1")`だけに置き換えていったんbaselineを再生成したところ、単体実行では成功したが、複数spec連続実行時に別の差分（下部の「関連スポット」欄）で再度失敗した。調査の結果、投稿詳細ページ自体が`関連スポット`セクションで同一locationの他投稿を`PostCard`（`data-testid="post-card"`）として表示しており、`[data-testid="post-card"]`のマスクは元々この関連投稿カード群を指すもので、削除すべきではなく`h1`と併用すべきだったと判明した。`mask: [..., page.locator("h1"), page.locator('[data-testid="post-card"]')]`へ修正した。
- **DBへのテスト投稿蓄積による見かけ上の再発**: 上記修正後も一度17,097px規模の大きな差分で再現し、当初は環境ドリフトの再発を疑ったが、原因は本節の検証作業中に`--update-snapshots`と通常実行を何度も同一のテスト用DB（`tripdiary-db-test`）に対して繰り返したため、`map-view-modal-dark`が撮影する「関連スポット」欄の対象投稿（location: 東京都）が実行のたびに積み増され、baseline撮影時と検証時でカード枚数・折り返し行数が変わっていたことだった。実CIは`mysql:8.0`サービスコンテナを毎回新規作成するため本番では発生しない、ローカルDocker検証作業に固有の後方互換性の問題。テスト用DBコンテナを`docker compose rm -f mysql-test && docker compose up -d --wait mysql-test`で再作成し、`prisma migrate deploy`からクリーンにやり直したところ、4件とも安定して成功した。

**回帰確認**: `pnpm typecheck`・`pnpm lint`・`pnpm test`（99ファイル715件）は成功。クリーンなテスト用DBに対し、上記4件を`e2e/dark-theme-explore-search.spec.ts`から`-g`で絞り込んで単体実行し4/4成功、PR-7fの16件を`e2e/dark-theme-remaining-screens.spec.ts`単体実行で16/16成功を確認した。**未実施**: 全E2Eファイルを1回のPlaywright実行でまとめて流す完全なフルスイート再検証（10.4節に記録済みの共有vCPUランナー起因フレーク・本節のテストDB蓄積問題と同様、ローカルDocker環境の1ワーカー逐次実行では大量の累積テストデータによる非決定的なタイムアウト・撮影差分が発生しやすく、個別ファイル単位の検証より signal が弱いと判断したため）。最終確認は実CI（`.github/workflows/ci.yml`のe2eジョブ、毎回まっさらな`mysql:8.0`サービスコンテナ）の実行結果に委ねる。

### 10.4 bcryptjsのCPU競合によるCI限定フレークと決定的手順への書き換え（2026-07-21、Phase 5-D2）

> 以下はPhase 5-D2時点（`bcryptjs`使用時）の調査記録。ネイティブ実装への置き換え自体は本節では見送られたが、本番のCPU競合対策として後続のPhase 6「6-A2」で`@node-rs/bcrypt`への置き換えが完了している（`実装計画書.md`参照）。

CI導入後、`account-settings.spec.ts`（パスワード/メール変更）が**GitHub Actionsランナー上でのみ**断続的に失敗した。ローカルではCIと同じワーカー数（1・2）で全33件を繰り返しても一度も再現せず、共有vCPUランナー固有のCPU制約が根本要因と判断した。CPU競合の最大要因は`bcryptjs`（純JS実装でメインスレッドをブロック）のコスト12でのログイン照合で、多くのspecが`beforeEach`でUIログインするため負荷が集中していた。

**「ネイティブbcryptに変えれば直るか」の切り分け**: まず`BCRYPT_COST=4`（ほぼゼロコスト）で複数回実行した。コストをほぼゼロにしても残るフレークはbcryptのCPU負荷が原因ではない、という論理的切り分けである。結果、実行時間は短縮したがフレークはゼロにならず、ネイティブ実装への置き換えは見送った（本番のCPU競合対策は別問題として6-A2に差し戻し）。

**対処（3点、並列度は落とさない）**:

- `BCRYPT_COST`環境変数化（`src/lib/password.ts`）。CIの`e2e`ジョブのみ`BCRYPT_COST=4`でログイン照合のCPUスパイクを低減（本番は既定12のまま）
- `playwright.config.ts`に`retries: process.env.CI ? 2 : 0`と、`expect`/`action`/`navigation`のグローバルタイムアウト延長（10s/15s/30s）を追加。`workers`は下げず並列度を維持し、稀な非決定的フレークは失敗テストのみ再実行して吸収する（`report.spec.ts`等はこれで回復した）
- `account-settings.spec.ts`のログアウト検証を、`signOut()`後の着地先（`/login`と`/`）の揺れに依存しない決定的な手順へ書き換え（settings/accountからの離脱確認→`clearCookies()`→`/login`でフォーム表示を待ってからログイン）。`signOut`自体はアプリの正常動作（`callbackUrl`はNextAuth v5でも`redirectTo`として受理されることを実装で確認済み）であり、実装バグではなく遷移タイミングへの依存を排除した対応

**結果**: PR #20マージ後のmain向けCIで`e2e`を含む4ジョブが`success`。e2eの必須化判断はPhase 5-C-2で確定した（11節参照。`continue-on-error`は削除しTimeLine方式に変更、ブランチ保護の必須チェックには含めない）。

**教訓**: ①CI限定・ローカル非再現のフレークは、コード上の決定的バグよりランナーのリソース制約を疑う。②`workers`削減（直列化）は確実だが遅くなるため、まず`retries`＋タイムアウト＋CPU源（bcryptコスト）の低減で、並列度を保ったまま解消できないか試す。③タイミング依存の検証は、アプリの遷移結果を待つより「明示的に状態を作ってから検証」する方が安定する。

---

## 11. CI実行フロー（Phase 5-C-1で導入済み）

`.github/workflows/ci.yml`（`main`へのPR・push時に起動）で以下を並列実行する:

1. `lint`（必須）: `pnpm lint`
2. `typecheck`（必須）: `pnpm typecheck`（`tsc --noEmit`）
3. `build`（必須、6-A2で新設）: `pnpm install --frozen-lockfile` → `pnpm build`（Next.jsのビルド可否を検証。6-A2で`@node-rs/bcrypt`〔napi-rsのネイティブアドオン〕を導入したため、型チェックだけでは検出できないバンドル時の不整合を検知する目的で追加）
4. `test`（必須）: `mysql:8.0`サービスコンテナ（ポート3307、healthcheck付き）を起動し、`DATABASE_URL`/`AUTH_SECRET`をworkflowレベルの`env:`で渡した上で`prisma migrate deploy`を直接呼び出し（`.env.test`はCIに存在しないため`pnpm prisma:migrate:test`の`dotenv-cli`は使わない）→`pnpm test:coverage`。カバレッジ閾値（9節）を下回るとこのジョブが失敗する（Phase 5-C-2で確定）
5. `e2e`（**必須ステータスチェックには含めないが、`continue-on-error`は付けない**）: Playwrightのブラウザキャッシュ（`~/.cache/ms-playwright`、`pnpm-lock.yaml`をキーに保存）を復元し、`playwright install-deps chromium`（OS依存ライブラリ）は毎回実行、ブラウザ本体の取得（`playwright install chromium`）はキャッシュ未取得時だけ実行してから、`pnpm playwright test --project=e2e`を実行する。GitHub Actionsのランナーは毎回新規VMのため、`libnss3`等のOS依存ライブラリは`~/.cache/ms-playwright`に含まれず`actions/cache`では復元できない。ブラウザ本体のキャッシュがヒットした回でもOS依存ライブラリの導入だけは省略しない（省略するとChromium起動時に共有ライブラリ不足で失敗するため）。これによりPlaywright更新時は新しいChromiumを取得し、それ以外のCIではブラウザ本体の再ダウンロードのみ省略する（`playwright.config.ts`の`webServer`が`pnpm build && pnpm start`で本番ビルドを起動する）。既存E2Eが高負荷時のタイムアウトで不安定になった経緯（10節）があり、GitHub Actionsランナーはローカルより低速なため、マージのブロックはしない。Phase 5-D2でCPU競合起因のフレークに対し`BCRYPT_COST=4`（このジョブのみ）・`retries`・グローバルタイムアウト延長・`account-settings.spec.ts`の決定的化を施し（10.3節）、直近のmain向けCIでは2回連続で`success`（2026-07-21・07-22）。ただしそれ以前は4回連続で`failure`だったため、「安定した」と断定するには実績がまだ薄いと判断し、**TimeLine方式**（姉妹プロジェクトの運用に合わせ、必須化はしないが失敗時は素直に赤く表示する）を採用（Phase 5-C-2、`continue-on-error: true`は削除）。`continue-on-error: true`のままだと失敗がGitHub UI上で握り潰され、CI安定性の実績をワークフロー単位のステータスだけでは追えなくなる（今回の判断時、ジョブ単位の結果を`gh api`で個別に掘り返す必要があった）ため不採用とした

`ENABLE_TEST_ENDPOINTS`/`TEST_CLEANUP_SECRET`/`DISABLE_RATE_LIMIT_FOR_TESTS`は`e2e`ジョブ単体の`env:`にのみ設定する（Phase 5-D、2026-07-20修正）。当初はworkflowレベルの`env:`（全ジョブに波及）に置いていたが、Phase 5-Dで`src/lib/rate-limit.ts`の`checkRateLimit`にも同フラグでのバイパス（下記）を追加したところ、`test`ジョブ（vitest）のレート制限ユニットテストまで無効化されてしまう不具合が判明したため、`e2e`ジョブ単体に切り出した。

> ✅ **2026-07-29更新（6-B着手前ゲート GATE-03対応）：** 当初は`ENABLE_TEST_ENDPOINTS`1つで下記2つの挙動を両方制御していたが、本番へ誤設定された場合に両方が同時に成立するリスクがあったため分離した。`checkRateLimit`のバイパスは新設の`DISABLE_RATE_LIMIT_FOR_TESTS`が単独で制御し、`/api/test/cleanup`は`ENABLE_TEST_ENDPOINTS`に加えて`TEST_CLEANUP_SECRET`（リクエストヘッダ`x-test-cleanup-secret`と一致する値）を必須化した。`playwright.config.ts`の`use.extraHTTPHeaders`が全E2Eリクエストにこのヘッダーを自動付与する。

`ENABLE_TEST_ENDPOINTS === "true"`で有効になる挙動:
- `/api/test/cleanup`のガード解除（`NODE_ENV === "production"`ではなくこのフラグで判定。本番環境変数には設定しないこと。**`TEST_CLEANUP_SECRET`との一致も別途必須**）

`DISABLE_RATE_LIMIT_FOR_TESTS === "true"`で有効になる挙動:
- `checkRateLimit`（`src/lib/rate-limit.ts`）自体のバイパス。7つのE2Eスペックファイルが同じテストユーザーで単一の永続プロセス（`next start`、共有インメモリの`Map`）に繰り返しログインするため、ログインのレート制限（メール単位5回/15分）と衝突し誤って`rate_limit`エラーになる不具合がPhase 5-Dで発覚し、この方式で解消した（実装計画書のPhase 5-D節に詳細）

---

## 12. パフォーマンステスト（Phase 5-B）

### 12.1 目的・スコープ

負荷・レイテンシの実測を行う。対象はSSRページ（`/`・`/posts/[id]`・`/mypage?tab=report`・`/users/[id]`）と主要API（`posts/explore`・`posts/portal`・いいねトグル・コメント投稿/一覧・プランCRUD・`stats`/`stats/years`・`notifications/unread-count`・フォロートグル・ログイン）。k6による負荷試験（Smoke/Load/Stress/Spike）と、Playwrightによるフロントエンド性能計測（Web Vitals・操作応答時間）の2本立て。**CIには一切組み込まず、指示があったときのみ手動実行する**（Vitest/E2Eのような回帰ゲートではなく、必要なタイミングで実測するための道具という位置づけ）。

### 12.2 perf専用DBとレート制限バイパスの分離方針

`docker-compose.yml`の`mysql-perf`（ポート3308、tmpfsの使い捨てDB）と`.env.perf`で完全に分離する。`DISABLE_RATE_LIMIT_FOR_TESTS=true`を設定し`checkRateLimit`（メール単位5回/15分）を意図的にバイパスする——ログイン系シナリオが同一ユーザーで繰り返しログインするため（11節のE2Eと同じ理由）。**2026-07-29更新（GATE-03）**: `.env.perf`は`/api/test/cleanup`を使わないため`ENABLE_TEST_ENDPOINTS`は設定しない（フラグ分離により、perfサーバーが露出させる範囲をレート制限バイパスのみに縮小した）。それでも**perfサーバーはローカル・使い捨てDB限定で起動し、外部に公開しないこと**。

tmpfsの性質上`perf:down`（`docker compose stop`）で中身は失われる。次回実行時は必ず`perf:up` → `perf:migrate` → `perf:seed`からやり直す。

### 12.3 シードデータ（`performance/seed.ts`）

ユーザー60名（`perf_001`〜`perf_060`、共通パスワード）・投稿3,000〜5,000件（偏りのある投稿者分布）・いいね/コメント各1万件（`toggleLike`/`createComment`の実関数を使用し非正規化カウンタとの整合性を保つ）・フォロー300件・通知3,000件・投稿の約8割にプレースホルダ画像を付与する。生成物は`performance/k6/data/{users.csv, sample-post-ids.json}`（gitignore対象）。`pnpm perf:seed`は再実行可能（毎回全テーブルをTRUNCATEしてから再投入）。

### 12.4 k6負荷試験

| 種別 | コマンド | スケジュール | 初回の合否判定 |
|---|---|---|---|
| Smoke | `pnpm perf:smoke` | VU=1、全シナリオ1周 | `http_req_failed: rate<0.01`。**シード後に必ず最初に実行するゲート** |
| Load | `pnpm perf:load` | ramp(0→10VU,3m)→steady(10VU,5m)→rampdown(2m) | `http_req_failed: rate<0.01` |
| Stress | `pnpm perf:stress` | 5→15→30→50VU、各3分 | 判定しない（`passed: null`＝限界点の記録が目的） |
| Spike | `pnpm perf:spike` | 5→40→5（ランプダウン）→5VU定常→60→5（ランプダウン）→5VU定常 | `http_req_failed: rate<0.05` |

各スクリプトは`performance/k6/run.sh`経由で実行され、終了コードが閾値違反時に非0で伝播する（＝`perf:smoke`が本当のゲートとして機能する）。`pnpm perf:report`で集約ダッシュボード（`performance/k6/results/index.html`）を再生成しブラウザで自動的に開く（`K6_NO_OPEN=1`で抑止可）。レイテンシの数値閾値（p95/p99）は初回実測後に確定し、本節および実装計画書に記録する運用とする。

シナリオの区間タグはk6の`scenarios`分割による自動`scenario`タグに委ねている（経過時間からの推定はランピング中に誤タグ付けになるため使わない）。全リクエストに`endpoint`タグも付与し、集約レポートで段階別・エンドポイント別の内訳（p95・エラー率）を確認できる。

### 12.5 Playwright（Web Vitals・操作応答時間）

`pnpm perf:vitals`（`playwright test --project=perf --workers=1`）。`.env.perf`の`PERF=1`により`playwright.config.ts`の`webServer`が無効化され、`perf:build && perf:start`済みのサーバーに直接接続する（未起動なら開発DBに化けず即座に接続エラーで落ちる）。`perf-setup`プロジェクトがUIログインで`storageState`を生成し、`perf`プロジェクトがそれに依存する。`users.csv`は`id,email,password,nickname`の4列であり、setupは先頭の`id`を読み飛ばしてメールアドレスとパスワードを使用する。

- Web Vitals（`*.vitals.spec.ts`）: `home`・`post-detail`（人気投稿）・`mypage-report`の3ページ。閾値はLCP<2500ms・TTFB<800ms・FCP<1800ms、CLSは通常0.1（mypage-reportはスケルトン表示があるため0.25に緩和）。必須メトリクス（ttfb・lcp等）が欠損した場合は黙って合格にせず失敗させる
- 操作応答時間（`*.timing.spec.ts`）: いいねトグル(<300ms)・コメント投稿(<3000ms)・検索(<1000ms)・無限スクロール(<2000ms、2026-08-01にボタンクリックからsentinelのスクロール表示計測へ変更)

結果は`e2e/performance/results/`にJSON保存され（`process.cwd()`依存にしない）、k6の集約ダッシュボードと同じ場所に置くことで1枚で見比べられる。

### 12.6 実行手順

`pnpm dev`停止 → `perf:up` → `perf:migrate` → `perf:seed` → 別ターミナルで`perf:build && perf:start` → `perf:smoke`（必ず最初） → `perf:load`/`perf:stress`/`perf:spike` → `perf:vitals` → サーバー停止 → `perf:down`。

### 12.7 初回実測結果（2026-07-21）

Smoke・Load・Stress・Playwright（Web Vitals・操作応答時間）を実時間で実行した。

**Smoke**: 全チェック100%成功。

**Load**（最大10VU・10分）: 全体エラー率1.15%で初回閾値（`rate<0.01`）を超過。ただし内訳ではSSR/GET系（`home_ssr`・`posts_explore`・`notifications_unread_count`等）は全てエラー率0%で、エラーは書き込み系の2エンドポイントに集中していた：

| エンドポイント | エラー率 | 原因 |
|---|---|---|
| `posts_like_toggle` | 4.5%（11/244件） | `like.repository.ts`の`toggleLike()`がPrisma `P2034`（デッドロック）を未捕捉 |
| `posts_comments_create` | 20.9%（51/244件） | `comment.repository.ts`の`createComment()`が同様に`P2034`を未捕捉 |

いずれも`interactionScenario`が意図的に人気投稿1件（ホットな行）へ書き込みを集中させたことで、同一`Post`行の`likeCount`/`commentCount`更新が競合しデッドロックする実際のバグを検出したもの（テストコードの不具合ではない）。

**Stress**（5→15→30→50VU、各3分）: `passed: null`（設計通り、合否判定なし）。段階が上がるにつれて書き込み系のエラー率が悪化することを確認：

| 段階 | p95 | エラー率（全体） | `posts_like_toggle` | `posts_comments_create` |
|---|---|---|---|---|
| vu5 | 1003ms | 1.34% | | |
| vu15 | 1026ms | 1.87% | | |
| vu30 | 1367ms | 3.25% | | |
| vu50 | 1760ms | 4.42% | 18.15%（全体累計） | 47.69%（全体累計） |

VU数が増えるほど同一投稿への書き込み競合が起きやすくなり、`posts_comments_create`はStress全体では約半数が失敗するまで悪化した。**限界点はSSR/GET系ではなく、ホットな行への書き込み集中（デッドロック）であることが判明**した。

**Spike**（5→40→5→60→5VU）: 全体エラー率3.11%で初回閾値（`rate<0.05`）はクリア（`passed: true`）。個別には同じ書き込み集中バグが継続しており、`posts_like_toggle`17.98%・`posts_comments_create`44.96%——SSR/GET系は引き続き全て0%。急増・回復を2セット繰り返しても回復区間（`recovery1`/`recovery2`）のエラー率が特段悪化・改善せず、書き込み系のボトルネックがVU数に対して概ね一定の割合で発生し続けることを確認した。

**Playwright（Web Vitals・操作応答時間）**: 8件全て成功（Home/投稿詳細/マイページ集計のWeb Vitals3件、いいね/コメント/検索/ページングの操作応答時間4件、perf-setupのログイン1件）。これは`users.csv`へ`id`列を追加する前の実測であり、4列CSV対応後に再実行して確定し直す。

**対応（修正済み）**: `src/lib/prisma-retry.ts`に`withDeadlockRetry()`を新設し、`performance/seed.ts`が採用しているのと同様の`P2034`検知・短いバックオフでのリトライを`toggleLike()`・`createComment()`・`deleteComment()`に適用した。単体テスト（T-111、`prisma-retry.test.ts`）で検証済み。修正後にLoadテストを実時間で再実行し、全エンドポイントでエラー率0.00%（`passed: true`）を確認した。

### 12.8 TimeLineを基準とした網羅性レビューでの追加対応（2026-07-21）

TimeLineの負荷テスト方針を基準としたレビューの結果、以下を追加・修正した：

- **フォロートグルの実装漏れ**: 12.1節に測定対象として記載していた「フォロートグル」が実際にはどのシナリオからも呼ばれていなかった不整合を修正。`followScenario`（`/users/[id]`のSSR閲覧＋フォロートグル）を新設し、Load/Stress/Spikeの加重分散に5%の割合で組み込んだ。対象ユーザーは`users.csv`の`id`列（新規追加）を使い、名簿上「次のユーザー」を決定的に選ぶことで自分自身への誤フォローを避けている
- **Cleanup設計の明記**: TimeLineの明示的DB削除ステップに対し、TripDiaryは`mysql-perf`のtmpfs破棄＋`perf:seed`の全TRUNCATE再投入で代替している旨を`performance/k6/README.md`に明記した
- **`run.sh`のブラウザ自動オープンのハング修正**: Git Bashの`start`コマンドが前面プロセスの終了を待ち続けてスクリプトがハングする不具合を発見し、即座にデタッチされる`explorer.exe`に置き換えた

### 12.9 全修正反映後の最終実測結果（2026-07-21）

`followScenario`追加後、Load/Stress/Spikeを実時間で再実行する過程で、さらに2件の問題を発見・修正した：

1. **`toggleFollow()`のデッドロック未捕捉**（`follow.repository.ts`）: `toggleLike()`/`createComment()`と全く同じ形（同一`User`行の`followerCount`/`followingCount`を`$transaction`内で更新）のバグで、Stress実行で`users_follow_toggle`が2.01%のエラー率を示したことで発覚。同じ`withDeadlockRetry()`でラップして解消
2. **`interactionScenario`のテスト設計上の誤検知**: 「自分の投稿にはいいねできない」という実際の業務ルール（`like.service.ts`の`ForbiddenError`、意図した仕様）により、`mostLikedPostId`の投稿者自身が実行するVUでは`toggleLikeRequest`が403を返すのを、テスト側が「サーバーエラー」として誤ってカウントしていた（`posts_like_toggle`が一時的に7%超のエラー率を示した原因）。`performance/seed.ts`が`sample-post-ids.json`に`mostLikedPostAuthorId`を追加出力し、`interactionScenario`が投稿者自身の場合はいいねをスキップするよう修正

両方の修正後、Load/Stress/Spikeを実時間でフル再実行し、以下を確認した：

| 種別 | 全体エラー率 | `posts_like_toggle` | `posts_comments_create` | `users_follow_toggle` | 判定 |
|---|---|---|---|---|---|
| Load（最大10VU） | 0.016% | 0% | 0.38% | 0% | PASS（閾値`rate<0.01`） |
| Stress（最大50VU） | 0.095% | 0.097% | 1.73%（vu50時点） | 0% | N/A（判定なし、設計通り） |
| Spike（最大60VU） | 0.126% | 0.53% | 1.88% | 0% | PASS（閾値`rate<0.05`） |

修正前（12.7節）と比較して、書き込み系エンドポイントのエラー率は数十%台から1%台以下まで低下した。Stress・Spikeにわずかに残る`posts_comments_create`のエラーは、`withDeadlockRetry()`の最大リトライ回数（3回）を極端な同時書き込み集中下で使い切った場合の残差であり、無限リトライにしない設計（リトライ自体が新たな行ロック競合を長引かせるリスクとのトレードオフ）を踏まえると許容範囲と判断する。全てのSSR/GET系エンドポイントは、Smoke/Load/Stress/Spikeの全区間・全VU数で一貫してエラー率0%を維持した。

### 12.10 計測ツール自体の不具合の修正と真の最終実測結果（2026-07-21）

ダッシュボードの履歴に残るFAIL表示についてユーザーからの確認を受け、結果JSONを精査したところ、**アプリではなく計測ツール側**の不具合を2件発見した：

1. **p99が全レポートで常に0**: k6のデフォルト`summaryTrendStats`は`avg`/`min`/`med`/`max`/`p(90)`/`p(95)`のみで`p(99)`を含まない。そのため`summary.ts`の`val(m["http_req_duration"], "p(99)")`が常に`null`（`?? 0`で0）を返し、かつ`p99Color`のロジック上「0ms＝良好（緑）」に見えてしまっていた（壊れているのに問題なく見える二重の不具合）。Spikeの目的である「recovery区間のp99回復確認」やLoadの「p95/p99は実測後に確定」という設計が、この不具合により実質的に機能していなかった。`config/config.ts`に`SUMMARY_TREND_STATS`（`p(99)`を含む配列）を追加し、全simulationの`options.summaryTrendStats`に反映して解消
2. **`posts_portal`が1リクエストも実行されていなかった**: `getPortalData`（`requests/apiRequests.ts`）が定義され`ENDPOINT_NAMES`にも含まれていたが、どのシナリオからも呼び出されておらず`requests: 0`のまま（`placeholderThresholds`が閾値を生成するため計測できているように見えていた）。TimeLineの「`users.csv`が死んだコードだった」問題（引き継がないと決めていたもの）を、別の形で再生産していた。`feedScenario`に`getPortalData`呼び出しを追加して解消

両方の修正後、Smoke→Load→Stress→Spikeを実時間でクリーンに再実行し、以下を最終結果として確定する：

| 種別 | 全体エラー率 | p95 | p99 | `posts_portal`件数 | 判定 |
|---|---|---|---|---|---|
| Smoke | 0% | - | 310.9ms | 1 | PASS |
| Load（最大10VU） | 0% | 469.9ms | 589.6ms | 796 | PASS（閾値`rate<0.01`） |
| Stress（最大50VU） | 0.12% | 2067.1ms | 2492.5ms | 2263 | N/A（判定なし、設計通り） |
| Spike（最大60VU） | 0.06% | 2170.4ms | 3145.8ms | 1264 | PASS（閾値`rate<0.05`） |

### 12.11 Opusによる追加レビューへの対応（2026-07-22）

12.10の実測値は、以前の計測不具合が直っただけで**アプリ自体の実装は未修正**の状態だった。追加レビューの結果、以下を発見・対応した。

**🔴 実アプリの実バグ：`posts_portal`にLIMIT漏れ（N+1級の全表スキャン）**

`posts_portal`をようやく計測対象に含めたことで、`home_ssr`（Portalページ自体がSSRで同じ関数を呼ぶ）に次いで2番目に重いエンドポイントであることが判明した。原因は`post.repository.ts`の2関数：

- `findLocationCounts()`：エリアごとのサムネイル1件を得るためだけに、全投稿を`images`込みで`orderBy`付き取得していた（本来必要なのはエリア数＝最大48件分の画像1枚ずつ）
- `findTopRatedByCategory()`：カテゴリごとの評価トップ1件を得るためだけに、該当する全投稿をフルペイロード（`POST_SELECT`、author・images・body・costBreakdown込み）で取得していた（本来必要なのはカテゴリ数＝8種程度）

**対応**：初回は全表スキャンを集合サイズ分の取得へ縮小した。その後、現行実装では`findLocationCounts()`を地域集計＋相関副問い合わせによるサムネイル取得の単一SQLへ、`findTopRatedByCategory()`を`ROW_NUMBER()`によるカテゴリ代表IDの一括選出＋必要な投稿だけの取得へ更新した。これにより投稿総数に比例するフルペイロード取得と、分類数に比例するアプリ側クエリ発行の両方を避けている。`post.repository.test.ts`にはカテゴリ代表の選出・除外・評価順を検証するテストを追加。

**効果（Stress、50VU、同一マシン・低負荷時での比較）**：

| エンドポイント | 修正前 p95 | 修正後 p95 | 改善率 |
|---|---|---|---|
| `posts_portal` | 2221.9ms | 598.3ms | -73% |
| `home_ssr`（同じ関数をSSRで呼ぶ） | 2508.4ms | 810.2ms | -68% |
| 全体p95 | 2067.1ms | 610.5ms | -70% |

**🟡 SSRページの本文マーカー検証を追加**

`pageRequests.ts`の4関数（home/post-detail/mypage-report/user-profile）は`status===200`のみでの検証だった。将来ミドルウェアでログインへのリダイレクトが追加された場合、リダイレクト先を200として誤検知するリスクがあるため、各ページの本文に必ず含まれるマーカー文字列（例：ホームは「人気の旅スポット」）の存在も検証するようにした。実際のページからマーカーが取得できることを`curl`で確認済み。

**🟡 集約レポートの可読性改善**

- `byScenario`（段階別）と`byEndpoint`（エンドポイント別）が1つの表に無差別に混在し、区間タグとエンドポイントタグの区別が付かなかった問題を、2つの表に分離
- `byEndpoint`はp95降順（重い順）にソートし、ボトルネックが表の先頭に来るようにした
- 段階別・エンドポイント別テーブルにも、最新カードと同じ閾値ロジック（p95<500=緑/<1000=黄/それ以上=赤）でのセル色分けを追加
- `summary.ts`の`byScenario`/`byEndpoint`抽出にp99を追加（従来はp95のみで、p99バグ修正の効果がこれらの表には反映されていなかった）
- Web Vitals・操作応答時間（`e2e/performance/results/`）を表示するタブを集約ダッシュボードに追加（確定事項1「Web Vitalsを集約ダッシュボードに統合する」がDoDにチェック済みで実装が伴っていなかった不整合を解消）
- JSON抽出をgrep/sed/awkから`jq`ベースに書き換え（複雑な条件分岐・ソートを非構造化テキスト処理で実装するリスクが増したため）。この過程で、**Windows環境の`jq`が出力にCRLFを使うため複数フィールド出力の最終フィールドに`\r`が紛れ込みprintfが落ちる不具合**を新たに発見・修正（`jqr`ラッパー関数で`tr -d '\r'`を一律適用）

**🟢 確定事項3の出口を閉じた（数値閾値の確定）**

12.7〜12.10で蓄積した実測値と464節のクリーン再シード後最終Load実測を基に、`helpers/thresholds.ts`へ`CONFIRMED_ENDPOINT_P95_THRESHOLDS_MS`/`CONFIRMED_ENDPOINT_P99_THRESHOLDS_MS`を定義し、計画の「測定対象」に明記した第一級のSSRページ・主要APIをp95/p99でゲートするようにした（Smoke・Loadに適用。マージンを持たせた値）：

| エンドポイント | 閾値（p95 / p99） | エンドポイント | 閾値（p95 / p99） |
|---|---|---|---|
| `home_ssr` | <1500ms / <1800ms | `posts_portal` | <600ms / <700ms |
| `post_detail_ssr` | <1200ms / <1500ms | `posts_explore` | <700ms / <900ms |
| `mypage_report_ssr` | <1300ms / <1500ms | `posts_like_toggle` | <1000ms / <1300ms |
| `user_profile_ssr` | <1200ms / <1500ms | `posts_comments_create` | <1000ms / <1300ms |
| | | `users_follow_toggle` | <900ms / <1200ms |

統計系・CRUD系の補助エンドポイント（`stats`/`stats_years`/`plans_*`/`notifications_unread_count`/`login`/`auth_csrf`/`posts_comments_list`）は引き続き計測のみ（非ゲート）とした。Stressは従来通り無条件（限界点の記録が目的、`passed: null`）。

**🟡 Spikeのrecovery区間にp99判定を追加、その過程で判明した実際の限界点**

Spikeの設計目的「recovery区間のp99が回復しているか」を、`http_req_duration{scenario:recovery1}`/`{scenario:recovery2}`への実閾値（recovery1: p99<2000ms、recovery2: p99<5000ms、recovery1のクリーンな実測値≈840msを基準）として実装した。

追加した直後の初回実行で、**recovery2のp99が23,613msまで悪化する事象を検出**した。サーバーログを確認すると、人気投稿への同時コメント投稿リクエストのうち7〜8件が、ほぼ同時刻に約396秒（≈8×49.5秒）で完了しており、MySQLの`innodb_lock_wait_timeout`（既定50秒）を複数リクエストが順番に使い切る形で連鎖したと推測される。

原因は`createComment()`が`Comment`のINSERT（投稿行への共有ロックを伴う）を先に行い、その後で同じ`Post`行の`commentCount`をUPDATEして排他ロックへ昇格しようとしていたことだった。Phase 5-B内でトランザクションを **Post行のカウンタ更新 → Comment作成** の順に修正した。これにより同一投稿への書き込みはPost行の取得時点で短時間直列化され、共有ロックから排他ロックへの昇格競合を作らない。

同じ構造を持つ`toggleLike()`・`toggleFollow()`にも展開した。LikeはPost行を、Followは関係する2つのUser行をID順で、子行のCREATE/DELETEより先に`SELECT ... FOR UPDATE`でロックする。Followの2行をID順に固定することで、A→BとB→Aの同時操作におけるロック順序の逆転も防ぐ。コメント・いいね・フォローは各12件の並行操作で、子行の件数と非正規化カウンタの整合性をrepositoryテストで確認する。

**当時の記録（後述の464節の最終再測定で実施済み）**：Spikeの閾値（recovery1: p99<2000ms、recovery2: p99<5000ms）は緩和していない。修正後のクリーン環境でのStress/Spike再計測は未実施だったため、性能改善値をここで確定値としては記録していなかった。再実行で閾値超過が残る場合は、アプリケーションのロック競合ではなく、DB接続プール・CPU・プロセス残留を含めて切り分ける方針としていた。

**🟡 パフォーマンステスト実装レビューへの対応**：Spikeの旧`recovery1`/`recovery2`は40/60VUから5VUへ落とす途中の全リクエストをp99判定しており、回復後の性能を測れていなかった。`rampdown1`/`rampdown2`と、5VUを1分維持する`cooldown1`/`cooldown2`へ分離し、後者だけを既存のp99閾値（2000ms/5000ms）でゲートするよう修正した。Loadの第一級エンドポイントp95/p99も、ramp/rampdownを混ぜず10VU定常の`steady`区間に限定した。

あわせて、レポートのVU値をexecutor予約数の`vus_max`から、現行では`options.scenarios`を解析する`peakWorkloadVUs()`による実ワークロードのピークVUへ変更し、シナリオ表の割合を「HTTPリクエスト比率」ではなく「シナリオ選択確率」と明記した。`run.sh`には`/api/health`疎通とNode.jsプロセス数のpreflightを追加し、既定上限4個を超える場合は明示的な上書きなしに実行を停止する。これらの測定基盤修正後のLoad/Stress/SpikeおよびPlaywright実測値は、後述の464節でクリーン環境にて再確定済みである。

なお、この修正の実装中に**閾値コード自体のバグ**（実閾値をduration専用に置くと、ペアとなる`http_reqs`/`http_req_failed`のプレースホルダが無いとそのsubmetricがmaterializeされず、集約レポートの`requests`が0のまま欠落する）も発見し、`gatingEndpointThresholds()`/`gatingScenarioAuxThresholds()`で常にペアで生成するよう修正した。

**検証時の環境要因について（当時の記録、464節の最終再測定で解消済み）**：閾値確定・recovery判定追加の最終確認としてSmoke→Load→Stress→Spikeを再度実行したところ、Stress（p95 2688ms）・Spike（recovery1 p99=4328ms、recovery2 p99=7624ms、`passed: false`）双方で数値が悪化した。ただし`auth_csrf`（DBアクセスを伴わない軽量エンドポイント）を含む**全エンドポイントが一様に悪化**しており、特定のエンドポイントに起因する回帰ではないことを確認した。同時点でNode.jsプロセスが29個確認されており、このセッション中に蓄積した多数のバックグラウンドプロセスによるマシンリソース枯渇が原因と判断した（アプリ・テストコード自体の問題ではない）。上記の改善率（-70%等）は、この環境要因が生じる前のクリーンな実行で確認したものを採用していた。クリーン再シード後の最終値は464節を参照。

`posts_portal`は3種別とも数百〜1000件超のリクエストが実行され、正しく計測対象に含まれるようになった。p99はp95より一貫して大きい値を示しており（Load 589.6ms > p95 469.9ms、Spike 3145.8ms > p95 2170.4ms）、これまで隠れていた分布の裾（テール）が正しく可視化されるようになった。

**ダッシュボード履歴のFAIL表示について**: `performance/k6/results/`・`e2e/performance/results/`はいずれも`.gitignore`対象であり、成果物には含まれない。過去のFAIL履歴（デッドロックバグ発見時の11%台のエラー率、意図的な閾値破壊テストなど）は「テストが壊れている証拠」ではなく、**改善の経過を示す一次データ**として意図的に残している（本節・12.7〜12.9節への転記が完了した後に整理する）。

**再測定（2026-07-22、測定基盤修正後）**: Node.jsプロセスは約13個であり既定のpreflight上限4個を超えていたため、`PERF_ALLOW_EXTRA_NODE_PROCESSES=1`を明示して実行した。直前のSpikeが同じ水準で正常だったこと、DB非経由の`auth_csrf`を含め全体的な異常遅延がないことを確認した上で、結果を採用した。Load（10VU）は6,953件・エラー率0%・定常`steady` p95 356.9ms / p99 404.2msでPASS。Stress（50VU）は18,522件・エラー率0.022%・50VU区間p95 1096.4ms / p99 1462.8msで、判定なしの限界点記録として採用した。Spike（60VU）は11,455件・エラー率0%でPASSし、5VU定常の`cooldown1` p99 666.3ms（閾値2,000ms）、`cooldown2` p99 657.2ms（閾値5,000ms）を確認した。旧`recovery`のFAILはランプダウン中の高負荷リクエストを回復判定へ混在させた測定設計によるものである。

**最終再測定（2026-07-22、perf DBをクリーンな状態に再シード後）**: `MAXVUS`表示を`peakWorkloadVUs()`（`options.scenarios`から`login`を除いた各シナリオの`vus`/ランプ先`target`の最大値を実際に算出する関数、`helpers/peak-vus.ts`）に置き換え、手動で数値を渡す実装から卒業した。あわせて`run.sh`のpreflightがカウントするNode.jsプロセスを`TripDiary`を含むコマンドラインのものだけに絞り、無関係なプロセス（Cursor/MCP等）を誤検知しないようにした。Smoke/Load/Stress/Spikeの4種別をクリーン再シード後に通しで実行し、以下を最終値として確定する：

| 種別 | 判定 | 件数 | エラー率 | 対象区間 | p95 | p99 |
|---|---|---|---|---|---|---|
| Smoke | PASS | 19 | 0% | 全体 | 120.0ms | 462.9ms |
| Load（10VU） | PASS | 5,680 | 0% | `steady`（定常10VU） | 669.9ms | 743.9ms |
| Stress（50VU） | 限界点記録（判定なし） | 18,755 | 0.005% | `vu50`（最大50VU） | 1010.6ms | 1380.0ms |
| Spike（60VU） | PASS | 11,616 | 0.034% | `cooldown1`/`cooldown2`（5VU定常） | 609.3ms/577.4ms | 654.6ms/603.5ms（閾値2,000ms/5,000ms） |

`maxVUs`表示もSmoke=1・Load=10・Stress=50・Spike=60と、業務シナリオの実際のピークVU数を正しく反映している（`login`用の補助VUは含まない）。

### 12.12 GATE-22種類A対応によるマイページ一覧APIの上限化（2026-07-30）

マイページの「行きたい」「訪問済み」一覧（`findWishlistedPosts`/`findVisitedPosts`）は、本対応まで`limit`引数を受け取りつつも呼び出し側（Server Component）が上限・cursorを指定せず全件取得していた。GATE-22種類A対応で、これらを`GET /api/mypage/wishlist`・`GET /api/mypage/visited`（新設、既定`limit=20`・最大50）経由のcursorページングに変更し、登録件数が多いユーザーでの応答肥大化リスクを解消した（詳細はリスト機能定義書3.3節）。

現時点のk6シナリオ（`scenarios/`）はこれら2エンドポイントを計測対象に含めていない。行きたい/訪問済みリストの登録件数はコメント・フォロワーほど無制限に増加しない想定のため、本PRでは新規シナリオを追加せず、上限なし全件取得の解消をコードレベルの修正として記録するに留める。件数が多いシードユーザーでの負荷試験対象化が必要になった場合は、GATE-22種類B対応（PR-6c、コメント/フォロワー/フォロー中/旅行プランの上限＋cursor新設）で見直すシナリオ構成とあわせて検討する。

Playwrightは4列CSVのログイン修正後に8件すべて通過した。トップページ（TTFB 14ms / FCP 208ms / LCP 576ms / CLS 0）、マイページ集計（11ms / 148ms / 584ms / 0）、投稿詳細（12ms / 112ms / 480ms）であり、操作応答時間もいいね65ms、コメント63ms、検索98ms、追加読込173msですべて閾値内だった。トップページのFCPがまれに未定義となったため、`getEntriesByName()`だけでなく`PerformanceObserver`の`paint`エントリを`buffered: true`で取得するよう計測ヘルパーを修正して再実行した。
