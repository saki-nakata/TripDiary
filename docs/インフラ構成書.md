# TripDiary インフラ構成書

**バージョン:** 2.3
**作成日:** 2026-06-27
**更新日:** 2026-07-25
**作成者:** Nakata Saki

> **注意：** 本ドキュメントは現時点で確定している構成のみを記載する。未定の項目は「未定」と明記する。

---

## 1. インフラ概要

### 設計方針

- AWS を中心としたクラウド構成で統一する
- EC2 で Next.js アプリを稼働、RDS で MySQL を管理、S3 で画像を保管
- 無料枠（Free Tier）の範囲内での運用を目指す
- インフラのコード管理（Terraform 等）は未定

---

## 2. アーキテクチャ図

```
[ユーザー（ブラウザ）]
        ↕ HTTP（HTTPS非採用、後述）
[EC2（Next.js アプリ / Nginx + PM2）]
  ├── フロントエンド（App Router / RSC）
  └── バックエンド（Route Handlers）
        ↕ Prisma（TCP / SSL）
[RDS（MySQL）]
        ↕ AWS SDK（HTTPS）
[S3（画像ストレージ）]
        ↕ タイルAPI（HTTPS）
[Leaflet + OpenStreetMap（地図サービス、APIキー不要）]
```

---

## 3. 開発環境構成

| 項目 | 内容 |
|------|------|
| アプリ | Next.js 開発サーバー（`pnpm dev`、localhost:3000） |
| DB | Docker コンテナで MySQL 8.0 を起動（`docker compose up -d`） |
| DB コンテナ名 | `tripdiary-db` |
| DB 接続情報 | `mysql://tripdiary:tripdiary@localhost:3306/tripdiary` |
| ORM | Prisma 6（`prisma migrate dev` でマイグレーション） |
| 環境変数 | `.env.local`（Next.js 用）・`.env`（Prisma CLI 用）※ともに gitignore 対象 |

---

## 4. サービス構成（本番）

### 4.1 EC2（アプリホスティング）

| 項目 | 内容 |
|------|------|
| 用途 | Next.js アプリのホスティング |
| インスタンスタイプ | t2.micro（Free Tier 対象） |
| OS | Amazon Linux 2023 |
| Webサーバー | Nginx（リバースプロキシ） |
| プロセス管理 | PM2 |
| デプロイ方式 | 未定（GitHub Actions + SSH / AWS CodeDeploy 等） |
| カスタムドメイン | 未定 |
| HTTPS | **非採用（HTTP運用を明示的に受容、2026-07-26確定・2026-07-28再確定）**。独自ドメイン・Elastic IP・Let's Encryptによる正式なHTTPS化はコスト・作業量を理由に見送る。確認用アカウントは使い捨て前提とし、本番の個人情報・パスワードを使い回さないことをREADMEに明記する |
| リージョン | 未定（ap-northeast-1 東京を推奨） |

### 4.2 RDS（MySQL）

| 項目 | 内容 |
|------|------|
| 用途 | MySQL データベースのホスティング |
| インスタンスクラス | db.t3.micro（Free Tier 対象） |
| エンジン | MySQL 8.0 |
| ストレージ | 20GB（gp2、Free Tier 対象） |
| バックアップ | 未定（自動バックアップ保持期間は実装時に設定） |
| 接続方式 | Prisma 経由（`DATABASE_URL` 環境変数） |
| SSL | RDS がデフォルトで提供 |
| マルチAZ | 未定（本番運用時に検討） |

### 4.3 S3（画像ストレージ）

| 項目 | 内容 |
|------|------|
| 用途 | 投稿写真・プロフィール画像のストレージ |
| プラン | Free Tier（5GB ストレージ・月 20,000 GETリクエスト・2,000 PUTリクエスト） |
| アップロード方式 | サーバーサイドから AWS SDK（@aws-sdk/client-s3）を使用（実装済み：`src/lib/s3.ts`） |
| オブジェクトキー | `uploads/{ownerUserId}/{randomUUID}.{ext}`（所有者スコープ付き。他人のオブジェクトを誤って削除・上書きできないようキーにアップロード実行者のユーザーIDを含める） |
| アクセス制御 | バケットポリシーで公開読み取り設定（バケット自体の作成はPhase 6-Bで実施。バケットポリシー・Public Access Block設定は6-A本文「S3公開方式」参照） |
| 画像変換 | 未定（サムネイル生成等は実装時に検討） |
| オブジェクト削除 | 投稿削除・画像差し替え（差分削除）・アバター差し替え時に実装済み（`deleteOwnedObjectsByUrl`、ベストエフォート・DB操作成功後に実行）。削除直前に`findStillReferencedUrls`で他の投稿・アバターからまだ参照されていないかを確認し、参照が残っていれば削除しない（旅行プランからの画像引き継ぎ`presetImageUrl`等で同一S3オブジェクトが複数レコードから参照され得るための保護）。アップロード済みだが未参照のまま残る画像の定期クリーンアップは未実装（Phase 6-Bへ引き継ぎ。判定方法・猶予期間等は実装計画書 Phase 6-A本文参照） |
| **Phase 6-Aメモ（`remotePatterns`・CSP）** | **実装済み**：`next.config.ts` の `images.remotePatterns` と `CSP_REPORT_ONLY` の `img-src` は、`AWS_REGION`/`AWS_S3_BUCKET_NAME` から組み立てた自バケットのホスト名（`<bucket>.s3.<region>.amazonaws.com`）に両方とも限定するよう変更済み（`src/lib/s3-url.ts` の `getBucketHostname()` を両ファイルで共有し、絞り忘れを防ぐ構成）。両変数が未設定の場合（CIビルド等）はS3関連のエントリを単純に省略し、ビルド自体は成功する。**6-Bで実際にEC2上で本番ビルドを実行する際は、`AWS_REGION`/`AWS_S3_BUCKET_NAME` を必ずビルド実行環境へ設定すること**（設定漏れがあるとビルドは成功するが画像だけ表示されない、気づきにくい壊れ方をする） |

### 4.4 Leaflet + OpenStreetMap（地図サービス）

| 項目 | 内容 |
|------|------|
| 用途 | 旅行スポットの位置を地図で表示 |
| ライブラリ | Leaflet（`react-leaflet`）+ OpenStreetMap タイル |
| APIキー | 不要（Mapboxから変更済み。当初 Mapbox GL JS を想定していたが、実装時に Leaflet + OpenStreetMap に変更した） |

---

## 5. 環境変数

| 変数名 | 設定先 | 説明 |
|--------|--------|------|
| `DATABASE_URL` | EC2 `.env.local` | MySQL 接続 URL（RDS） |
| `AUTH_SECRET` | EC2 `.env.local` | Auth.js のシークレットキー |
| `AUTH_URL` | EC2 `.env.local` | アプリの URL |
| `AWS_REGION` | EC2 `.env.local` | S3 バケットのリージョン。ビルド時にも `next.config.ts` が参照するため、ビルド実行環境にも設定が必要 |
| `AWS_S3_BUCKET_NAME` | EC2 `.env.local` | S3 バケット名（変数名は実装時に `AWS_S3_BUCKET_NAME` へ確定。上記と同様ビルド実行環境にも設定が必要） |
| `AWS_ACCESS_KEY_ID` | 原則未設定 | 本番はEC2にアタッチしたIAMロールの一時credentialsを`@aws-sdk/client-s3`のデフォルトプロバイダチェーンが自動的に使うため、長期アクセスキーは設定しない。ローカル開発でのみ`.env.local`に一時的な認証情報を設定する場合に使用 |
| `AWS_SECRET_ACCESS_KEY` | 原則未設定 | 用途は上記と同じ |

---

## 6. デプロイ手順

### 初回セットアップ（未定：詳細は実装時に確定）

```bash
# 1. AWS コンソールで EC2 インスタンスを作成（t2.micro / Amazon Linux 2023）

# 2. EC2 に Node.js・Nginx・PM2 をインストール
sudo dnf install -y nodejs nginx
pnpm add -g pm2

# 3. RDS で MySQL インスタンスを作成し DATABASE_URL を取得

# 4. S3 バケットを作成し、EC2 用 IAM ロール（S3 最小権限ポリシー付き）を作成・アタッチ
#    （長期アクセスキーは発行しない。IAMユーザーは使わない。Terraformでのコード管理は6-B参照）

# 5. EC2 に .env.local を配置し環境変数を設定

# 6. DB マイグレーション（初回）
pnpm prisma migrate deploy

# 7. PM2 でアプリ起動
pm2 start pnpm --name "tripdiary" -- start
pm2 startup
pm2 save
```

### 通常デプロイ

```bash
# 未定（GitHub Actions + SSH でのデプロイ等を実装時に検討）
git push origin main
```

---

## 7. コスト概算

| サービス | プラン | 月額コスト |
|---------|--------|-----------|
| EC2（t2.micro） | Free Tier（12ヶ月） | $0〜 |
| RDS（db.t3.micro） | Free Tier（12ヶ月） | $0〜 |
| S3 | Free Tier（5GB まで） | $0〜 |
| Leaflet + OpenStreetMap | 無料（APIキー不要） | $0 |
| **合計** | | **$0〜/月（Free Tier 期間中）** |

> Free Tier 期間（12ヶ月）終了後は EC2・RDS の費用が発生する（未定）

---

## 8. セキュリティ方針

| 項目 | 方針 |
|------|------|
| パスワード | bcrypt（ソルトラウンド：12）でハッシュ化 |
| セッション | Auth.js JWT Strategy（HttpOnly Cookie に JWT を保存、DB への書き込みなし） |
| 環境変数 | .env.local はリポジトリにコミットしない（.gitignore で除外） |
| AWS IAM | S3 アクセス用に EC2 インスタンスへ IAM ロール（`uploads/*`限定の`s3:PutObject`/`s3:DeleteObject`のみを許可する最小権限ポリシー）をアタッチ。本番では IAM ユーザー・長期アクセスキーは使用しない |
| AWS IAM キー | 本番はIAMロールの一時credentialsを`@aws-sdk/client-s3`のデフォルトプロバイダチェーンが自動的に使用し、長期キーは発行しない。ローカル開発でのみ`.env.local`に一時的な認証情報を設定する場合があり、その場合もサーバーサイドのみで使用しクライアントに公開しない |
| S3 バケット | Public Access Block（`BlockPublicPolicy`/`RestrictPublicBuckets`のみ無効化、ACL関連は有効のまま維持）・バケットポリシー（`uploads/*`に限定した匿名`s3:GetObject`のみ許可）は6-A本文・4.3節で確定済み。実際のTerraformでの構築・実機確認は6-Bで実施 |
| HTTPS | **非採用（HTTP運用、4.1節参照）** |
| セキュリティグループ | EC2：**80のみ公開（443は開放しない）**、3306（MySQL）は EC2 からのみ許可 |
| CORS | 未定（実装時に検討） |
| レート制限 | 実装済み（Phase 5-D、`src/lib/rate-limit.ts`）。固定ウィンドウ方式のインメモリカウンタで、ログイン（メール単位5回/15分）・サインアップ（IPアドレス単位10回/時、`X-Forwarded-For`の最左値を採用）・画像アップロード（ユーザーID単位、投稿30回/時・アバター10回/時）に適用。**単一プロセス前提**（EC2 t2.micro 1台構成のPhase 6を想定）のため、複数インスタンスへスケールする場合はRedis等の外部ストアへの置き換えが必要。CI（`.github/workflows/ci.yml`の`e2e`ジョブ）では`ENABLE_TEST_ENDPOINTS="true"`により無効化（本番環境変数には絶対に設定しないこと） |
| レート制限 | 実装済み（Phase 5-D、`src/lib/rate-limit.ts`）。固定ウィンドウ方式のインメモリカウンタで、ログイン（メール単位5回/15分）・サインアップ（IPアドレス単位10回/時、`X-Forwarded-For`の最左値を採用）・画像アップロード（ユーザーID単位、投稿30回/時・アバター10回/時）に適用。**単一プロセス前提**（EC2 t2.micro 1台構成のPhase 6を想定）のため、複数インスタンスへスケールする場合はRedis等の外部ストアへの置き換えが必要。CI（`.github/workflows/ci.yml`の`e2e`ジョブ）・perf（`.env.perf`）では`DISABLE_RATE_LIMIT_FOR_TESTS="true"`により無効化（本番環境変数には絶対に設定しないこと。2026-07-29 GATE-03対応で`ENABLE_TEST_ENDPOINTS`〔`/api/test/cleanup`用〕から分離） |
| セキュリティヘッダー | 実装済み（Phase 5-D、`next.config.ts`）。`X-Content-Type-Options`/`X-Frame-Options`/`Referrer-Policy`を常時付与、`Strict-Transport-Security`は本番のみ付与するコードになっているが、**HTTPS非採用のためHTTP運用では実質無効**（ブラウザはHTTPS接続でのみHSTSヘッダーを解釈するため）。CSPは`Content-Security-Policy-Report-Only`で運用開始（App Routerのhydrationがnonceなしinline scriptを使うため、まず違反レポートを収集してから段階的にstrict化する方針） |

---

## 9. 監視・ロギング

| 項目 | 内容 |
|------|------|
| アプリログ | 構造化ログ（pino、Phase 2.5-C導入済み）。出力先は現状PM2ログ／標準出力。CloudWatch等への集約は本番デプロイ（Phase 6）時に導入する（EC2のPM2ログを CloudWatch Logs エージェントで転送する想定。具体的な保持期間・アラーム設定は未定） |
| アクセスログ | Phase 5-Dで正常系アクセスログ（`method`/`path`/`status`/`duration_ms`/`userId`/`requestId`）を全APIに追加済み（`src/lib/request-logging.ts`）。Phase 5-E後の是正で同一`requestId`を`handleApiError()`のエラーログにも付与し、アクセスログとエラーログを突き合わせ可能にした。出力先はアプリログと同じくpino経由 |
| DB モニタリング | RDS のダッシュボードで確認 |
| ヘルスチェック | `GET /api/health`（Phase 5-D実装済み、DB疎通確認込み、認証不要）。Nginx/PM2からのヘルスチェック・k6の疎通確認の両方で利用する想定。Swagger（`/api-docs`）にも`Health`タグで登録済み |
| エラー監視 | 連携ポイントのみ実装済み（Phase 2.5-D、サーバー側 `src/lib/monitoring.ts`、クライアント側 `src/lib/monitoring-client.ts`）。`global-error.tsx` からクライアント側の送信口を呼び出す。実際のSaaS（Sentry想定）導入・DSN発行は未定（ユーザー確認後に実施）。詳細は `docs/ログ運用設計書.md` 4節参照 |

---

## 10. CI構成（GitHub Actions）

`.github/workflows/ci.yml` で、プッシュ・PR時に以下5ジョブを実行する。

| ジョブ | 内容 |
|--------|------|
| `lint` | ESLint |
| `typecheck` | `tsc --noEmit` |
| `build` | `pnpm install --frozen-lockfile` → `pnpm build`（Next.jsのビルド可否を検証。6-A2で`@node-rs/bcrypt`〔napi-rsのネイティブアドオン〕を導入したため新設） |
| `test` | Vitest（`mysql-test` コンテナで実DB検証）。カバレッジ閾値（Statements 85% / Branches 75% / Functions 78% / Lines 86%）を下回るとジョブが失敗する |
| `e2e` | Playwright E2E。`continue-on-error` は設定していないため失敗時はCI上に赤く表示される。一方でブランチ保護の必須ステータスチェックには含めていない（TimeLine方式。運用実績を見て今後必須化を判断する） |

ブランチ保護の必須ステータスチェックは `lint` / `typecheck` / `build` / `test` の4つ。`e2e` は非必須だが `continue-on-error: true` は使わず失敗を隠さない運用としている。

Playwrightブラウザのキャッシュは `actions/cache`（`pnpm-lock.yaml`キー）で保存し、OS依存ライブラリ（`libnss3`等）は毎回`playwright install-deps chromium`で導入、ブラウザ本体のみキャッシュ未取得時に取得する2段階方式（詳細は`docs/テスト設計書.md`参照）。

---

## 11. 関連ドキュメント

| ドキュメント | ファイル |
|------------|---------|
| 要件定義書 | [要件定義書.md](要件定義書.md) |
| DB 設計書 | [DB設計書.md](DB設計書.md) |
