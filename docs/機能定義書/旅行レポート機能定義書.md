# 旅行レポート機能定義書

**バージョン:** 1.5
**作成日:** 2026-06-28
**更新日:** 2026-07-14
**作成者:** Nakata Saki

> ✅ **2026-07-09 更新：** Phase 4-C にて実装完了。月別費用・カテゴリ別集計はDB側の日付関数（`groupBy`でのMONTH抽出等）を使わず、対象年の投稿を1クエリで取得しService層（JS側）で集計する方式で実装（Phase 2-Gで確立した「取得は1クエリ+メモリ内グルーピング」方針を踏襲）。「訪問スポット数」「投稿数」の2カードは同値のため `StatsResponse` に専用フィールドは追加せず `totalPosts` を共用している。
>
> ✅ **2026-07-11 更新：** 年選択に「全期間」を追加（デフォルト選択）。月別グラフは「月別旅行費用」から「月別投稿数」に変更し、全期間選択時は「年別投稿数の推移」を表示するよう仕様変更（費用の年次比較よりも投稿数の推移の方が振り返り体験として有用と判断）。いずれも折れ線グラフ（ピンクグラデーション）で表示し、右側にY軸目盛り（件数）を表示する。
>
> ✅ **2026-07-13 更新：** 対象外仕様としていた「日本地図の塗り分け表示」を実装（3.3節）。訪問エリアバッジ一覧の上に都道府県ごとの塗り分け地図を追加した。あわせて訪問エリアバッジの並び順を、初回訪問日順から地図と同じ地理順（北海道→沖縄県）に変更した。
>
> ✅ **2026-07-13 更新（表示順の見直し）：** まとめカードと年別タイムラインの間のブロック順を「カテゴリ別投稿数→投稿数の推移→訪問エリア」から「**カテゴリ別投稿数→訪問エリア→投稿数の推移**」に変更（3.2〜3.4節の番号を入れ替え）。地図による訪問エリアのブロックを同種のグラフ（バーグラフ・折れ線グラフ）の間に挟むことで視覚的なアクセントにしつつ、「投稿数の推移」を直後の「年別タイムライン」（3.5節）に隣接させ、時系列の文脈が地理の話で分断されないようにした。
>
> ✅ **2026-07-14 更新（カードの追加・再配色、表示順の再見直し）：**
> - まとめカードを**7枚→8枚**に増やし、「🏷️ カテゴリ数」（投稿したカテゴリの種類数、`categoryBreakdown.length`）を追加（3.1節）。カテゴリ別投稿数の内訳グラフとは重複しない「多様性」の指標として追加した
> - カードの並びを「記録量（投稿数・写真枚数・訪問スポット数）→広がり（訪問エリア数・最多訪問エリア・カテゴリ数）→旅行実績（旅行回数・旅行費用合計）」というテーマ別グルーピングに変更（3.1節）
> - 8枚それぞれに重複しないグラデーション配色を割り当て（カテゴリ数＝青紫→マゼンタ、旅行費用合計＝金→クリーム色）
> - まとめカードと年別タイムラインの間のブロック順を「カテゴリ別投稿数→訪問エリア→投稿数の推移」から「**訪問エリア→カテゴリ別投稿数→投稿数の推移**」に再度変更（3.2〜3.4節の番号を入れ替え）。まとめカードの並び（訪問エリア系→カテゴリ数）とブロックの並びを一致させ、カードクリック時のスクロール体験を自然にした
> - 「投稿数の推移」の見出しを、年選択に応じて「月別投稿数の推移」/「年別投稿数の推移」と切り替えていたのを「**投稿数の推移**」に固定表示（3.4節）。グラフが表示するデータ自体（月別/年別）は従来通り年選択に応じて切り替わる
> - 訪問エリア見出しの件数表示「（{件数}件）」、年別タイムライン見出しの件数表示「（{件数}件）」をいずれも削除（3.2節・3.5節）
> - 配色調整: 訪問エリアバッジをピンク枠線＋淡黄色背景＋明るいオレンジ文字に変更、カテゴリ別投稿数のバーグラフをバイオレット85%→明るいピンクのグラデーションに変更、投稿数の推移の折れ線グラフをピンクの比率がより広いグラデーションに調整（3.2節・3.3節・3.4節）
>
> ✅ **2026-07-14 追加更新：** 訪問エリア・カテゴリ別投稿数・投稿数の推移の3見出しに、選択中の年（または「全期間」）を示す接頭辞を再度追加（「{yearLabel}の訪問エリア」「{yearLabel}のカテゴリ別投稿数」「{yearLabel}の投稿数の推移」、3.2〜3.4節）。まとめカード見出し（「{yearLabel}の旅まとめ」）と表記を揃え、どの期間のデータを見ているか各ブロック単体でも分かるようにした。

---

## 1. 機能概要

自分の旅行記録を年別に集計・可視化する振り返り機能。
Spotify Wrapped 風の年間まとめカードをはじめ、カテゴリ分析・費用グラフ・訪問エリア一覧・年別タイムラインを提供する。
4Travel にはない付加価値として、旅行記録アプリならではの統計体験を実現する。

---

## 2. ユーザーストーリー

| ID | ストーリー |
|----|-----------|
| US-01 | ユーザーとして、今年の旅行を一枚のまとめカードで振り返りたい |
| US-02 | ユーザーとして、何県を訪れたか・何枚写真を撮ったかを数字で確認したい |
| US-03 | ユーザーとして、過去の年の旅行もドロップダウンで切り替えて確認したい |
| US-04 | ユーザーとして、自分がどのカテゴリ（グルメ・自然など）をよく訪れるか傾向を知りたい |
| US-05 | ユーザーとして、月ごと・年ごとの投稿数の推移をグラフで確認したい |
| US-06 | ユーザーとして、全年の旅行記録を年別タイムラインで一覧したい |

---

## 3. 機能詳細

### 3.1 年間旅行まとめカード（Wrapped 風）

- URL：`/mypage?tab=report`（年選択時は `/mypage?tab=report&year=YYYY`）
- 年選択ドロップダウンで対象年を切り替え（投稿がある年に加えて「全期間」を選択肢の先頭に表示。初期表示は「全期間」）
- 年選択は `router.replace()` でURLの `year` クエリパラメータに同期される（2026-07-12追加）。ブラウザの戻る操作で他画面から本画面に戻った際も選択中の年が復元される
- 選択年（または全期間）の投稿・プランデータを集計し、以下 8 枚のカードをグラデーション背景でアニメーション表示
- カードは「記録量→広がり→旅行実績」の3グループでテーマ別にグルーピングし、以下の順で表示する（2026-07-14変更）

| カード | 集計対象 |
|-------|---------|
| 投稿数 | 選択年（全期間の場合は全年）の Post 件数（訪問スポット数と同値） |
| 写真枚数 | 選択年（全期間の場合は全年）の Post に紐付く PostImage の合計枚数 |
| 訪問スポット数 | 選択年（全期間の場合は全年）の Post 件数 |
| 訪問エリア数 | 選択年（全期間の場合は全年）の Post の `location` ユニーク件数 |
| 最多訪問エリア | `location` の最頻値（同数の場合は最初に出現したもの） |
| カテゴリ数 | 選択年（全期間の場合は全年）の `categoryBreakdown` の件数（投稿したカテゴリの種類数、2026-07-14追加） |
| 旅行回数 | `completed=true` かつ `startDate` が選択年（全期間の場合は全年）の Plan 件数 |
| 旅行費用合計 | 選択年（全期間の場合は全年）の Post の `cost` 合計（記録なしの場合は「—」） |

- カードは `animation-delay` を 0.1s ずつずらして順番にフェードインアニメーション
- 各カードはクリックすると関連するブロックへスムーズスクロールする（投稿数→3.4節投稿数の推移、訪問スポット数→3.5節年別タイムライン、写真枚数・旅行費用合計→自分の投稿一覧、訪問エリア数・最多訪問エリア→3.2節訪問エリア、カテゴリ数→3.3節カテゴリ別投稿数、旅行回数→旅行プラン一覧）

### 3.2 訪問エリアバッジ一覧・都道府県塗り分け地図

- 見出しは「{yearLabel}の訪問エリア」（例: 「2026年の訪問エリア」「全期間の訪問エリア」）。件数表示は行わない（2026-07-14変更：当初は見出しに「（{件数}件）」を表示していたが削除）
- `location` が未設定の投稿は集計から除外
- 表示順は訪問日順ではなく、`src/lib/constants.ts` の `LOCATIONS` 配列の並び順（北海道→沖縄県→海外の地理順・都道府県塗り分け地図と同じ基準）に統一する（2026-07-13変更：当初は初回訪問日順だったが、地図と一覧の並び順を揃えるため変更）
- バッジ一覧の直前に日本地図（SVG）を表示し、`visitedLocations` に含まれる都道府県を緑色、それ以外をグレーで塗り分ける（2026-07-13追加）
  - 地図データは Geolonia の都道府県別 SVG（`data-code="1"`〜`"47"`）を使用し、`data-code` の並び順が `src/lib/constants.ts` の `LOCATIONS` 配列（北海道〜沖縄県）と一致することを利用して県名と対応付ける
  - 新規 API・DB変更は不要（既存の `visitedLocations` をそのまま可視化）
- バッジ配色: ピンク枠線＋淡黄色背景＋明るいオレンジ文字（2026-07-14変更：当初は紫系の枠線・背景だった）

### 3.3 カテゴリ別投稿数（CSS バーグラフ）

- 見出しは「{yearLabel}のカテゴリ別投稿数」（例: 「2026年のカテゴリ別投稿数」「全期間のカテゴリ別投稿数」）
- 選択年（全期間の場合は全年）の Post を `category` でグルーピングし件数を集計
- 件数降順で CSS バーグラフ（横棒）として表示
- 最多カテゴリを 100% として各カテゴリの幅を相対表示
- バーの配色はバイオレット（左側85%）→明るいピンク（右端15%）のグラデーション（2026-07-14変更：当初は黄橙→ピンクのグラデーションだった）

### 3.4 投稿数の推移（折れ線グラフ）

- 見出しは「{yearLabel}の投稿数の推移」（例: 「2026年の投稿数の推移」「全期間の投稿数の推移」）。文言自体は年選択に関わらず「投稿数の推移」で固定し、先頭の年ラベルのみが切り替わる（2026-07-14変更：当初は選択内容に応じて「月別投稿数の推移」/「年別投稿数の推移」と文言自体を切り替えていた）
- 特定の年を選択している場合：選択年の Post を訪問月（`visitedAt` の月）でグルーピングし件数を集計した月別データを表示
  - 1月〜12月を全て表示（投稿がない月は 0 件）
- 「全期間」を選択している場合：全 Post を訪問年（`visitedAt` の年）でグルーピングし件数を集計した年別データを表示
- いずれもピンクグラデーションの折れ線グラフ（SVG、Reactハンドロール実装）で表示し、右側にY軸目盛り（件数）と横方向のグリッド線を表示する。ピンクの占める比率を広げたグラデーション（2026-07-14変更）
- 月別/年別ラベルは各データ点の直下（SVG上のx座標に対応する位置）に表示する

### 3.5 年別タイムライン

- 見出しに件数表示は行わない（2026-07-14変更：当初は見出しに「（{件数}件）」を表示していたが削除）
- **全年**の自分の投稿を `visitedAt` の年でグルーピング
- 各年セクションは `visitedAt` 降順（新しい訪問日が上）で表示
- **各年 3 件まで表示**し、4 件目以降は「▼ 残りN件を表示」ボタンでトグル展開
- 「▲ 折りたたむ」で再び 3 件に戻せる
- 各行のサムネイル・タイトル・エリア・訪問日をクリックで投稿詳細へ遷移

---

## 4. 使用 API

| Method | エンドポイント | 説明 | 認証 |
|--------|--------------|------|------|
| GET | `/api/stats/years` | 投稿がある年一覧取得 | 必要 |
| GET | `/api/stats?year=YYYY\|all` | 年別統計データ取得（`year=all` で全期間集計） | 必要 |

**GET `/api/stats?year=YYYY|all` レスポンス型**

```typescript
type StatsResponse = {
  year: number | "all"
  completedPlans: number
  totalPosts: number
  totalPhotos: number
  totalCost: number
  visitedLocations: string[]
  topLocation: string | null
  categoryBreakdown: { category: string; count: number }[]
  monthlyPostCount: { month: number; count: number }[]   // 特定の年を選択した場合のみ値を持つ（全期間選択時は空配列）
  yearlyPostCount: { year: number; count: number }[]      // 全期間を選択した場合のみ値を持つ（特定の年選択時は空配列）
}
```

---

## 5. バリデーション仕様

| 項目 | 内容 |
|------|------|
| 年選択 | 自分の投稿が 1 件以上存在する年に加えて「全期間」を選択肢に表示（初期選択は「全期間」） |
| 投稿ゼロ時 | 「YYYY年の旅の記録がありません。」（全期間選択時は「全期間の旅の記録がありません。」）メッセージを表示 |
| location 未設定 | エリア集計・訪問エリア表示から除外（エラーにしない） |
| cost 未設定 | 費用合計を 0 として集計（カード表示は「—」） |

---

## 6. 制約・前提条件

- 閲覧できるのは自分のデータのみ（他ユーザーのレポートは非公開）
- `location` は投稿作成・編集時にドロップダウンで選択する必須項目
- 旅行回数の集計は Plan の `completed` フラグと `startDate` の年で判定する。`startDate` 未設定のプランは集計から除外
- タイムラインは選択年に関わらず全年分を表示する（年ドロップダウンの切り替えはまとめカード・グラフ・エリアバッジのみに影響）

---

## 7. 対象外仕様

| 機能 | 理由 |
|------|------|
| 他ユーザーのレポート閲覧 | プライベートな統計情報のためスコープ外 |
| SNS シェア（画像書き出し） | スコープ外 |
| 費用の年次比較グラフ | 「全期間」選択時は年別投稿数の推移グラフを提供する方針とし、費用の年次比較は対象外 |

---

## 8. 関連ドキュメント

| ドキュメント | ファイル |
|------------|---------|
| 要件定義書 | [../要件定義書.md](../要件定義書.md) |
| DB 設計書 | [../DB設計書.md](../DB設計書.md) |
| API 仕様書 | [../API仕様書.md](../API仕様書.md) |
| 画面設計書（S-17） | [../画面設計書.md](../画面設計書.md) |
| 旅行プラン機能定義書 | [旅行プラン機能定義書.md](旅行プラン機能定義書.md) |
| 投稿機能定義書 | [投稿機能定義書.md](投稿機能定義書.md) |
